# Roadmap — BillWatch
**Maturity:** HIGH (live, web+iOS+Android). **Wedge:** simpler/cheaper *federal-only* consumer tracker vs FastDemocracy. **Stopping point:** federal tracking + digests + alerts + AI summaries, mobile at parity → maintain; don't chase 50-state coverage.

## P1 — the consumer wedge (next ~2 quarters)
- [ ] [P1] [decomposed] Weekly digest (grouped by stage/topic) + endpoint + view {cat: backend+web; size: M; multifile: yes; research: repo}
- [ ] [P1] [decomposed] Real-time alerts on tracked topics/bills (rules + match) {cat: backend+web; size: M; multifile: yes; research: repo}
- [ ] [P1] [decomposed] Plain-English "what changed" status summaries {cat: backend; size: S; multifile: no; research: repo}
## P2 — mobile parity + polish (~q3-4)
- [ ] [P2] [decomposed] iOS/Android feature parity with web (tracking, digests, alerts) — researched 2026-09-09: both apps are NATIVE (Swift/SwiftUI iOS, Kotlin Android — not Flutter/cross-platform as earlier notes assumed). They've already DIVERGED from EACH OTHER, not just from web: iOS has AICoachingView/AdvancedAnalyticsView/CareerHistoryTimelineView/LegislatorFinanceView/RecommendationsView that Android lacks; Android has BillTrackingScreen/AdvancedFilterScreen/PaywallScreen/TopicsScreen that iOS lacks. This is a 3-way gap (mobile-vs-mobile as much as mobile-vs-web), not a simple "port web to mobile." First real task: audit each of the ~30 backend routers + ~19 web views against what each native app actually consumes, then close mobile-vs-mobile gaps first (cheaper, both apps already have the muscle memory) before mobile-vs-web. {cat: mobile; size: L; research: web}
- [ ] [P2] [decomposed] Legislator profiles + scorecards surface polish {cat: web; size: M; multifile: yes; research: repo}
- [ ] [P2] [decomposed] Committee hearing & floor-activity calendar for tracked bills — Surface a "This Week in Congress" view showing scheduled committee hearings/markups for bills a user follows, pulled from Congress.gov's documented `/committee-meeting` endpoint (filterable by chamber/date range, already the same data source BillWatch's `committee_sync_service` and `committees` router consume). Fits the wedge: federal-only, single unified calendar (not 50-state schedules), and it's a natural next hop from committee data already synced rather than new infrastructure. Ties directly into existing alert/digest plumbing (a hearing becomes another alert-worthy "what changed" event). {cat: backend+web; size: M; multifile: yes; research: none — completed 2026-09-10, sources: github.com/LibraryOfCongress/api.congress.gov CommitteeMeetingEndpoint.md, congress.gov/help/committee-schedule-meetings-hearings}
- [ ] [P2] [decomposed] Shareable bill status cards — One-tap "share" on any tracked bill generates a branded image/link card (bill number, plain-English status line, latest vote tally, BillWatch logo) for text/social, reusing the existing plain-English status-summary text (`status_change_summary.py`) and vote data (`models/vote.py`) already in the DB — no new data source, just an OG-image/render endpoint plus a share button in the 3 clients. Fits the wedge as a near-zero-cost, high-leverage growth loop for a solo dev (free user acquisition vs. FastDemocracy's sales-led enterprise motion) without adding any tracking scope beyond federal bills already followed. {cat: backend+web; size: S; multifile: yes; research: none — completed 2026-09-10, sources: FastDemocracy.com feature review (G2/SourceForge), GovTrack.us feature survey}
## P3 — depth (~year 2)
- [ ] [P3] [decomposed] Campaign-finance / vote-record insights as a light premium tier — CORRECTED 2026-09-09: this was tagged needs-research but the core work is ALREADY BUILT — `app/services/fec_api.py` is a real OpenFEC client (api.open.fec.gov), with `campaign_finance_sync_service.py`, `models/campaign_finance.py`, and a router already exposing `/legislator/{id}/finance` + `/disclosures` (tested: tests/test_fec_api.py). The ONLY missing piece is the "premium tier" gate itself — no premium/subscription/tier check currently exists on these endpoints. Decomposes into: (1) add a premium-tier check to the finance/disclosures endpoints (reuse whatever gate billing.py/PremiumView already uses elsewhere), (2) a light paywall/blur treatment on LegislatorFinanceView (iOS) for non-premium users. Small, NOT a research item. {cat: backend+web; size: S (was M); research: none — verified against current code 2026-09-09}
- [ ] [P3] [decomposed] Plain-English bill "odds of passage" indicator — A lightweight, transparent heuristic (not ML) scoring a bill's chance of advancing, based on stage, days-since-introduction, committee, and historical base rates computed from BillWatch's own synced bill-history data (`bill_sync_service`, `bill_stage.py`), presented as a plain-language line ("similar bills at this stage became law ~12% of the time") consistent with BillWatch's existing "what changed" plain-English framing. Deliberately simpler than GovTrack's regression-based Prognosis model to keep it cheap to build/maintain solo, while still closing a visible gap vs. GovTrack. Federal-only and additive to existing bill-detail/digest surfaces, no new external data source required. {cat: backend+web; size: M; multifile: yes; research: none — completed 2026-09-10, sources: GovTrack.us "New on GovTrack: Bill Prognosis" + "Even Better Bill Prognosis" methodology posts}
- [ ] [P3] [decomposed] "Bills like this" similarity finder — Surface companion/duplicate/related bills (e.g. House and Senate versions of the same policy) on a bill's detail page, using lightweight embedding similarity over bill titles/AI summaries already generated by the existing `llm_service`/`bill_summary_service` pipeline — no new AI vendor or infra, just an embedding step added to a summary job that already runs. Matches GovTrack's well-liked "Find Similar Bills" feature but stays federal-only and reuses paid-for AI calls already in the budget rather than adding a new integration surface. {cat: backend+web; size: M; multifile: yes; research: none — completed 2026-09-10, sources: GovTrack.us "Find Similar Bills" feature docs}
## Maintenance
- [ ] [P4] [decomposed] Add unit tests for advanced_auth router — app/routers/advanced_auth.py (29 lines, GET /login-history) has zero test coverage anywhere in tests/ (verified via repo-wide grep for "login-history"/"advanced_auth"); add a test file covering the happy path and an unauthenticated 401 case. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P4] [decomposed] Add unit tests for recommendations router — app/routers/recommendations.py (4 GET endpoints: bills-for-me, legislators-for-me, trending-bills, similar/{bill_id}) has no dedicated router test; existing tests/test_trending.py only covers the underlying service's generate_trending_recommendations(), not these endpoints. Add tests/test_recommendations_router.py. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Add unit tests for email_service.py — app/services/email_service.py (309 lines) has zero test coverage despite being used by routers/feedback.py, digest_service.py, and status_change_alert_service.py; test the SendGrid-API-first/SMTP-fallback branching and failure handling with mocked urllib/smtplib calls. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P4] [decomposed] Delete two unused stub services — app/services/github_integration_service.py and app/services/legislator_relationship_service.py both return hardcoded fake data (e.g. "Senator {i}", "org/bill-tracking-{i}") and are never imported by any router, job, or other service (verified via repo-wide grep for both filenames); remove them as dead code. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P4] [decomposed] Add unit tests for apiError.ts — billwatch-web/src/utils/apiError.ts (getErrorMessage/getErrorStatus, 43 lines) is used across 9 files for API error handling but has no test file despite every sibling util in src/utils/ having one; add apiError.test.ts covering FastAPI string detail, Pydantic array-of-errors detail, and non-Axios-shaped errors. {cat: web; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Fix activity-logs audit-report fake-data endpoint — app/routers/activity_logs.py's GET /api/activity-logs/audit-report still calls the hardcoded-fake ActivityLoggingService.get_audit_report() (static total_events: 5420, users_active: 45, canned events_by_type — never touches the DB). app/services/security_audit_service.py's own docstring says it "replaces the fake-data stubs (advanced_auth_service / activity_logging_service)", and app/routers/advanced_auth.py already migrated its login-history endpoint to SecurityAuditService.get_login_history(); audit-report was missed. Wire it to SecurityAuditService.get_audit_log() (same AuthEvent model, already supports event_type filtering) adding a created_at >= cutoff filter for the days param, and update the router test. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P4] [decomposed] Add tests for the two untested api_docs endpoints — app/routers/api_docs.py has 3 GET endpoints (/overview, /endpoints/{endpoint}, /auth); only /overview has smoke coverage (tests/test_mounted_routers.py::test_api_docs_overview_public). GET /api/docs/endpoints/{endpoint} and GET /api/docs/auth have zero router-level test coverage despite their backing APIDocumentationService methods already being unit-tested in tests/test_api_documentation_service.py. Add the two missing router tests. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P4] [decomposed] Add tests for GET /api/topics/{slug} — app/routers/topics.py's list-endpoint caching is already tested (tests/test_performance_caching.py), but get_topic_by_slug (the /{slug} route) has zero coverage: no test for its 404 "Topic not found" branch, and no test asserting is_followed reflects current_user.followed_topics. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P4] [decomposed] Add unit tests for LegislatorStatisticsService — app/services/legislator_statistics_service.py (93 lines, voting-record stats, kept separate from campaign finance per its own module docstring) has zero test coverage (verified: the only similarly-named hits are LegislatorFinanceService.get_voting_record_summary tests, a different class). Cover get_legislator_statistics's zero-division guard (yea/nay percentage when total_votes is 0) and legislator-not-found branch (returns {}), plus get_voting_record's join/ordering. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P4] [decomposed] Add unit tests for NotificationService — app/services/notification_service.py (183 lines, Firebase push notifications; consumed by app/jobs/scheduler.py, app/routers/preferences.py, app/services/user_preferences_service.py) has zero test coverage. Mock firebase_admin to cover _init_firebase's no-credentials-configured warning path, send_notification success/failure, and send_pending_notifications' return count. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P1] [decomposed] Fix crash in data_export.py's current_user["id"] access — app/routers/data_export.py's request_export, list_exports, get_export_status, and delete_account all type-hint `current_user: dict = Depends(get_current_user)` and subscript `current_user["id"]`, but get_current_user (app/routers/auth.py:58-61) actually returns a `User` SQLAlchemy ORM instance (app/models/user.py:54, `class User(Base)`, no `__getitem__` defined anywhere in the model or `DeclarativeBase`) — every authenticated call to POST /api/export/export, GET /api/export/exports, GET /api/export/exports/{export_id}, or DELETE /api/export/account raises `TypeError: 'User' object is not subscriptable` in production. tests/test_data_export_router.py (verified) only checks route registration and the unauthenticated 401/403 case, never an authenticated request, so this has never been caught. Fix: replace `current_user["id"]` with `current_user.id` in all 4 handlers, correct the type hint to `User`, and add authenticated happy-path tests to tests/test_data_export_router.py. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] Re-enable address capture at registration — app/routers/auth.py's register() (line ~141) has `# address=user_data.address,` commented out under a stale `# TODO: Re-enable after address migration`, but that migration already landed (alembic/versions/001_add_address_column_to_users.py adds users.address, guarded/idempotent), the User model already has the column (app/models/user.py:63), UserCreate/UserResponse already declare `address` (app/schemas/auth.py), and billwatch-web/src/stores/auth.ts's register() (lines 42-51) already sends `address` on every signup call — it is silently discarded server-side today. Uncomment the field assignment so registration persists it. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P2] [decomposed] Add PATCH /api/auth/me/address and wire ProfileView.vue's saveAddress() to it — billwatch-web/src/views/ProfileView.vue's saveAddress() only mutates the local Pinia store per its own `// TODO: Replace with actual API call when backend endpoint is available` comment; no such endpoint exists today (app/routers/auth.py only exposes GET /me, PATCH /me/notifications, and POST /me/device-token — verified via grep). Add `PATCH /api/auth/me/address` following the exact pattern of the existing `update_notification_prefs` handler (app/routers/auth.py:254-269), adding a new `AddressUpdate` schema next to `NotificationPrefsUpdate` in app/schemas/auth.py, then change ProfileView.vue's saveAddress() to call it through the existing `api` service (same pattern already used by stores/auth.ts) instead of only touching `authStore.user.address` locally. {cat: backend+web; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Fix feedback router's dead background-task wiring — app/routers/feedback.py's submit_feedback() accepts a `background_tasks: BackgroundTasks` parameter and defines `_run_async_email_task()` (line 80) specifically to send the feedback email off the request path, but never calls `background_tasks.add_task(...)` anywhere (verified: repo-wide grep for `_run_async_email_task` and `background_tasks.add_task` only matches the function's own `def` line) — instead it calls `email_service.send_feedback_sync(...)` (app/services/email_service.py:228, confirmed real) directly and synchronously, per a leftover `# Try calling email sync directly (not as background task) to test` comment, blocking every feedback submission on the email send and leaving heavy debug-level logging in place. Wire the existing `_run_async_email_task` into `background_tasks.add_task` (the already-present, currently-unused param) and remove the direct synchronous call plus the leftover debug logging. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P4] [decomposed] Add router tests for priority_api.py — app/routers/priority_api.py (6 endpoints: set_bill_priority, get_priority_dashboard, get_bills_requiring_action, create_priority_alert, get_priority_recommendations, get_priority_statistics; mounted via `app.include_router(priority_api.router, tags=["Priorities"])` at app/main.py:106) has zero router-level test coverage — only its backing `TrackedBillPriority` service (app/services/legislative_priority_service.py) is tested, in tests/test_legislative_priority_service.py (verified: no test file references priority_api, `/api/priorities`, or TrackedBillPriority besides that one). Add tests/test_priority_api_router.py covering the 401 unauthenticated case and mocked-service happy paths for each route. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P4] [decomposed] Add router tests for bill_chat.py — app/routers/bill_chat.py's two endpoints (`post_bill_message` — POST /api/bills/{bill_id}/chat, `get_bill_chat` — GET /api/bills/{bill_id}/chat) have zero test coverage; verified via repo-wide grep that no test file references bill_chat, BillMessage, post_bill_message, or get_bill_chat (tests/test_bill_summary_endpoint.py exercises a different, unrelated /summary route on bills.py). The moderation calls these endpoints depend on, `LLMService.moderate_comment` and `LLMService.filter_comment_tone` (app/services/llm_service.py:116 and :193), already exist. Add tests/test_bill_chat_router.py covering the post+moderation-approved happy path, the bill-not-found 404 branch, and get with filtered=true/false. {cat: backend; size: S; multifile: no; research: none}

## Newly discovered (2026-09-14 refuel — roadmap was dry)
- [ ] [P1] [decomposed] Fix crash bug in POST/PATCH /api/alerts (duplicate broken alert system) — app/routers/alerts.py's create_alert/update_alert call app/services/alert_service.py's AlertService.create_alert/update_alert, which construct BillAlert(..., sponsor_states=..., ...) and later set alert.notify_on_match, but app/models/analytics.py's BillAlert (lines 57-80) has no sponsor_states or notify_on_match columns — only keywords/policy_areas/chambers/bill_types/notify_on_status_change/notify_on_action/notify_on_vote/is_active/created_at/last_checked_at — so BillAlert(sponsor_states=...) raises TypeError on every authenticated POST /api/alerts; get_user_alerts also calls selectinload(BillAlert.matched_bills), a relationship that doesn't exist anywhere on the model (verified via grep of app/models/analytics.py and app/models/bill.py). This is a second, independent alert system living alongside the working app/routers/analytics.py /api/analytics/alerts endpoints (app/services/bill_alert_service.py's BillAlertService, which correctly matches the real model columns and is schema-validated via app/schemas/analytics.py's BillAlertCreateRequest). Verified via grep that tests/test_alert_service.py only covers the unrelated pure function should_trigger_hearing_alert, and tests/test_mounted_routers.py only checks the unauthenticated-401 case and route registration for /api/alerts — never an authenticated create — so this crash is 100% uncaught today. Fix alert_service.py to only set real BillAlert columns (drop sponsor_states/notify_on_match, use chambers like bill_alert_service.py does) and remove/fix the matched_bills selectinload, then add an authenticated create/update router test. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P1] [decomposed] Fix crash bugs in GET /api/votes/comparison and GET /api/votes/statistics/{bioguide_id} — app/routers/votes.py's compare_legislator_votes queries `select(Vote)...order_by(Vote.vote_date.desc())` and reads `v.congress_id`/`v.stance`, and get_voting_statistics reads `v.stance` in list comprehensions (`sum(1 for v in votes if v.stance == "yes")` etc.), but app/models/vote.py's Vote model only has id/roll_call_id/legislator_id/position — no vote_date, congress_id, or stance column (vote_date lives on the related RollCallVote, reached via a join). The file's own header comment (votes.py lines 18-22) already documents this exact pitfall and defines a `_stance(position)` helper plus a RollCallVote join, which get_legislator_votes and get_bill_votes correctly use — compare_legislator_votes and get_voting_statistics were simply never updated to follow it. Verified via grep that no test anywhere invokes these two endpoints with real vote data (tests/test_mounted_routers.py only checks /api/votes/comparison's route registration, no functional call). Fix both to join RollCallVote for vote_date and use `_stance(v.position)` in place of `v.stance`/`v.congress_id`, then add router tests with seeded Vote/RollCallVote rows. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P4] [decomposed] Delete unused dead-code service app/services/export.py (ExportService) — its export_bills_pdf always returns an empty BytesIO() (stub, literal `# TODO: Use reportlab or similar`) and export_legislator_voting_record always returns b'' regardless of the requested format, and the whole class is never imported by any router, job, or other service (verified via repo-wide grep for `app.services.export` and `ExportService` — the only hits are the class's own definition and its dedicated tests/test_export_bills_json.py). This is a third orphaned stub service alongside the already-flagged github_integration_service.py/legislator_relationship_service.py pair in the Maintenance section above (that earlier pass didn't catch this one); the real, wired-in export path is app/services/data_export_service.py's DataExportService, used by app/routers/data_export.py. Remove app/services/export.py and tests/test_export_bills_json.py. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P4] [decomposed] Add router-level tests for app/routers/notifications.py — GET /api/notifications (pagination, unread_only filter, unread_count), PATCH /api/notifications/{id} (mark as read, sets read_at), and POST /api/notifications/mark-all-read have zero test coverage anywhere in tests/ (verified via repo-wide grep for `routers.notifications`, `/api/notifications`, and `mark_all_read` — no hits outside the router file itself). Note this router (app/models/notification.py's Notification, in-app notification list/read-state) is distinct from the already-tracked NotificationService maintenance item (app/services/notification_service.py, Firebase push delivery) — different files, different concern. Add tests/test_notifications_router.py covering the three endpoints plus the 404-not-found branch on PATCH. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Add tests for GET/POST/DELETE /api/me/bills and /api/me/topics — app/routers/users.py's get_followed_bills/follow_bill/unfollow_bill and get_followed_topics/follow_topic/unfollow_topic (mounted at prefix /api/me, app/main.py:98) have zero test coverage anywhere in tests/ (verified via repo-wide grep for `follow_bill`, `user_followed_bills`, `follow_topic`, and `user_followed_topics` — no hits), even though the parallel follow_legislator/follow_committee endpoints in the exact same file are already tested in tests/test_follow_legislators.py and tests/test_follow_committees.py. Add tests/test_follow_bills.py and tests/test_follow_topics.py mirroring that existing pattern (already-following idempotency, 404 on missing bill/topic, successful unfollow, 404 on unfollow-when-not-following). {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P4] [decomposed] Add router-level tests for app/routers/ranking.py (mounted at /api/ranking, app/main.py:106) — GET/PUT /preferences, GET /ranked-bills, GET /ranked-bills/{congress}, POST /detect-activity, POST /apply-cooldown/{bill_id}, and GET /recently-enacted have zero HTTP-level test coverage; only the underlying BillRankingService logic is unit-tested (tests/test_bill_ranking.py, tests/test_bill_ranking_service.py — verified via grep that no test file references `/api/ranking` through a TestClient). Add tests/test_ranking_router.py covering the preferences 400 validation branch (score outside 0-2) and the empty-followed-bills early-return on both ranked-bills routes. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Add a router-level test for POST /api/articles/rank — app/routers/article_relevance.py's rank_articles (mounted app/main.py:111) is only exercised indirectly today: tests/test_article_relevance_service.py unit-tests ArticleRelevanceService with mocks, but no test calls the actual endpoint through a TestClient (verified via grep for `rank_articles`, `/api/articles`, and `/rank"` across tests/), so the ArticleRelevanceRequest/ArticleRelevanceResponse schema wiring and the try/except-500 branch are unverified end-to-end. Add tests/test_article_relevance_router.py covering a mocked-service success response and the 500-on-exception path. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Add a router-level test for POST /api/bills/{bill_id}/background — app/routers/bill_background.py's get_bill_background is only tested via its backing BillBackgroundService (tests/test_bill_background_service.py has 4 tests, all service-level, verified via grep for `client.` in that file returning no hits); no test exercises the actual endpoint, so the non-numeric-bill_id 400 branch and the 503-with-error-body degrade path (when service.get_background returns None) are unverified end-to-end. Add tests/test_bill_background_router.py covering both branches plus the 200 success path. {cat:backend; size:S; multifile:no; research:none}

## Newly discovered (2026-09-17 refuel — roadmap was dry)
- [ ] [P2] [decomposed] Add router-level test coverage for app/routers/analytics.py — mounted at prefix /api/analytics (app/main.py:105), this router has 18 endpoints total including the "real" bill-alerts CRUD (list_user_alerts/create_bill_alert/update_bill_alert/delete_bill_alert/toggle_bill_alert, lines 145-267) that the 2026-09-14 refuel entry described as the working alert system "correctly matches the real model columns and is schema-validated" — but verified via repo-wide grep for `routers.analytics`, `/api/analytics`, and `analytics.router` across tests/ that ZERO test file references any of them. The backing BillAlertService is unit-tested in tests/test_bill_alert_service.py, but no test ever exercises the HTTP endpoints themselves (auth gating, schema validation, or the trending/popular/track_bill_view/stats/dashboard/summaries-generate-batch/bill-detail routes). Add tests/test_analytics_router.py covering unauthenticated 401 on the alerts CRUD, an authenticated create/list/update/delete/toggle happy path, and smoke coverage of trending/popular/stats/dashboard. {cat:backend; size:L; multifile:no; research:none}
- [ ] [P4] [decomposed] Add router tests for POST /api/legislators/sync and POST /api/legislators/sync-stats — app/routers/legislators.py's sync_legislators (line 119) and sync_legislator_stats (line 152) have zero HTTP-level test coverage, verified via repo-wide grep for "legislators/sync" across tests/ (the only hits are unrelated: test_finance_router_gate.py's /api/finance/legislators/{id}/refresh and test_follow_legislators.py's /api/me/legislators/{id}). tests/test_legislator_stats_service.py unit-tests LegislatorStatsService directly but never the router, and tests/test_legislator_search.py only covers the not-found branches on missed-votes/sponsored-bills. Add tests/test_legislators_sync_router.py mocking LegislatorSyncService/LegislatorStatsService to cover the happy path, the 503 missing-CONGRESS_API_KEY branch, and the CongressAPIError-to-HTTPException mapping. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Vote/RollCallVote data has no production ingestion path — app/services/congress_api.py's get_votes() (line 260) is a permanent stub that always returns `{"votes": [], "pagination": {"count": 0}}` per its own TODO at line 275 ("Integrate with ProPublica Congress API for votes"), and verified via repo-wide grep for `RollCallVote(` and `Vote(` across app/ and scripts/ that no service, job, or script anywhere in the codebase ever constructs a Vote or RollCallVote row — the only matches are the model class definitions in app/models/vote.py itself. app/jobs/scheduler.py's start_scheduler() registers 8 recurring jobs (sync_bills, notifications, summaries, backgrounds, saved_search_alerts, campaign_finance, daily/weekly digest, committees) but none for votes. This means votes.py's endpoints (get_legislator_votes, get_bill_votes, compare_legislator_votes, get_voting_statistics — the stance/congress_id bugs on the latter two were already fixed in the 2026-09-14 pass) and LegislatorStatisticsService's voting-record stats have zero real data to serve in production; only test fixtures populate these tables today. Needs a real ProPublica/Congress.gov votes integration (or, if permanently out of scope, the feature should be documented as fixture-only rather than silently returning empty). {cat:backend; size:M; multifile:yes; research:repo}
- [ ] [P4] [decomposed] Delete orphaned dead-code service app/services/webhook_service.py — its WebhookService (register_webhook/get_webhooks/trigger_webhook/delete_webhook) has no consumer anywhere in app/ (verified via repo-wide grep for `webhook_service` and `WebhookService` — the only matches are the service file itself and its own test file). Its sole caller, app/routers/webhooks.py, was deliberately deleted in commit 37234d4 ("feat(billwatch): premium/upgrade UI + delete two dead/buggy routers"; verified via `git log --all --oneline -- app/routers/webhooks.py`, created in c2bef1b) but the backing service and tests/test_webhook_service.py (4 tests exercising the orphaned service directly with no router in front of it) were left behind. This is a 4th orphaned stub alongside the already-flagged github_integration_service.py/legislator_relationship_service.py/export.py trio from the earlier Maintenance and 2026-09-14 passes. Delete app/services/webhook_service.py and tests/test_webhook_service.py. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Add tests for billwatch-web Pinia stores auth.ts, bills.ts, and billChat.ts — billwatch-web/src/stores/ has 10 store files; src/stores/__tests__/ only covers 7 of them (billing.test.ts, civic.test.ts, legislators.test.ts, preferences.test.ts, priorities.test.ts, ranking.test.ts, savedSearches.test.ts — verified via directory listing). auth.ts (113 lines: login/register/fetchCurrentUser/saveAddress/logout/checkAuth, owns token persistence), bills.ts (105 lines), and billChat.ts (179 lines) have no matching test file. auth.ts is the highest-value gap of the three — it's read by every authenticated view in the app. Add auth.test.ts, bills.test.ts, and billChat.test.ts mirroring the existing store-test pattern in src/stores/__tests__/. {cat:web; size:M; multifile:yes; research:none}
- [ ] [P2] [decomposed] Fix iOS SettingsView — Save Settings and Logout buttons are both non-functional stubs — billwatch-ios/Sources/Views/SettingsView.swift's SettingsViewModel.saveSettings() (line 10) builds a settings dict (notifications/darkMode/language) but the function body ends at a bare `// TODO: POST to /api/settings` (line 20) — verified via grep that no `/api/settings` route exists anywhere in app/routers/ on the backend either, so there is nothing to wire it to yet without also adding that endpoint. logout() (line 23) is a single `// TODO: Clear tokens and redirect to login` (line 24) — the "Logout" button (`Button(role: .destructive, action: { Task { await viewModel.logout() } })`, line 51) does not clear the session or navigate anywhere, even though a real, working logout already exists elsewhere in the same app target (Sources/Services/BillWatchAPIClient.swift:80's `func logout() async throws`, verified via grep for "func logout" across Sources/) — SettingsView's stub simply never calls it. Wire logout() to BillWatchAPIClient.logout() plus navigation back to the login screen; for saveSettings(), either add a real PATCH /api/settings (or the existing PATCH /api/auth/me/notifications pattern) or remove the screen's implied persistence until a backend endpoint exists. {cat:mobile; size:S; multifile:yes; research:none}

## Newly discovered (2026-09-18 refuel round 2 — backlog dry again)
- [ ] [P4] [decomposed] Delete 3 more orphaned dead-code files with self-testing test suites — app/services/congress.py's `CongressClient` (78 lines) has zero production callers anywhere in app/ (verified via repo-wide grep for `CongressClient` — the only hits are its own `class` line and `tests/test_congress_client.py`, which imports and tests the otherwise-unused class); the real, wired-in Congress.gov client is `app/services/congress_api.py`'s `CongressAPIClient` (used by legislators.py, bills.py, etc). app/services/trending.py's `TrendingBillsService`/`TrendDirection`/`TrendCategory` (387 lines) are likewise never imported by any router/job/service (verified via repo-wide grep — only `tests/test_trending.py` and `tests/test_trending_pure.py` reference them); the real trending logic used in production is `app/services/trending_service.py`'s `TrendingService`, consumed by app/routers/bills.py:24/421/440/459/472. app/services/bill_summary.py's lone function `apply_embedding_to_bill` (12 lines) has no caller anywhere in app/ either (verified via repo-wide grep), only exercised by one test in tests/test_bill_summary_service.py (a file otherwise dedicated to the real, wired-in `BillSummaryService` in bill_summary_service.py). This is a 5th/6th/7th orphan alongside the already-flagged github_integration_service.py/legislator_relationship_service.py/export.py/webhook_service.py quartet from earlier passes — same pattern, newly found. Delete all 3 source files plus test_congress_client.py, test_trending.py, test_trending_pure.py, and the apply_embedding_to_bill test in test_bill_summary_service.py. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P3] [decomposed] Add unit tests for SummaryScheduler (app/services/summary_scheduler.py, 194 lines) and remove its dead cleanup_old_summaries method — this is a real, production-wired service (instantiated as a module-level singleton and called by app/jobs/scheduler.py:62-68's registered job, and directly by app/routers/analytics.py:403-406's summaries-generate-batch endpoint) with zero test coverage anywhere in tests/ (verified via repo-wide grep for `summary_scheduler`/`SummaryScheduler` across tests/ — no hits). Its core logic has several untested branches worth covering: `_get_bills_needing_summary`'s NOT-EXISTS/priority query, `_process_bill`'s "never persist a failed/placeholder generation" skip-on-`generated=False` guard (the comment at line ~132 flags this as previously having caused a real production bug — permanently-stuck placeholder rows), and `generate_missing_summaries`'s top-level exception handler. Separately, `cleanup_old_summaries` (lines 158-186) is dead code: verified via repo-wide grep for `cleanup_old_summaries` that it is never called anywhere in the codebase (not from scheduler.py's job registrations, not from any router) — either wire it into app/jobs/scheduler.py as a recurring job or delete it. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Add unit tests for LegislatorSyncService._upsert_legislator's field-mapping logic (app/services/legislator_sync_service.py:104-198) — sync_members itself has status-tracking coverage in tests/test_sync_status_reliability.py, but that test's `_MockCongress` only supplies `{"bioguideId": ..., "name": "Doe, John"}` (verified: no `terms`, `state`, `partyName`, or `district` keys), so _upsert_legislator's real branching is never exercised: name-splitting on ", " (last, then first-word-of-remainder), chamber normalization ("House" in raw_chamber / "Senate" in raw_chamber / else raw_chamber[:20]), party_code derivation ("Democrat" in party_name → "D" / "Republican" → "R" / else "I"), the 3-level state fallback (current_term.stateCode → STATE_CODES lookup on state name → `data.get("depiction", {}).get("attribution", "")[:2]`), and the congress/endYear-based in_office determination. The third state fallback in particular (slicing the first 2 characters of a photo-credit/attribution string as a state code) looks like a copy-paste bug rather than intentional design — worth a test asserting/documenting its actual behavior either way. Add tests/test_legislator_sync_upsert.py covering each branch with a realistic Congress.gov-shaped payload (terms, state name only, state code present, missing chamber, endYear in the past). {cat:backend; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Add unit tests for app/utils/datetime_utils.py's utcnow() — this 12-line module's own docstring says it's meant to be "replacing the deprecated `datetime.utcnow()` usage throughout the codebase," but it currently has exactly one adopter (`app/services/campaign_finance_sync_service.py`, verified via repo-wide grep for `datetime_utils`) and zero test coverage (verified: no test file references `datetime_utils` or imports `utcnow` from it). Every sibling util in app/utils/ (chamber.py, ordinal.py, party_color.py, percent_change.py, text_truncate.py, vote_outcome.py, etc.) has a matching test_*.py; this one doesn't. Add test_datetime_utils.py asserting the returned datetime is timezone-aware (tzinfo is not None) and within a small delta of `datetime.now(timezone.utc)`. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Delete orphaned dead Vue component billwatch-web/src/components/BillDetailView.vue — this 107-line component is never imported anywhere in src/ (verified via repo-wide grep for `BillDetailView` across all .vue/.ts files — the only import found is app/router/index.ts:36's `component: () => import('@/views/BillDetailView.vue')`, which resolves to the *other*, actually-used 220-line billwatch-web/src/views/BillDetailView.vue). The components/ copy appears to be a stale leftover from an earlier UI pass (git log shows commits like "feat(ui): add settings, detail views, and comprehensive UI components" and "fix: add type=button to untyped buttons in BillDetailView" touching it, but no removal). Delete src/components/BillDetailView.vue (and any dedicated test file if one exists — verified none does). {cat:web; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Consolidate duplicate/stale billwatch-web/src/services/api.test.ts — two separate test files exist for the same api.ts client: src/services/api.test.ts (60 lines, older style — asserts things like `expect(mockAxios).toBeDefined()` without exercising real request/response behavior, uses a loose `vi.mock('axios')` with `.mockReturnValueOnce`) and src/services/__tests__/api.test.ts (47 lines, newer style — proper `vi.mock('axios', () => ...)` factory, imports and calls the real `auth` export from '../api'). Every other test file in the codebase lives under a `__tests__/` subdirectory (src/stores/__tests__/, src/components/__tests__/, src/utils/__tests__/, src/composables/__tests__/, src/pages/__tests__/) — the loose src/services/api.test.ts is the sole outlier and appears to predate that convention. Delete the stale src/services/api.test.ts and confirm src/services/__tests__/api.test.ts's coverage isn't missing any assertion the old one had (it currently is not — old one only checked axios.create was called, no real behavior). {cat:web; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Fix Android registration silently dropping the address field — billwatch-android/app/src/main/java/com/billwatch/ui/Navigation.kt:72 calls `authViewModel.register(email, password, displayName, address)` with a real address string from RegisterScreen's callback, and AuthViewModel.kt's `register()` does accept an `address: String? = null` parameter — but its body only ever calls `authRepository.register(email, password, displayName)` (AuthViewModel.kt line ~61), dropping `address` entirely. AuthRepository.kt:44's `register()` doesn't have an address parameter either, and the `RegisterRequest` data class it builds (data/models/User.kt:30-34) only has email/password/displayName fields — there is nowhere left for the value to go even if forwarded. This is the Android-native counterpart to the already-fixed backend "Re-enable address capture at registration" item and the still-open web "PATCH /api/auth/me/address" item elsewhere in this roadmap, but is a distinct bug in a third codebase (verified via grep of Navigation.kt/AuthViewModel.kt/AuthRepository.kt/User.kt — no other reference to "address" exists anywhere under billwatch-android/app/src/main/java). Fix: add `address` to `RegisterRequest`, thread it through `AuthRepository.register()`, and have `AuthViewModel.register()` actually pass it along — but only once the backend's own address-capture fix (already tracked above) has landed, since the field is currently discarded server-side too. {cat:mobile; size:S; multifile:yes; research:none}

## Stopping point
Federal tracking + digests + alerts + AI summaries live on all 3 platforms = COMPLETE. Maintain data sync + upkeep. State-level = out of scope (that's FastDemocracy's game).

## Newly discovered (2026-09-18 refuel — went dry after today's queue-item batch landed)
- [ ] [P2] [decomposed] Fix the two broken/never-run files in billwatch-ios/Tests/ and wire the directory into a real test target — Tests/SettingsViewModelTests.swift (added whole by commit 5811eb8d on 2026-09-18) references `SettingsViewModel(apiService: mockAPIService)`, `viewModel.errorMessage`, `viewModel.clearError()`, and a `MockAPIService: APIServiceProtocol` — none of which exist in the actual SettingsViewModel (billwatch-ios/Sources/Views/SettingsView.swift; verified via repo-wide grep that `APIServiceProtocol` and `class SettingsViewModel` appear ONLY in that one source file and this one test file — the real SettingsViewModel takes no init params, has no errorMessage property, and no clearError() method), so it cannot compile as written. Tests/BillWatchAPIClientTests.swift makes live network calls to `BillWatchAPIConfig.TestBackends.all` (Sources/Config/BillWatchAPIConfig.swift lines 62-76) — 5 hardcoded production URLs for OTHER Shrike Labs products, including `task-manager-platform-production.up.railway.app` (FlockWorks, line 65), which CLAUDE.md documents as a Railway backend taken DOWN on 2026-09-09 — so testHealthCheck_FlockWorks_Success (line 35) and testConnectivity_AllBackends can never pass; separately its own `performRequest` test-helper extension (line 146-157) unconditionally `throw`s APIError.unknown regardless of the real request, so testErrorHandling_InvalidEndpoint (line 62) will always XCTFail (the code comment on line 153 admits this is a stand-in: "This would need to be refactored in the actual client to expose this method"). Root cause both were never caught: billwatch-ios/Package.swift declares only one library target (no `.testTarget`) and BillWatch.xcodeproj/project.pbxproj has exactly one PBXNativeTarget (the app itself, verified via grep for "PBXNativeTarget" — zero test-bundle targets), so nothing under Tests/ ever compiles or runs in CI or Xcode. Fix: rewrite SettingsViewModelTests.swift to match the real SettingsViewModel's actual surface, remove the cross-product/discontinued-backend network assertions and the always-failing performRequest stub from BillWatchAPIClientTests.swift (keep only assertions against BillWatch's own backend), then add a `.testTarget` to Package.swift pointing at "Tests" so this suite actually executes. {cat:mobile; size:M; multifile:yes; research:none}
- [ ] [P3] [decomposed] Add real unit tests for BillSummaryService (app/services/bill_summary_service.py, 136 lines) — this is the actual, wired-in Claude-backed summary service, imported only by app/routers/analytics.py (verified via repo-wide grep for `bill_summary_service`/`BillSummaryService` — 2 hits total: the service file and that router). tests/test_bill_summary_service.py, despite its name, only imports `apply_embedding_to_bill` from the unrelated orphaned module `app/services/bill_summary.py` (already flagged for deletion in the "2026-09-18 refuel round 2" section above) and never references `BillSummaryService` at all (verified: `grep -rl "BillSummaryService" tests/` returns zero hits) — so the real service's `get_or_generate_summary` (cache-hit short-circuit, bill-not-found None branch, Anthropic call + persistence) has zero coverage under any filename. Add tests/test_bill_summary_service_real.py (a distinct name, since the existing test_bill_summary_service.py will likely be emptied/deleted by the already-tracked orphan cleanup) mocking `anthropic.Anthropic` and the DB session. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Add unit tests for TrendingService (app/services/trending_service.py, 172 lines) — this is the real, wired-in service consumed by app/routers/bills.py (lines 24/421/440/459/472, per the already-tracked "2026-09-18 round 2" dead-code item) and app/models/bill.py, but has zero test coverage under its own class name: verified via `grep -rl "TrendingService\b" tests/` returning zero hits — the only trending-named tests (tests/test_trending.py, tests/test_trending_pure.py) test the unrelated orphaned `TrendingBillsService`/`TrendDirection`/`TrendCategory` classes from `app/services/trending.py` (already flagged for deletion elsewhere in this roadmap). Add tests/test_trending_service.py covering TrendingService's real methods with a mocked DB session. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Add unit tests for BillAnalyticsService (app/services/bill_analytics_service.py, 176 lines) — real, wired-in service consumed by app/routers/analytics.py (verified via repo-wide grep for `bill_analytics_service`/`BillAnalyticsService` — 2 hits total: the service file and that router), with zero test coverage anywhere (verified: no tests/ file references `BillAnalyticsService`, and there is no test_bill_analytics_service.py at all — unlike the bill_summary/trending cases this isn't even a naming collision, just a plain gap). Add tests/test_bill_analytics_service.py mocking the DB session and covering its core query/aggregation methods. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Add unit tests for Android BillsRepository + BillsViewModel — billwatch-android/app/src/main/java/com/billwatch/data/repository/BillsRepository.kt (79 lines) and .../ui/screens/BillsViewModel.kt (100 lines) have zero test coverage (verified: billwatch-android/app/src/test/java/ contains exactly 2 files, AuthRepositoryTest.kt and AuthViewModelTest.kt — no Bills* test exists anywhere under app/src/test/, despite 7 repositories and 9 viewmodels existing under app/src/main/). These back the app's core bill-browsing/tracking screen. Add BillsRepositoryTest.kt and BillsViewModelTest.kt mirroring the existing mockk-based pattern already established in AuthRepositoryTest.kt (mock the API/DataStore, use `coEvery`/`runTest`). {cat:mobile; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Add unit tests for Android BillingRepository + BillingViewModel — billwatch-android/app/src/main/java/com/billwatch/data/repository/BillingRepository.kt (35 lines) and .../ui/screens/BillingViewModel.kt (63 lines) have zero test coverage (verified: only AuthRepositoryTest.kt/AuthViewModelTest.kt exist under app/src/test/java/com/billwatch/, same gap as BillsRepository/BillsViewModel above). This is the app's premium/subscription-gating path — a revenue-critical, currently fully-untested surface on Android. Add BillingRepositoryTest.kt and BillingViewModelTest.kt following the existing mockk/coEvery/runTest pattern in AuthRepositoryTest.kt. {cat:mobile; size:S; multifile:yes; research:none}
- [ ] [P4] [decomposed] Add a unit test for billwatch-web/src/components/UnifiedBillCard.vue — this 70-line component is used across 3 views (BillsView.vue, FollowingView.vue, TrendingBillsPage.vue — verified via repo-wide grep for `UnifiedBillCard`) but has no matching test file under src/components/__tests__/ (verified: that directory only contains ChangePriorityModal.a11y.test.ts, DataFreshnessBadge.test.ts, FeedbackModal.test.ts, LegislatorScorecard.test.ts, RepCard.test.ts, StatusBadge.test.ts — no UnifiedBillCard entry, despite it being one of the most widely-reused components in the app). Add UnifiedBillCard.test.ts. {cat:web; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Add a unit test for billwatch-web/src/components/BillChatPanel.vue — this 276-line component (the UI for the already-router-tested bill-comment/chat feature backed by app/routers/bill_chat.py) has no matching test file under src/components/__tests__/ (verified: same directory listing as above — no BillChatPanel entry). Add BillChatPanel.test.ts covering its core rendering/interaction logic. {cat:web; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Expose the new dark_mode/language user-settings columns via the API — commit 6daacd2 ("feat(billwatch-backend): add dark_mode/language settings columns + migration", landed 2026-09-18) added `dark_mode`/`language` columns to app/models/user.py (lines 98-99) with a migration, but verified via repo-wide grep for `dark_mode`/`language` across app/routers/ and app/schemas/ that NEITHER field is exposed anywhere: UserResponse (app/schemas/auth.py) doesn't return them, and there is no PATCH endpoint that can set them (the only existing preference-update endpoints on app/routers/auth.py are `PATCH /me/notifications` at line 254 and `PATCH /me/address` at line 275). The columns are currently unreachable from any client. Add `dark_mode`/`language` to UserResponse, add a `SettingsUpdate` schema + `PATCH /api/auth/me/settings` endpoint following the exact pattern of the existing `update_notification_prefs` handler (app/routers/auth.py:254-269), and add router tests. This is the specific missing backend piece blocking the already-tracked "Fix iOS SettingsView" item's saveSettings() wiring elsewhere in this roadmap. {cat:backend; size:S; multifile:yes; research:none}

## Newly discovered (2026-09-19 refuel — roadmap fully [decomposed], no [ready] items left)
- [ ] [P1] [decomposed] Fix Bill Chat "send message" always failing with 401 for logged-in users — billwatch-web/src/stores/billChat.ts and src/components/BillChatPanel.vue both `import axios from 'axios'` directly and call the raw client (`axios.post(\`/api/bills/${billId}/chat\`, ...)`), instead of the shared src/services/api.ts client. Verified: the shared `api` client's request interceptor is the ONLY place that attaches `Authorization: Bearer <token>` from localStorage (app/services/api.ts lines 11-17); raw `axios.*` calls never go through it. The backend endpoint `POST /{bill_id}/chat` (app/routers/bill_chat.py line 28) requires `Depends(get_current_user)` (hard auth, not `get_current_user_optional` like the GET at line 82) — so every send-message request from these two files reaches the backend with no Authorization header and gets a 401. This bug survives even the Vercel `/api/:path*` rewrite (vercel.json) since it's an auth failure, not a routing failure. Fix: switch both files to `import api from '../services/api'` and use `api.post('/bills/${billId}/chat', ...)` (relative path, no leading `/api`, matching every other store's convention). {cat:web; size:S; multifile:yes; research:none}
- [ ] [P1] [decomposed] Fix double `/api/api/...` 404s breaking global search and legislator search — billwatch-web/src/pages/SearchPage.vue (`api.get('/api/search/advanced', ...)`) and src/components/LegislatorSearch.vue (`api.get('/api/legislators/search', ...)`) both call the shared `api` client (src/services/api.ts) whose `baseURL` is `VITE_API_URL` = `https://billwatch-production.up.railway.app/api` in production (verified in billwatch-web/.env.production) — then ALSO prepend a literal `/api/` to the call path. The resulting request goes to `.../api/api/search/advanced` and `.../api/api/legislators/search`, which don't exist: the real routes are mounted at `/api/search` (`app.include_router(search.router, prefix="/api/search", ...)`, main.py line 117) and `/api/legislators` (main.py line 101). Unlike the raw-axios bug above, this one is NOT saved by the Vercel rewrite, because the `api` client's baseURL is already the full absolute Railway URL — the request never touches the Vercel origin the rewrite intercepts. Fix: drop the leading `/api` from both call sites (`api.get('/search/advanced', ...)`, `api.get('/legislators/search', ...)`). {cat:web; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Fix the same double `/api/api/...` 404 bug breaking the admin dashboard — billwatch-web/src/pages/Admin.vue calls `api.get('/api/admin/users')` and `api.get('/api/admin/statistics')` through the same shared `api` client described above, producing `.../api/api/admin/users` against a backend that only registers `/api/admin/...` (app/routers/admin.py line 14: `router = APIRouter(prefix="/api/admin", ...)`, mounted with no extra prefix in main.py line 118). Same root cause and same fix pattern as the search bug above (drop the leading `/api`), called out separately since it's a distinct file/feature (admin-only, lower blast radius than the two P1 items). {cat:web; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Bill comments feature (BillCommentsSection.vue) has zero backend implementation — the component calls `api.get(\`/api/bills/${billId}/comments\`)` and `api.post(\`/api/bills/${billId}/comments\`, ...)` (same double-/api-prefix bug as above, but fixing that alone won't help here), yet there is no comments router, schema, model, or table anywhere in billwatch-backend (verified via repo-wide search for "comment" across app/routers/, app/models/, app/schemas/ — no hits outside vendored alembic/sqlalchemy internals). This is a shipped frontend component with no server-side feature behind it at all. Either implement a minimal comments backend (model + router, mirroring bill_chat.py's shape/moderation pattern) or remove the component if comments were superseded by the bill-chat feature. {cat:backend; size:M; multifile:yes; research:needed}
- [ ] [P2] [decomposed] Scheduled vote-sync job is a hardcoded no-op stub, never syncs real votes — app/jobs/scheduler.py registers `sync_votes_job` (line 369, id="sync_votes") on a real recurring APScheduler job, which calls `VoteSyncService.sync_votes()` (app/services/vote_sync_service.py). That method is entirely fake: it hardcodes `votes = [{"voter": "Rep A", "vote": "Yea"}, {"voter": "Rep B", "vote": "Nay"}]`, never calls any external API, never touches the DB, and its own docstring says "Simulate syncing votes." Meanwhile the REAL integration already exists and is unit-tested but unused: `CongressAPIClient.get_votes()` (app/services/congress_api.py line 324, hits Congress.gov's `/house-vote`/`/senate-vote` endpoints) and `parse_vote_response()` (same file, line 14, normalizes the raw payload into vote dicts — covered by tests/test_congress_api_votes.py) are never called from vote_sync_service.py or anywhere else in the codebase (verified via repo-wide grep for `parse_vote_response` — only its own definition and its unit test reference it). The `Vote` model already has a UniqueConstraint on (congress_id, chamber, roll_call_number) from a recent migration, ready to receive real data. Rewrite `VoteSyncService.sync_votes()` to actually call `CongressAPIClient.get_votes()` + `parse_vote_response()` and upsert into the `Vote` model, following the same DB-session/upsert pattern already established in campaign_finance_sync_service.py. {cat:backend; size:M; multifile:no; research:none}

## Newly discovered (2026-09-20 refuel — roadmap fully [decomposed], 0 backlog items left; Claude research pass)
- [ ] [P1] [decomposed] Wire up production error monitoring/crash reporting across all 3 clients — `sentry-sdk[fastapi]>=1.45.0` has been listed in `billwatch-backend/requirements.txt` but is never imported or initialized anywhere (confirmed via repo-wide grep for `sentry_sdk`/`SENTRY_DSN`/`sentry.init` in billwatch-backend — zero hits outside the dependency line itself). The backend already has real observability plumbing built around the gap it leaves: structured root-logger config (`app/core/logging.py`) and per-request correlation ids (`app/core/request_id_middleware.py`, `X-Request-ID` stamped on every log line and response header) — but no actual exception capture. The two `except Exception` catch-alls in `app/main.py` (the `/health` DB-check failure path and the `/metrics` psutil failure path) just log/swallow; nothing durable records real user-facing errors from any of the ~30 routers or the APScheduler jobs. None of the 3 live client platforms have any crash-reporting SDK either (confirmed via grep for "sentry"/"crashlytics" across billwatch-web, billwatch-ios, and billwatch-android — zero hits in all three). Net effect: this is a live, revenue-generating production app (real users, real Stripe billing) with zero visibility into real-world errors beyond whatever happens to scroll past in Railway's stdout logs. Scope: (1) backend — add a `sentry_dsn: str = ""` field to `Settings`, call `sentry_sdk.init(dsn=..., integrations=[...])` in the app factory gated on its presence (same "empty string = no-op" pattern already used for other optional integrations in this codebase), and confirm an unhandled exception in a router is actually captured with environment/release tags; (2) web — add `@sentry/vue` (or a minimal `app.config.errorHandler` + `window.onerror` capture posting to Sentry's browser SDK) gated on a `VITE_SENTRY_DSN` env var, matching the "unset = no-op" pattern; (3) iOS/Android — flag as a follow-up rather than force it into this same pass (adding a native crash-reporting dependency to two separate mobile build systems is a bigger, riskier change than the backend/web piece and deserves its own scoped pass once the web+backend half proves out the pattern). {cat: backend+web; size: M; multifile: yes; research: repo}

## Newly discovered (2026-09-21 refuel — roadmap fully [decomposed], 0 backlog items left; Claude research pass, production robustness beyond monitoring)
- [x] [P1] [done] The GDPR/CCPA "export my data" and "delete my account" endpoints are 100% fake — `app/services/data_export_service.py`'s `DataExportService` (all 4 methods: `request_export`/`get_export_status`/`list_exports`/`delete_account`) never touches the DB or any real storage: `request_export` returns a hardcoded `{"status": "processing", ...}` dict with no persistence; `get_export_status` unconditionally returns `{"status": "completed", "download_url": f"https://cdn.billwatch.app/exports/{export_id}.zip", ...}` regardless of whether that export_id was ever requested (the CDN URL doesn't exist — verified via repo-wide grep for `cdn.billwatch.app`, zero references anywhere else); `list_exports` returns two literal hardcoded fake rows for every user; and `delete_account` returns `{"status": "deleted", "data_wiped": True, "message": "All data has been permanently deleted"}` while never issuing a single DELETE against any table — confirmed via `grep -n 'db\.' app/services/data_export_service.py` returning zero hits, the service takes no db session at all. `tests/test_data_export_service.py` only asserts against this same hardcoded fake shape (e.g. `assert result['data_wiped'] is True`), so this has never been caught. This sits directly behind the already-tracked P1 crash bug elsewhere in this roadmap (`current_user['id']` subscript on a `User` ORM object) — once that crash is fixed, every authenticated call to `POST /api/export/export`, `GET /api/export/exports(/{id})`, and `DELETE /api/export/account` will succeed and claim success while doing nothing real. No web/iOS/Android client currently calls any of these four endpoints either (confirmed via repo-wide grep for `delete.*account`/`deleteAccount`/`/api/export` across billwatch-web/src, billwatch-ios/Sources, billwatch-android/app/src/main — the only hit is static privacy copy on TermsPage.vue), so this is a live production app whose privacy-policy-promised data rights are currently unimplementable even manually. Scope: rewrite `DataExportService` to take a DB session and (1) `request_export`/`get_export_status` build a real export by querying the user's own rows (bill follows, topics, alerts, saved searches, watch/notification history) and persist an export record + real status, or at minimum synchronously assemble and return the real data rather than a fake async job; (2) `delete_account` actually deletes/anonymizes the `User` row and cascades their owned rows (follows, alerts, saved searches, device tokens, etc.) in a real transaction; (3) rewrite `tests/test_data_export_service.py` to assert against a seeded DB fixture instead of the hardcoded shape; (4) add a real "Delete my account" control reachable in at least one client (web `ProfileView.vue` is the natural home, next to the existing address-save flow) — Apple Guideline 5.1.1(v) already requires in-app account deletion for any app with account creation, independent of this bug. {cat: backend+web; size: L; multifile: yes; research: none} <!-- SUPERSEDED 2026-09-21: real fix implemented and PR'd by interactive Claude session (branch fix/gdpr-delete-account-real-deletion, PR #29 -> develop): instance-based DataExportService with real cascading delete_account, real request_export/get_export_status backed by a new export_records table + 0004_add_export_records.py migration, real DB-state tests. Do not decompose or stage further work on this item -- it duplicates/conflicts with PR #29's architecture (e.g. this queue's own second auto-refill created a competing export_request.py model + 0004_add_export_request_table.py migration with a colliding revision id). Any already-committed overnight/feature work on data_export_service.py from this item should be reconciled against PR #29 by a human/Claude reviewer when that PR merges, not auto-merged over it. -->

## Newly discovered (2026-09-21 refuel round 2 — same-day re-check after prior item superseded by PR #29; roadmap/OVERNIGHT_PROGRESS.md both fully dry again, 0 doable items; web-frontend focus since backend routers/services/utils are now almost exhaustively test-covered per repeated repo-wide greps below)
- [ ] [P4] [decomposed] Delete the entirely-orphaned billwatch-web/src/api/ directory (5 files, dead since it was superseded by src/services/adminApi.ts + the real DeleteAccountButton.vue implementation) — verified via repo-wide grep for `api/client`, `api/admin`, and `from '@/api` across all .vue/.ts under src/ (excluding src/api itself and its own __tests__) that ZERO real application file imports anything from src/api/client.js, src/api/client.ts, or src/api/admin.js: src/api/client.ts's `deleteAccount()` (calls `api.delete('/export/account')`) is superseded by the real, wired-in src/components/DeleteAccountButton.vue, which calls `api.delete('/auth/me')` directly instead; src/api/admin.js (creates an `adminApi` via `api.create({baseURL: ...})`, but axios instances don't expose `.create` — this file would throw `api.create is not a function` if anything ever imported it) is superseded by the real, wired-in src/services/adminApi.ts. The two test files in src/api/__tests__/ (client.test.js, client.test.ts) only test the orphaned client.ts, and client.test.js additionally never even executes: vitest.config.ts's `test.include` glob is `src/**/__tests__/**/*.test.{ts,tsx,vue}` and `*.spec.{ts,tsx,vue}` — no `.js` extension — so client.test.js is invisible to `npx vitest run` today, verified by the extension list in vitest.config.ts (read directly). Delete the whole src/api/ directory (admin.js, client.js, client.ts, __tests__/client.test.js, __tests__/client.test.ts). Deliberately stops short of touching src/services/adminApi.ts or DeleteAccountButton.vue themselves — those are the real, live implementations and are out of scope here. VERIFY: `test ! -d billwatch-web/src/api && echo OK`. Tests: none added (pure dead-code removal; no behavior to cover). {cat:web; size:S; multifile:yes; research:none}
- [ ] [P4] [decomposed] Delete stale duplicate test file DeleteAccountButton.spec.js — billwatch-web/src/components/__tests__/ has both DeleteAccountButton.spec.js and DeleteAccountButton.spec.ts testing the same real component (src/components/DeleteAccountButton.vue), verified via diff that the two files are identical except the .js version drops TypeScript typing and has a latent scoping bug (`let pinia` declared but never assigned before being passed to `mount(..., { global: { plugins: [pinia] } })`, since `beforeEach` calls `setActivePinia(createPinia())` without capturing the created instance) that the .ts version fixes. As with the sibling src/api/ finding above, vitest.config.ts's `test.include` glob only matches `.test.{ts,tsx,vue}`/`.spec.{ts,tsx,vue}` — no `.js` — so DeleteAccountButton.spec.js never actually runs under `npx vitest run` today; it is silently-dead weight, not a source of double coverage. Delete DeleteAccountButton.spec.js and keep DeleteAccountButton.spec.ts, matching the repo-wide convention that every other test file under src/**/__tests__/ is .ts (verified: it and the two src/api/ files above are the only .js test files anywhere under billwatch-web/src). VERIFY: `test ! -f billwatch-web/src/components/__tests__/DeleteAccountButton.spec.js && npx vitest run billwatch-web/src/components/__tests__/DeleteAccountButton.spec.ts 2>&1 | grep -q "passed"`. Tests: none added (dedupe only; DeleteAccountButton.spec.ts already covers the same 3 cases). {cat:web; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Add a unit test for LoginView.vue — billwatch-web/src/views/LoginView.vue (84 lines) has no test file anywhere (verified: src/views/__tests__/ contains only FormLabels.a11y.test.ts and NotificationPreferencesView.a11y.test.ts — no LoginView entry, and it is not an a11y-only gap, the view has real untested logic). `handleSubmit()` (lines 12-17) calls `authStore.login(email.value, password.value)` and, only `if (success)`, calls `router.push('/')`; the template also conditionally renders `authStore.error` in a `role="alert"` div and disables the submit button while `authStore.isLoading` is true. Add src/views/__tests__/LoginView.test.ts mirroring the existing `vi.mock('@/services/api', ...)` + `setActivePinia(createPinia())` pattern already used in src/stores/__tests__/auth.test.ts, plus `vi.mock('vue-router', () => ({ useRouter: () => ({ push: vi.fn() }) }))` (same pattern as src/components/__tests__/DeleteAccountButton.spec.ts), covering: successful login navigates to '/', failed login (mocked `authStore.login` resolving false) does not navigate and the error alert renders, and the submit button is disabled while `authStore.isLoading` is true. Deliberately stops short of testing RegisterView.vue's near-identical pattern — that is a separate, distinct untested file left for a future pass. VERIFY: `cd billwatch-web && npx vitest run src/views/__tests__/LoginView.test.ts 2>&1 | grep -q "passed"`. Tests: new billwatch-web/src/views/__tests__/LoginView.test.ts. {cat:web; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Add a unit test for FollowingView.vue — billwatch-web/src/views/FollowingView.vue (75 lines) has no test file (verified: same src/views/__tests__/ directory listing as above). `onMounted` calls `billsStore.fetchFollowedBills()` and toggles a local `isLoading` ref around it; the template branches on three states verified by reading the file directly — `isLoading` (renders a `role="status"` loading message), `billsStore.followedBills.length === 0` (renders an empty-state message plus a "Browse Bills" button that calls `router.push('/bills')`), and the non-empty list (renders one `UnifiedBillCard` per followed bill, each wrapping the raw bill through the local `asTrendingBill()` helper which backfills `congress_id`/`view_count`/`favorite_count`/`follower_count`/`trending_score` defaults). Add src/views/__tests__/FollowingView.test.ts following the existing `vi.mock('@/services/api', ...)` pattern from src/stores/__tests__/bills.test.ts, seeding the bills store's `followedBills` directly (bypassing the mocked API call) to cover the loading, empty, and populated-list render branches, and asserting `router.push` is called with `/bills` on the empty-state button click. Deliberately stops short of testing `asTrendingBill()`'s exact default-value fallbacks field-by-field — a single populated-bill snapshot assertion is enough to catch a regression in the render path, a dedicated fallback-defaults test can follow later if it proves worthwhile. VERIFY: `cd billwatch-web && npx vitest run src/views/__tests__/FollowingView.test.ts 2>&1 | grep -q "passed"`. Tests: new billwatch-web/src/views/__tests__/FollowingView.test.ts. {cat:web; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Add a direct unit test for searchService.ts — billwatch-web/src/services/searchService.ts (20 lines, exports `searchService.advancedSearch(params)` which calls `api.get('/search/advanced', { params }).then(r => r.data)`) is only covered indirectly today through its consumer, SearchPage.vue (src/pages/__tests__/SearchPage.test.ts), verified via `grep -rl "searchService" src/services/__tests__/` returning zero hits — no test imports/exercises the service module itself. Add src/services/__tests__/searchService.test.ts mocking `./api` (same `vi.mock('@/services/api', ...)` shape as src/services/__tests__/api.test.ts) and asserting `advancedSearch` calls `api.get` with the literal path `/search/advanced` (not `/api/search/advanced` — this exact leading-`/api` double-prefix bug was already fixed once for this file per the 2026-09-19 refuel entry above, so pinning the correct path in a direct unit test guards against a silent regression) and `{ params }`, and that it resolves to `response.data`. Deliberately stops short of adding a matching direct test for analyticsService.ts (110 lines, larger surface, better scoped as its own follow-up). VERIFY: `cd billwatch-web && npx vitest run src/services/__tests__/searchService.test.ts 2>&1 | grep -q "passed"`. Tests: new billwatch-web/src/services/__tests__/searchService.test.ts. {cat:web; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Add a direct unit test for legislatorService.ts — billwatch-web/src/services/legislatorService.ts (21 lines, exports `legislatorService.search(params)` which calls `api.get('/legislators/search', { params }).then(r => r.data)`) is only covered indirectly today through its consumer, LegislatorSearch.vue (src/components/__tests__/LegislatorSearch.test.ts), verified via `grep -rl "legislatorService" src/services/__tests__/` returning zero hits. Same rationale and same already-fixed-once double-`/api`-prefix regression risk as searchService.ts above (both bugs were fixed together in the 2026-09-19 refuel entry). Add src/services/__tests__/legislatorService.test.ts mocking `./api` and asserting `search` calls `api.get` with the literal path `/legislators/search` and `{ params }`, resolving to `response.data`. VERIFY: `cd billwatch-web && npx vitest run src/services/__tests__/legislatorService.test.ts 2>&1 | grep -q "passed"`. Tests: new billwatch-web/src/services/__tests__/legislatorService.test.ts. {cat:web; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Add a unit test for Icon.vue — billwatch-web/src/components/Icon.vue (28 lines) is a small dependency-free glyph-map wrapper (`GLYPHS` record keyed by icon name: alertTriangle/check/loader/minus/refresh/search/x, falling back to `•` for any unknown key via `GLYPHS[props.icon] ?? '•'`) with no test file anywhere (verified: src/components/__tests__/ directory listing above has no Icon entry). Add src/components/__tests__/Icon.test.ts mounting the component with each of a few known `icon` prop values and asserting the rendered glyph text plus the `role="img"` / `:aria-label="icon"` attributes, and one case with an unrecognized icon name asserting the `•` fallback renders. VERIFY: `cd billwatch-web && npx vitest run src/components/__tests__/Icon.test.ts 2>&1 | grep -q "passed"`. Tests: new billwatch-web/src/components/__tests__/Icon.test.ts. {cat:web; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Add a unit test for PremiumPaywall.vue — billwatch-web/src/components/PremiumPaywall.vue (32 lines, takes a required `featureName` prop, renders it inside a `data-testid="premium-paywall"` block, and its one interactive element — an "Upgrade to Premium" button — calls `router.push({ name: 'premium' })` on click) has no test file anywhere (verified: same src/components/__tests__/ listing as above, no PremiumPaywall entry). Add src/components/__tests__/PremiumPaywall.test.ts mocking `vue-router`'s `useRouter` (same pattern as src/components/__tests__/DeleteAccountButton.spec.ts) and covering: the `featureName` prop renders in the message text, and clicking the upgrade button calls `router.push` with `{ name: 'premium' }`. VERIFY: `cd billwatch-web && npx vitest run src/components/__tests__/PremiumPaywall.test.ts 2>&1 | grep -q "passed"`. Tests: new billwatch-web/src/components/__tests__/PremiumPaywall.test.ts. {cat:web; size:S; multifile:no; research:none}

## Newly discovered (2026-09-22 refuel round 3 — Claude research pass; the prior 6 "Newly discovered" headers below were never actually decomposed into real backlog items, and live-repo verification found most of that older work was already done separately — these 6 are freshly re-verified as still genuinely open)
- [ ] [P2] [decomposed] Add router-level tests for app/routers/advanced_auth.py — `GET /api/advanced/login-history` (the only route on this router, DB-backed via `SecurityAuditService.get_login_history`) has zero router-level test coverage: verified via `grep -rn "login-history\|login_history\|advanced_auth" tests/*.py` returning only `tests/test_security_audit.py`, which tests `SecurityAuditService.get_login_history` directly, never the actual HTTP route (auth wiring, `limit` query-param bounds `ge=1,le=100`, or the `{"events": [...]}` response shape). `tests/test_advanced_auth.py` does NOT exist as source (only a stale `.pyc` in `tests/__pycache__/` from a deleted file). Add `tests/test_advanced_auth_router.py`. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Add router-level tests for app/routers/priority_api.py — all 6 endpoints (`POST .../bills/{bill_id}/priority`, `GET .../dashboard`, `GET .../bills-requiring-action`, `POST .../alerts`, `GET .../recommendations`, `GET .../statistics`) have zero router-level coverage: verified via `grep -rln "priorities\|priority_api\|TrackedBillPriority" tests/*.py` returning only `tests/test_legislative_priority_service.py` (service-level only). `tests/test_priority_api.py` does not exist as source. This leaves auth wiring, the router's own `except ValueError` → 400 branch on an invalid `priority` string, and every response shape completely untested. Add `tests/test_priority_api_router.py`. {cat:backend; size:M; multifile:no; research:none}
- [ ] [P2] [decomposed] Add router-level tests for app/routers/bill_chat.py — `POST /api/bills/{bill_id}/chat` and `GET /api/bills/{bill_id}/chat` (Gemma-moderated bill comments) have ZERO test coverage of any kind: verified via `grep -rln "post_bill_message\|get_bill_chat\|BillMessage" tests/*.py` returning no hits at all. Untested behavior includes: bill-not-found 404 on both routes, the moderation-hidden-but-persisted branch, pagination (`limit`/`offset`), hidden-message exclusion from both the list and `message_count`, and the `filtered=true` tone-filter generation/caching path (mocking `llm_service`). Add `tests/test_bill_chat_router.py`. {cat:backend; size:M; multifile:no; research:none}
- [ ] [P4] [decomposed] Finish deleting the orphaned billwatch-web/src/api/ directory — the 2026-09-21 refuel item to delete this directory only partially landed: `src/api/admin.js`, `src/api/client.js`, and the two `.js` test files were removed, but `src/api/client.ts` (gutted to a thin `deleteAccount()` re-export, still imported by nothing — verified via repo-wide grep for `api/client` outside `src/api/` itself, zero hits) and its test `src/api/__tests__/client.test.ts` are still present and still fully dead. Delete both remaining files (do not add a separate directory-removal step — that was already auto-retired as vague in the prior attempt since there's no file content to act on). {cat:web; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Add unit tests for Android SavedSearchesRepository + SavedSearchesViewModel — `billwatch-android/.../data/repository/SavedSearchesRepository.kt` (55 lines, 5 methods) and `.../ui/screens/SavedSearchesViewModel.kt` (80 lines) have zero test coverage (verified: `app/src/test/java/com/billwatch/` only contains Auth/Billing/Bills repository+viewmodel tests). Add `SavedSearchesRepositoryTest.kt` and `SavedSearchesViewModelTest.kt` mirroring the existing mockk/coEvery/runTest pattern established in `BillsRepositoryTest.kt`. {cat:mobile; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Add unit tests for Android LegislatorsRepository + LegislatorsViewModel — `billwatch-android/.../data/repository/LegislatorsRepository.kt` (135 lines, 12 methods) and `.../ui/screens/LegislatorsViewModel.kt` (212 lines, incl. debounced search, pagination via `loadMoreLegislators`, and follow/unfollow toggling) have zero test coverage (same gap pattern as SavedSearches above, verified against the same test-directory listing). Add `LegislatorsRepositoryTest.kt` and `LegislatorsViewModelTest.kt` following the same established mockk pattern. {cat:mobile; size:M; multifile:yes; research:none}

## Newly discovered (2026-09-22 refuel round 4 — Claude deep research pass; exhaustive backend+web+mobile sweep after round 3)
- [ ] [P1] Fix crash bug in GET /api/activity-logs/logs — app/routers/activity_logs.py's get_activity_logs (line 22-23) type-hints `current_user: dict = Depends(get_current_user)` and subscripts `current_user["id"]`, but get_current_user (app/routers/auth.py, `async def get_current_user(...) -> User`) returns a real `User` SQLAlchemy ORM instance with no `__getitem__` — every authenticated call to GET /api/activity-logs/logs raises `TypeError: 'User' object is not subscriptable` in production. This is the SAME bug pattern already tracked for app/routers/data_export.py elsewhere in this roadmap, but in a DIFFERENT, previously-unflagged file (verified via direct read of activity_logs.py, not assumption). tests/test_activity_logs_router.py's `test_get_activity_logs` hides this completely: it overrides `get_current_user` with `async def override_get_current_user(): return {"id": "test-user-id", "workspace_id": "test-workspace"}` — a plain dict, not a real `User` — so the subscript never fails in the test even though it always would in production (verified by reading the fixture directly). Separately, `ActivityLoggingService.get_activity_log` and `.log_activity` (app/services/activity_logging_service.py, 67 lines) are ALSO both 100% hardcoded fake stubs (`"total_entries": 1250`, two canned `{"action": "view"/"follow", ...}` entries) that never touch the DB — the exact same fake-stub pattern the `/audit-report` endpoint in this same router already had fixed (migrated to the real `SecurityAuditService`, verified in the same file) — `/logs` was simply missed in that migration. Fix: change `current_user: dict` to `current_user: User` and `current_user["id"]` to `current_user.id`, rewrite the test to authenticate via a real registered user + JWT (the `valid_token`-fixture pattern already used in tests/test_follow_bills.py) instead of a dict override, and either wire `/logs` to `SecurityAuditService` (matching `/audit-report`) or explicitly scope `ActivityLoggingService.get_activity_log` to real DB-backed activity if that's a distinct concern. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P1] CORRECTION — the "GDPR/CCPA export/delete-account... SUPERSEDED by PR #29" note elsewhere in this roadmap is STALE and the underlying bug is confirmed still live today. Verified via `git merge-base --is-ancestor <PR#29 commit e0a496e> origin/develop` returning NOT_ANCESTOR, and `git log --oneline -- app/services/data_export_service.py` on the current overnight/feature checkout showing only the original `4895cdd feat(export): add data export service` commit — PR #29's branch (`fix/gdpr-delete-account-real-deletion`, commit e0a496e) exists on `origin` but was never merged into `develop`. Reading the current `app/services/data_export_service.py` directly confirms all 4 methods are still the exact original 100%-fake stubs (hardcoded `download_url: f"https://cdn.billwatch.app/exports/{export_id}.zip"`, two canned `list_exports` rows, `delete_account` never issuing a DB delete), and `app/routers/data_export.py` still has the identical `current_user["id"]` subscript crash on all 4 endpoints. Action: correct the stale "superseded" annotation, and note that a human/Claude reviewer should either land PR #29 or re-decompose this fix directly against current `develop` — do not silently trust the annotation on a future pass. {cat:backend; size:S (doc correction only); multifile:no; research:none}
- [ ] [P4] Delete orphaned dead-code module app/core/rate_limiting.py — this 118-line module (`Limiter`, `LIMIT_PUBLIC`/`LIMIT_SEARCH`/`LIMIT_AUTH`/`LIMIT_SENSITIVE` constants, decorators) has ZERO references anywhere in app/ or tests/ (verified via repo-wide grep for `from app.core.rate_limiting`, `core.rate_limiting`, and `rate_limiting import` — zero hits, and zero test files reference it either). The real, wired-in rate limiter is the separate, much smaller `app/core/rate_limiter.py` (16 lines, exports `limiter`), imported by app/main.py, app/routers/auth.py, and app/routers/feedback.py (verified). This is a 9th+ orphaned-module find in the same ongoing series as github_integration_service.py/legislator_relationship_service.py/export.py/webhook_service.py/congress.py/trending.py/bill_summary.py already flagged in earlier rounds. Delete app/core/rate_limiting.py (no dedicated test file exists to also delete). {cat:backend; size:S; multifile:no; research:none}
- [ ] [P4] Delete orphaned dead-code module app/services/caching.py (CacheService) and its 3 orphan-only test files — verified via repo-wide grep for `from app.services.caching import` and `services.caching\b` across app/ that CacheService (58 lines) has zero production callers; the real, wired-in cache is the separate `app/services/caching_service.py`'s `CachingService` (93 lines), used by app/routers/bills.py, app/routers/topics.py, and app/services/bill_sync_service.py (verified). Three dedicated test files exist that only exercise the dead module directly (verified via each file's own import line): tests/test_cache_service.py (`from app.services.caching import CacheService`), tests/test_caching_get_or_set.py (same import), and tests/test_caching.py (same import) — none of these overlap with the correctly-named tests/test_caching_service.py, which tests the real `CachingService`. This is the same naming-collision orphan pattern already found for bill_summary.py/trending.py in the round-2 refuel. Delete app/services/caching.py, tests/test_cache_service.py, tests/test_caching_get_or_set.py, and tests/test_caching.py. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P4] Finish two already-tracked dead-code deletions that only partially landed — the 2026-09-14 roadmap item to delete app/services/export.py (ExportService, stub `export_bills_pdf`/`export_legislator_voting_record`) and the 2026-09-17 item to delete app/services/webhook_service.py (WebhookService, no router since commit 37234d4) both only got their TEST files removed (tests/test_export_bills_json.py and tests/test_webhook_service.py are both gone) — but the source files themselves are still present today (verified: `ls app/services/export.py app/services/webhook_service.py` both succeed) and still confirmed zero production callers (verified via fresh repo-wide grep for `services.export`/`ExportService` and `webhook_service`/`WebhookService` — only each file's own class-definition line matches). Delete both remaining source files now that their test coverage is already gone. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P4] Delete orphaned dead-code Pinia store billwatch-web/src/stores/billChat.ts — this 179-line store (`useBillChatStore`, exposing fetchSummary/fetchMessages/postMessage/clearBillCache/clearAllCache) is never imported anywhere in src/ outside its own test file (verified via repo-wide grep for `useBillChatStore` across all .vue/.ts under src/ — the only two hits are the store's own `export const` line and stores/__tests__/billChat.test.ts). The real, live bill-chat feature is implemented directly inside src/components/BillChatPanel.vue (which already correctly imports the shared `api` client per the earlier "refactor(web): route BillChatPanel through the shared api client" fix) and src/components/BillSummaryPanel.vue — neither imports this store. This orphaned store still contains the exact raw-`axios`-instead-of-shared-client pattern the 2026-09-19 roadmap item flagged as a 401 risk, but it's moot since nothing calls it. Delete src/stores/billChat.ts and src/stores/__tests__/billChat.test.ts. {cat:web; size:S; multifile:yes; research:none}
- [ ] [P4] Delete orphaned dead Vue component billwatch-web/src/components/BillSummaryCard.vue — this 124-line component is never imported anywhere in src/ (verified via repo-wide grep for `BillSummaryCard` across all .vue/.ts files — zero hits outside its own file, not even a test). The real, live, wired-in summary display is the separate `src/components/BillSummaryPanel.vue`, imported by src/views/BillDetailView.vue (verified). This is a second orphaned Vue component alongside the already-flagged components/BillDetailView.vue from the round-2 refuel (same directory-confusion pattern: a `components/` copy left behind after a `views/`-based rewrite superseded it). Delete src/components/BillSummaryCard.vue. {cat:web; size:S; multifile:no; research:none}
- [ ] [P3] Delete unreachable dead component billwatch-web/src/components/BillCommentsSection.vue (correction/narrowing of the existing 2026-09-19 roadmap item) — that earlier item correctly flagged this component's backend as nonexistent, but framed the fix as an either/or ("implement a minimal comments backend... or remove the component"). Fresh verification via repo-wide grep for `BillCommentsSection` across every .vue file in src/ shows it is not imported by ANY view or component at all (zero hits outside its own file) — it isn't just missing a backend, it's completely unreachable from any route a user could ever visit, in addition to still calling the double-prefixed `/api/bills/${billId}/comments` (would 404 even if a backend existed) and reading `route.params.id` without ever receiving a `billId` prop from a parent. Given it's dead on both ends (unreachable AND backendless), delete it outright rather than building new backend infrastructure for a component nothing renders. Supersedes the "add a backend" branch of the 2026-09-19 item. {cat:web; size:S; multifile:no; research:none}
- [ ] [P3] Add unit tests for views/AlertsView.vue — this 343-line view (real CRUD against the working `analyticsService`/`/api/analytics/alerts` backend, verified in services/analyticsService.ts) has zero test coverage anywhere (verified: no AlertsView entry under src/views/__tests__/). Real, verified logic worth covering: `submitAlert()`'s empty-keywords guard (calls the browser `alert()` and returns early without calling the service), the create-vs-update branch keyed on `editingId`, `editAlert()`'s pre-fill of the edit form from an existing `BillAlert`, `toggleAlert`/`deleteAlertConfirm` (the latter gated behind `window.confirm`), and `resetForm()`'s field reset. Add src/views/__tests__/AlertsView.test.ts mocking `@/services/analyticsService` (not raw api/axios, since this view calls the service directly) following the established `vi.mock` pattern used elsewhere in src/views/__tests__/. {cat:web; size:M; multifile:no; research:none}
- [ ] [P3] Add unit tests for views/RegisterView.vue — this 144-line view (verified by direct read) has real, untested branching: `handleSubmit()`'s early-return guard when `password !== confirmPassword || !agreedToTerms.value`, the password-mismatch `role="alert"` div that only renders once BOTH fields are non-empty and they differ, the submit button's `:disabled` binding (loading OR mismatch OR terms-not-agreed), and the success path calling `authStore.register(...)` then `router.push('/')`. No test file exists (verified: src/views/__tests__/ has no RegisterView entry, unlike its sibling LoginView.test.ts which already covers the analogous login flow). Add src/views/__tests__/RegisterView.test.ts mirroring LoginView.test.ts's `vi.mock('@/services/api', ...)` + `vi.mock('vue-router', ...)` pattern. {cat:web; size:S; multifile:no; research:none}
- [ ] [P3] Expand stores/__tests__/auth.test.ts — this test file exists (91 lines) but, verified by reading it in full, ONLY exercises `login()`'s success and failure paths; `register()`, `saveAddress()`, `logout()`, `checkAuth()`, and `fetchCurrentUser()`'s own failure branch (catches and sets `user.value = null`) are completely untested (verified via grep for each function name as a `store.<fn>(` call in the test file — zero matches for any of them). `saveAddress` in particular is the real, live implementation behind the already-shipped address-editing feature on ProfileView.vue (`api.patch('/auth/me/address', { address })`) and deserves direct coverage of both its success (sets `user.value` from the response) and failure (sets `error.value`, returns false) branches. Add the missing test cases to the existing file rather than a new one, matching its established `vi.mock('@/services/api', ...)` + localStorage-mock pattern. {cat:web; size:S; multifile:no; research:none}
- [ ] [P4] Add direct unit tests for services/adminApi.ts and services/analyticsService.ts — both are currently only exercised indirectly via other components' mocks (adminApi.ts verified only referenced from `__tests__/pages/Admin.spec.ts`'s `vi.mock`; analyticsService.ts verified only referenced from `components/__tests__/UnifiedBillCard.test.ts` and `pages/__tests__/TrendingBillsPage.a11y.test.ts`'s mocks — neither has a dedicated test of its own exported functions). adminApi.ts (42 lines) exists specifically to prevent a double-`/api`-prefix regression per its own docstring ("the bug this file fixes") — a direct test pinning its 3 literal paths (`/admin/users`, `/admin/statistics`, `/admin/users/{id}/ban`) guards that intent directly instead of only through Admin.vue's mock. analyticsService.ts (110 lines, 8 methods backing the real `/api/analytics/*` alert CRUD used by AlertsView.vue above) has no direct test of its own request shapes (params/body) at all. Add src/services/__tests__/adminApi.test.ts and src/services/__tests__/analyticsService.test.ts, mocking `./api` per the established `vi.mock('@/services/api', ...)` pattern used in searchService.test.ts/legislatorService.test.ts. {cat:web; size:S; multifile:yes; research:none}
- [ ] [P4] Add unit tests for the 3 untested Priority-Dashboard presentational components — components/BillActionItem.vue (~70 lines, `viewBill`/`setPriority` emits, `actionTypeLabel` map, days-until-action badge), components/BillPriorityItem.vue (52 lines, `priorityConfig` critical/high/medium/low color+icon map, sponsor-list truncation showing first 2 + "+N more", `click` emit), and components/PriorityLevelCard.vue (69 lines, `levelConfig` map, and a 3-way pluralization branch on `count` — 0 shows "No bills...", 1 shows singular "1 bill...", 2+ shows plural "N bills...") are all real, wired-in components consumed by pages/PriorityDashboardPage.vue (verified via grep) with zero test coverage anywhere (verified: none of the three appear in src/components/__tests__/). Add BillActionItem.test.ts, BillPriorityItem.test.ts, and PriorityLevelCard.test.ts, each asserting their respective config-map rendering and emit behavior. {cat:web; size:S; multifile:yes; research:none}
- [ ] [P4] Add a unit test for components/BillRankingCard.vue — this 130-line component (verified via direct read) is real and wired-in (consumed by pages/BillRankingPage.vue), with two untested computed properties (`urgencyColorClass`/`urgencyBadgeClass`, each a 4-way switch on `bill.urgency_state`: rising/steady/cooling/default) and a `record-activity` emit wired through a local `recordActivity(activityType)` helper. No test file exists (verified: no BillRankingCard entry in src/components/__tests__/). Add BillRankingCard.test.ts covering all 4 urgency-state branches and the emit payload shape. {cat:web; size:S; multifile:no; research:none}
- [ ] [P3] Add a functional (non-a11y) unit test for components/modals/ChangePriorityModal.vue — the existing components/__tests__/ChangePriorityModal.a11y.test.ts (verified by reading it) only asserts dialog/aria semantics; the component's real interactive logic — `handleConfirm()` (emits `confirm` with the selected priority + optional trimmed notes, then resets `selectedPriority`/`notes` back to `props.currentPriority`/empty) and `handleClose()` (emits `close` and performs the same reset) — is completely untested (verified: no assertion in the a11y file touches `emitted()`, priority selection, or the notes field). Add a new components/__tests__/ChangePriorityModal.test.ts (separate file, keeping the a11y suite focused) covering: selecting a different priority option and confirming emits the right value, entering notes text is included/omitted correctly (undefined when blank), and closing resets local state. {cat:web; size:S; multifile:no; research:none}
- [ ] [P2] views/NotificationsView.vue is a permanent static placeholder, never fetches real notifications — verified by reading the full 12-line file: it has no `<script setup>` logic at all, just a hardcoded "No Notifications" empty-state template, and is mounted at the real `/notifications` route (router/index.ts line 87-89). Meanwhile a real, working, already-router-tested backend feature exists and is unused by web entirely: app/routers/notifications.py's GET /api/notifications (pagination + `unread_only` filter + `unread_count`), PATCH /api/notifications/{id}, and POST /api/notifications/mark-all-read (all already covered by tests/test_notifications_router.py per the 2026-09-14 refuel). No Pinia store or service for notifications exists anywhere in billwatch-web/src (verified via repo-wide grep for "notification" — the only hits are the unrelated NotificationPreferencesView.vue, about settings not the notification feed itself). Web users can never see in-app notifications despite a fully working, tested backend. Scope: add a `notifications` service/store calling the 3 existing endpoints, and wire NotificationsView.vue to show a real loading/empty/populated list with mark-as-read. {cat:backend+web; size:M; multifile:yes; research:none — endpoints already exist, this is purely a web-wiring gap}
- [ ] [P3] Add unit tests for Android TopicsRepository + TopicsViewModel — billwatch-android/.../data/repository/TopicsRepository.kt (45 lines) and .../ui/screens/TopicsViewModel.kt (55 lines) have zero test coverage (verified: app/src/test/java/com/billwatch/ only contains Auth/Billing/Bills test pairs — no Topics entry), same established gap pattern as the already-flagged SavedSearches/Legislators pairs from round 3. Add TopicsRepositoryTest.kt and TopicsViewModelTest.kt mirroring the existing mockk/coEvery/runTest pattern in AuthRepositoryTest.kt. {cat:mobile; size:S; multifile:yes; research:none}
- [ ] [P3] Add unit tests for Android CivicRepository + FindRepsViewModel + HomeViewModel — billwatch-android/.../data/repository/CivicRepository.kt (29 lines), .../ui/screens/FindRepsViewModel.kt (73 lines), and .../ui/screens/HomeViewModel.kt (54 lines) have zero test coverage (verified: same test-directory listing as above, no Civic/FindReps/Home entries). These back the app's "find your representatives" and home-screen surfaces. Add CivicRepositoryTest.kt, FindRepsViewModelTest.kt, and HomeViewModelTest.kt following the same established mockk pattern. {cat:mobile; size:M; multifile:yes; research:none}
- [ ] [P4] Add unit tests for Android PreferencesRepository + NotificationPreferencesViewModel — billwatch-android/.../data/repository/PreferencesRepository.kt (35 lines) and .../ui/screens/NotificationPreferencesViewModel.kt (56 lines) have zero test coverage (same gap pattern, verified against the same test-directory listing). Add PreferencesRepositoryTest.kt and NotificationPreferencesViewModelTest.kt following the established mockk pattern. {cat:mobile; size:S; multifile:yes; research:none}

## Refuel 2026-09-25 (Claude research pass — roadmap was fully [decomposed]/dry)

All research is grounded in the live repo.

- [ ] [P1] [decomposed] **[LIVE BUG]** Fix `bill_status.py`'s `STATUS_ALIASES` to recognize the literal status strings `congress_api.py` actually writes to `bill.status`, breaking the real-time alert threshold check silently — `CongressAPIClient.determine_bill_status` (`app/services/congress_api.py:443-476`, called from `bill_sync_service.py:212` and persisted directly to `bill.status` at line 223) returns underscored literals `passed_house`/`passed_senate`/`to_president`/`became_law`/`vetoed`, but `app/utils/bill_status.py`'s `STATUS_ALIASES` (lines 15-28) only maps the SPACED variants (`"passed house"`, `"passed senate"`) to `passed_chamber`, and has no entry at all for `to_president`, `became_law`, or `vetoed` (`STATUS_ORDER`, lines 6-11, also has no `vetoed` slot). Since `normalize_status()` only lowercases/strips (no space/underscore normalization), a real production `bill.status` of `"passed_house"` falls through unmatched and `status_rank()` returns -1 for it — verified by reading both files directly. This -1 silently breaks `should_alert()` (`app/utils/alert_threshold.py`: `new_rank > old_rank` becomes `-1 > 1` = False) for real House/Senate passage and veto transitions on the flagship "real-time alerts" feature, and also skews `digest.py`/`digest_grouping.py`'s "most-advanced-first" ordering. Confirmed uncaught: every test in `tests/test_alert_threshold.py` and `tests/test_alert_priority.py` exercises only the spaced/aliased forms (`"Passed House"`, `"Signed Into Law"`) or the already-correct `passed_chamber`/`enacted`, never the real underscored value the sync job actually stores. Fix: add `passed_house`/`passed_senate` (underscore) to `STATUS_ALIASES` alongside the existing spaced keys, add `to_president`/`vetoed` as real rank slots (vetoed as a terminal/dead-end rank, not below `enacted`), and map `became_law` as an alias to `enacted`; add a regression test using the literal strings `determine_bill_status` actually returns. {cat: backend; size: S; multifile: no; research: none}

- [ ] [P1] [decomposed] **[LIVE BUG]** Fix `recommendation_service.py`'s stale "Active"/"Introduced" status literals silently disabling all personalized bill recommendations — `RecommendationService.recommend_bills_for_user` (`app/services/recommendation_service.py:66`) filters candidate bills with `.where(Bill.status.in_(["Active", "Introduced"]))`, but `bill.status` is always written in lowercase snake_case by `determine_bill_status()` (`introduced`, `in_committee`, `passed_house`, etc. — confirmed via `bill_sync_service.py:223` and `congress_api.py:443-476`) — `"Active"` is not a status this app has ever produced, and `"Introduced"` (capital I) never case-matches the stored `"introduced"` under Postgres's default case-sensitive comparison. This means the topic/committee-based match clause (lines 55-64, built from the user's real followed_topics/followed_committees) always returns zero rows against real data, so the code always falls through to `self._trending_excluding(...)` (line 68-69) — every user's "recommended for you" bills are silently just generic trending bills, never actually personalized, regardless of what they follow. Confirmed uncaught: `tests/test_recommendation_service.py`'s `_bill()` fixture helper (line 33) defaults `status="Active"` to match the buggy code, hiding the mismatch from every test in the file. Fix: change the filter to the real lowercase values (e.g. `["introduced", "in_committee", "passed_house", "passed_senate"]`, or reuse `bill_status.status_rank` to select "not yet enacted/dead"), and update the test fixture default to a real status value so a future regression is caught. {cat: backend; size: S; multifile: no; research: none}

- [ ] [P2] [decomposed] Alert the founder when a Congress.gov ingestion source goes stale, instead of only exposing it to end users as a passive badge — `app/services/sync_status_service.py`'s `is_stale()` (lines 68-72) and `staleness_threshold()` (lines 62-66) already compute exactly this signal, but the ONLY consumer anywhere in the codebase is `app/routers/sync_status.py:19` feeding a `DataFreshnessBadge.vue` UI badge (verified via repo-wide grep for `is_stale`/`staleness` — zero hits outside that one router + the service itself). For an app whose entire value proposition is "accurate legislative data," a Congress.gov outage or API-key expiry (both already handled gracefully as retryable errors in `bill_sync_service.py`, but not alarmed on) could go unnoticed for days with nothing but a quiet badge color change — nobody is watching Railway logs 24/7 on a solo-founder app. `app/services/email_service.py` (SendGrid-API-first/SMTP-fallback, already used by `digest_service.py` and `status_change_alert_service.py`) is the natural existing mechanism to reuse. Add a scheduled job (mirroring the existing job-registration pattern in `app/jobs/scheduler.py`) that checks `sync_status_service.get_all_statuses()` against `is_stale()` for each of the 3 KNOWN_SOURCES and emails a configured admin address (a new `admin_alert_email` setting, empty-string-is-no-op like other optional settings) once a source crosses the threshold, deduped so it doesn't re-fire every run while still stale. {cat: backend; size: S; multifile: no; research: none}

- [ ] [P2] [ready] **[HUMAN/design]** Add a real "bill died / session ended" terminal status instead of leaving dead bills stuck at their last real stage forever — verified via direct read of `app/services/congress_api.py`'s `determine_bill_status` (lines 443-476) and `app/utils/bill_status.py`'s `STATUS_ORDER`/`STATUS_ALIASES` that neither has ANY concept of a bill dying at the end of a two-year Congress (the fate of the vast majority — historically ~90%+ — of introduced bills never gets a distinct terminal state; such a bill just silently stops receiving new actions and sits at whatever stage it last reached, e.g. `in_committee`, indefinitely). A user tracking a bill that died in 2025 sees it forever as "in committee" with no signal it's over, which is a direct hit against the "core promise is accurate legislative data" and against the already-shipped "plain-English what changed" framing (a bill going dead IS a status change worth surfacing). This needs a human/Claude design call before decomposing into 27B-sized tasks: the detection heuristic isn't a clean API field — it likely needs "no action in N months AND the Congress that introduced it has since ended" (Congress sessions are known 2-year windows, `congress_number` already exists on `Bill` per `bill_stage.py`/`congress_number.py`) rather than anything Congress.gov returns directly, so the exact threshold/logic deserves a deliberate decision rather than an LLM guess. Once decided, the pipeline is straightforward: new terminal status (e.g. `died`) in `STATUS_ORDER`, a periodic job (same scheduler pattern as `sync_votes_job`) to sweep old-Congress bills below `enacted` and mark them, and a status-change alert/digest entry for the transition (reusing `status_change_summary.py`'s existing plain-English framing). {cat: backend; size: M; multifile: yes; research: needed — human/Claude judgment call on the death-detection heuristic before decomposing}

- [ ] [P2] [ready] Prune invalid/unregistered FCM device tokens after failed push sends instead of retrying them forever — `NotificationService.send_notification` (`app/services/notification_service.py:116-125`) calls `messaging.send_each(messages)` and reads only `response.success_count` to decide overall success, but never inspects the per-message `response.responses[i].exception` results to detect Firebase's `UNREGISTERED`/invalid-token errors (the standard signal that a device uninstalled the app or its token rotated) — verified by reading the full method, there is no token-removal logic anywhere in this 183-line file. Since `POST /me/device-token` (`app/routers/auth.py:312-329`) only ever appends/dedupes tokens and nothing ever removes a bad one, `user.device_tokens` grows monotonically for the lifetime of the account, wasting an FCM call on every send to every dead token forever and never self-healing. Fix: after `send_each`, iterate `response.responses` alongside the original `messages`/tokens list, collect tokens whose result failed with an unregistered/invalid-argument error, and remove them from `user.device_tokens` in the same commit. {cat: backend; size: S; multifile: no; research: none}

- [ ] [P3] [ready] Add `DELETE /api/auth/me/device-token` so a logged-out device stops receiving another user's pushes — `app/routers/auth.py` has `POST /me/device-token` (line 312) to register a token but no corresponding way to remove one; verified via grep for "device-token"/"device_tokens" across `app/routers/` and `app/services/` that no endpoint or logout path ever calls `current_user.device_tokens = [...]` to strip an entry. On a shared or resold device, or simply when a user logs out and a different person logs into the same app install, the old user's device token remains in their `device_tokens` list and keeps receiving that old user's bill-alert pushes indefinitely — a real, live privacy/correctness gap for a native-mobile app with push notifications already shipped on both iOS and Android. Add `DELETE /api/auth/me/device-token` (body or query: `device_token`) mirroring the exact list-filter pattern already used by the POST handler's de-dupe step (`tokens = [t for t in tokens if t.get("token") != token_data.device_token]`), and a matching router test. (Wiring the iOS/Android logout flows to actually call it is a natural mobile follow-up once this exists, deliberately left out of scope here.) {cat: backend; size: S; multifile: no; research: none}

- [ ] [P3] [ready] Wire the already-built `MobileGapAnalyzer` + `mobile_feature_map` static data into a real admin endpoint, instead of leaving it dead — `app/services/mobile_gap_analyzer.py`'s `analyze_mobile_gaps(ios_features, android_features)` (lines 68-99, prioritized iOS-vs-Android feature-parity diffing, already unit-tested in `tests/test_mobile_gap_analyzer.py`) and `app/schemas/mobile_feature_map.py`'s `IOS_FEATURES`/`ANDROID_FEATURES` constants (a real inventory of `billwatch-ios/Sources/Views` and `billwatch-android/.../ui/screens`, per that file's own docstring, dated to the 2026-09-09 mobile-parity research) are both fully-written, tested, pure logic — but verified via repo-wide grep for `mobile_gap_analyzer`/`mobile_feature_map`/`MobileGapAnalyzer` across `app/routers/` and `app/main.py` that NEITHER is imported by any router; only their own test files reference them. Add `GET /api/admin/mobile-gaps` to the existing `app/routers/admin.py` (reusing its established `Depends(require_admin)` pattern, e.g. line 22) that calls `analyze_mobile_gaps(IOS_FEATURES["views"], ANDROID_FEATURES["screens"])` and returns the prioritized gap list, and refresh the two static feature lists against the current file_tree (e.g. add iOS's `AdvancedAnalyticsView`/`RecommendationsView`/`AICoachingView`/`CareerHistoryTimelineView` and Android's `PaywallScreen`/`TopicsScreen`/`AdvancedFilterScreen` if not already present) so the tool reflects reality rather than the 2026-09-09 snapshot. {cat: backend; size: S; multifile: no; research: none}

- [ ] [P4] [ready] Delete 5 more orphaned dead-code backend services with self-testing-only test suites — verified via repo-wide grep that each of the following has ZERO production callers anywhere in `app/` (only their own class-definition line and a same-named test file reference them), matching the same recurring orphan pattern already found and deleted across ~10 earlier refuel rounds: `app/services/advanced_search.py` (`AdvancedSearchService`/`SearchFilter` — its `full_text_search` always returns a hardcoded empty `{"total": 0, "results": []}`; the REAL, wired-in advanced-search endpoint at `/api/search/advanced` actually calls the separate, real `SearchService.advanced_search` in `app/services/search_service.py:116`, confirmed by reading `app/routers/search.py:70`), `app/services/performance_optimization_service.py` (`PerformanceOptimizationService`, in-memory cache never instantiated by anything), `app/services/realtime_service.py` (`RealTimeUpdateService`, WebSocket pub/sub scaffold with no WebSocket route anywhere), `app/services/batch_operations.py` (`BatchOperationService`, hardcoded-fake bulk-op returns like `{"user_id": user_id, "followed": len(bill_ids), "failed": 0}`), and `app/services/image_service.py` (`ImageService`, thumbnail/URL helper never called from any router). Delete all 5 source files plus their dedicated orphan test files: `tests/test_advanced_search_filter.py`, `tests/test_performance_optimization_service.py`, `tests/test_realtime_service.py`, `tests/test_realtime_service_subscribe.py`, `tests/test_batch_operations.py`, `tests/test_image_service.py`. {cat: backend; size: M; multifile: yes; research: none}

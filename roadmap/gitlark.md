# Roadmap — gitlark v2 (mobile plan-and-dispatch command center)
**Maturity:** LOW — active rebuild (P0-P6). Most runway in the portfolio. **NOT a PR-review tool.** **Stopping point:** far off — this is the flagship build-out.

## P1 — the core loop (make it testable)
- [ ] [P1] [decomposed] Planner backend: goal → routed task plan (schemas, plan state machine) {cat: backend; size: L; multifile: yes; research: repo}
- [ ] [P1] [decomposed] PWA: conversation-first mobile UI (talk to repos, see plans) {cat: web; size: L; multifile: yes; research: repo}
- [ ] [P1] [decomposed] Git-mediated dispatch: plan → commit tasks to the fleet's OVERNIGHT_PROGRESS {cat: backend; size: M; multifile: yes; research: repo}
## P2 — the full loop
- [ ] [P2] [decomposed] Review loop: see + merge what the fleet built (PR/diff surfaces, big-diff pagination) {cat: web+backend; size: L; multifile: yes; research: repo}
- [ ] [P2] [decomposed] Conversational control plane: fleet events in chat, add-to-queue from chat, gated ops {cat: backend+web; size: M; multifile: yes; research: repo}
## P3 — Agent Connectors (the differentiator)
- [ ] [P3] [decomposed] Connector abstraction for external coding agents — Normalize on a `Connector` interface with `build_command(task) -> argv`, `parse_output(stream) -> {diff, cost, tokens}`, and `extract_diff(workdir) -> git patch`, since all three targets are shell-invocable today: Claude Code via `claude -p "<prompt>" --output-format json --allowedTools "Read,Edit,Bash" --max-turns N` (JSON reply includes `session_id`, `total_cost_usd`, resumable via session id); aider via `aider --message "<task>" --yes-always --auto-test --test-cmd "..."` (already used by gitlark's fleet, git-diff comes free from aider's own commits); Cursor now ships a genuinely headless path too via `cursor-agent` CLI plus its Background Agent API (as of 2026 it's no longer purely IDE-bound), so it slots into the same interface rather than needing to be excluded. Git-diff extraction is the one step to standardize outside each tool (`git diff` post-run) since only aider auto-commits. {cat: backend; size: L; multifile: yes; research: none — completed 2026-09-10, sources: code.claude.com/docs/en/headless, aider.chat/docs/scripting.html, cursor.com/docs/cli/overview}
- [ ] [P3] [decomposed] Connector registry + event normalization + picker UI {cat: backend+web; size: M; multifile: yes; research: repo}
## P4 — native
- [ ] [P4] [decomposed] Native iOS + Android via Capacitor wrap of the PWA — gitlark's web app is Vue 3 + Vite (confirmed in `web/package.json`), which is exactly Capacitor's sweet spot: point `capacitor.config.ts`'s `webDir` at the existing Vite `dist` output and ship the same code to iOS/Android with near-zero rewrite, adding native shells only for push notifications, biometric approve-and-dispatch, and app-store presence. Avoid React Native/Flutter (would mean a parallel codebase) and a from-scratch native rewrite (not justified pre-PMF); the main non-trivial work is stripping PWA-only install-prompt logic and wiring an OTA update strategy (e.g. Capgo/OtaKit) so approve/dispatch fixes don't wait on app-store review cycles. {cat: mobile; size: L; multifile: yes; research: none — completed 2026-09-10, sources: capgo.app/blog/transform-pwa-to-native-app-with-capacitor, ajmani.dev/how-to-use-capacitor-with-vue-js}
## P2 — Agent Connectors hardening (fleet-verified gaps, added 2026-09-11)
- [ ] [P2] [decomposed] Per-workspace encrypted connector credentials — add a `connector_credentials` table (workspace_id, connector_name, encrypted_secret) and `POST/GET /api/workspaces/{workspace_id}/connectors/{name}/credentials` in a new `backend/app/routers/connector_credentials.py`, encrypting the stored secret with the existing `EncryptionService` in `backend/app/services/encryption.py` (currently only used for memory data) instead of a shared server-wide env var, so each workspace can hold its own Claude Code / aider / Cursor API key. {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Per-repo connector run lock — add `backend/app/services/connector_lock.py` (in-process asyncio lock keyed by `workspace_id:repo`) and acquire it around any connector dispatch path, returning 409 with the current holder's task id on contention; `backend/app/services/action_guard.py` today only gates which actions a tier may request (`check_fleet_operation_gate`), nothing serializes two connector runs against the same target repo/branch, which is the exact class of bug flagged in this portfolio's "parallel agents can sweep each other's git commits" incident. {cat: backend; size: S; multifile: no; research: none}
## P3 — Agent Connectors reporting + review round-trip
- [ ] [P3] [decomposed] Connector Runs dashboard page — new `web/src/pages/ConnectorRunsPage.vue` + `web/src/stores/connectorRuns.ts`, modeled on the existing `UsageDashboardPage.vue`/`stores/analytics.ts` pattern, listing historical connector invocations (connector name, task, cost_usd, tokens, success/fail) once `backend/app/services/event_normalizer.py` starts normalizing connector-run-completed events (today it only special-cases `connector_id == "github"` push events and passes everything else through raw); add a `GET /api/connectors/runs` endpoint to `backend/app/routers/connectors.py` backing it. {cat: web; size: M; multifile: yes; research: none}
- [ ] [P3] [decomposed] "Send review feedback to connector" round-trip — add a `POST /api/workspaces/{workspace_id}/pull-requests/{number}/reinvoke` action in `backend/app/routers/pull_requests.py` (which today only supports list/get/merge, per `_get_config_and_token` + `merge_pull_request`) that packages a PR's unresolved inline review comments (built via `backend/app/services/inline_comments.py`'s `build_inline_comments` output) into a new PlanTask routed back to the same connector that produced the diff, plus a "Send to connector" button on `web/src/components/CodeReviewPanel.vue`. {cat: backend+web; size: M; multifile: yes; research: none}
## P2/P3 — repo-intelligence, multi-repo dispatch, and tooling (added 2026-09-13)
- [ ] [P2] [decomposed] Ground plan generation in real repo analysis — Wire the already-built `RepoAnalysis` model/data (`backend/app/models/repo_analysis.py`, populated via `backend/app/services/repo_analyzer.py`'s `RepoAnalyzer.analyze`) into `PlannerService.generate_plan()`: have `_fetch_repo_context` in `backend/app/routers/plans.py` (currently returns a bare file-path list only) also load the workspace's latest `RepoAnalysis` row and pass its `primary_language`/`frameworks`/`dependencies` into `backend/app/services/planner.py`'s `_build_prompt`, so generated tasks account for the repo's actual stack instead of guessing from filenames alone. {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Multi-repo plan fan-out — Today a `Plan` (backend/app/models/plan.py) belongs to exactly one `workspace_id`, and `DispatcherService.dispatch()` (backend/app/services/dispatcher.py) writes every task in a plan using one shared `ProjectConfig`, so a single conversational goal can only ever target one repo — at odds with the roadmap's own "talk to repos" (plural) framing. Add a nullable `target_workspace_id` FK on `PlanTask`, defaulting to the plan's own workspace; let `web/src/pages/PlanPage.vue` assign individual tasks to a sibling workspace the user owns; update `DispatcherService.dispatch` to resolve each task's `ProjectConfig` by its own `target_workspace_id` before writing it to that repo's queue file. {cat: backend+web; size: L; multifile: yes; research: none}
- [ ] [P3] [decomposed] Review rule template packs — `ReviewRule` (backend/app/models/review_rule.py) is 100% custom-authored per workspace today (plain CRUD in backend/app/routers/review_rules.py, no seeded content). Add an `is_template: bool` column plus a `POST /api/workspaces/{workspace_id}/review-rules/apply-template` endpoint that accepts a template key ("security" | "testing" | "style") and bulk-inserts 4-6 pre-written `ReviewRule` rows for that category, plus a "Start from a template" picker in `web/src/pages/ReviewRulesPage.vue`, so a new workspace gets useful review coverage without hand-writing every rule. {cat: backend+web; size: M; multifile: yes; research: none}
- [ ] [P3] [decomposed] VS Code extension: Plans tree view — the extension already ships `WorkspacesTreeProvider` / `ConversationsTreeProvider` / `SnippetsTreeProvider` (vscode-extension/src/views/*.ts) but nothing surfaces Plans, even though the backend already exposes `GET /api/workspaces/{workspace_id}/plans` and `GET .../plans/{plan_id}/status` (backend/app/routers/plans.py). Add `vscode-extension/src/views/plans-tree.ts` (a `PlansTreeProvider` following the existing `WorkspacesTreeProvider` pattern), a `getPlans`/`getPlanStatus` pair in `vscode-extension/src/services/api-client.ts`, and register the new view in `vscode-extension/src/extension.ts`, so a developer can see plan/task dispatch status without leaving the editor. {cat: web; size: M; multifile: yes; research: none}

## Stopping point
Not near. Revisit priorities once the PWA loop (P1+P2) is testable end-to-end.

## P1 — Command Center v1 (greenlit 2026-09-09) — 27B-buildable backend foundations
- [ ] [P1] [decomposed] LocalLLMClient service: OpenAI-compatible streaming client for the LiteLLM box, mirroring ClaudeClient.stream_message signature (httpx SSE), + LOCAL_LLM_* settings in core/config {cat: backend; size: M; multifile: no; research: repo}
- [ ] [P1] [decomposed] Repo-file GET/PUT endpoint: thin wrapper over github get_file_content/create_or_update_file returning content+sha (powers the doc viewer/editor) {cat: backend; size: S; multifile: no; research: repo}
- [ ] [P1] [decomposed] Single ad-hoc task enqueue endpoint: append one backlog line to a repo queue file, reusing task_to_backlog_line + dispatch dedup {cat: backend; size: S; multifile: no; research: repo}
- [ ] [P1] [decomposed] Stage-run stats aggregator: pure module parsing stage_runs jsonl -> by-tier verified counts, tok totals, tok/s, median/max step, fail histogram (ports ovn_stage_stats.py, unit-tested) {cat: backend; size: M; multifile: no; research: repo}
- [ ] [P1] [decomposed] Packaging enablers: extract the backlog item-line grammar into a versioned serialize/parse module + de-hardcode status_reader staging branch/keywords via config {cat: backend; size: S; multifile: no; research: repo}

## P2 — Agent Connectors wiring gaps (verified against live code, added 2026-09-14)
- [ ] [P2] [decomposed] Wire `POST /api/connectors/reinvoke` — `web/src/components/CodeReviewPanel.vue`'s "Send to connector" button calls `useApi.ts`'s `reinvokePullRequest()` (`api.post('/connectors/reinvoke', ...)`), but no such route exists anywhere in the backend (`grep -rn reinvoke backend/app` only finds the unused `backend/app/services/reinvoke_service.py`), so the button 404s in the running app today. Add a `POST /api/connectors/reinvoke` route (in `backend/app/routers/connectors.py`, alongside the existing `list_connectors`/`register_connector`) that accepts `{repo_owner, repo_name, pr_number, review_data}`, resolves the PR's unresolved inline comments via `build_inline_comments` (`backend/app/routers/pull_requests.py` line 95), and calls the already-tested `build_reinvoke_payload(pr_number, inline_comments)` (`backend/app/services/reinvoke_service.py`) to produce the connector payload. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] Fix connector-runs URL mismatch — `web/src/stores/connectorRuns.ts`'s `fetchRuns()` calls either `/connectors/${connectorId}/runs` or `/connector-runs`, but the only real backend route is `GET /api/connectors/runs` (`backend/app/routers/connectors.py`'s `list_connector_runs`, prefix `/api/connectors`) — neither frontend path resolves, so `ConnectorRunsPage.vue` always shows "No connector runs found" even when data exists, and this is untested because `ConnectorRunsPage.spec.ts` mocks `useApi` entirely. Change `fetchRuns` to always call `/connectors/runs` (dropping the broken per-connector-id and hyphenated variants) to match the real route. {cat: frontend; size: S; multifile: no; research: none}
- [ ] [P2] [decomposed] Wire real persistence into connector credentials + register the router — `backend/app/routers/connector_credentials.py` is entirely mocked (`create_connector_credential` returns a hardcoded `id=1` without saving anything; `list_connector_credentials` always returns `[]`; get/update/delete always 404), even though the exact model the roadmap called for already exists and is unused: `ConnectorCredential` in `backend/app/models/connector.py` (`workspace_id`, `connector_name`, `encrypted_secret` columns). Worse, the router is never mounted — `backend/app/main.py`'s router import list and `app.include_router(...)` calls (lines 14 and ~120-138) omit `connector_credentials` entirely, so none of its endpoints are reachable in the real app; its own test file only passes because it builds a throwaway standalone `FastAPI()` app. Rewrite the router's 5 handlers to query/insert/update/delete real `ConnectorCredential` rows via the injected `db: AsyncSession`, encrypt secrets with `encryption_service.encrypt` (fixing the current call which passes a plain string secret and a user id where the service expects `(data: dict, secret: str)`, per `backend/app/services/encryption.py`), and add `app.include_router(connector_credentials.router)` to `backend/app/main.py`. {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Multi-repo plan fan-out — expose target_workspace_id in the UI — the backend already fully supports per-task workspace routing (`PlanTask.target_workspace_id` in `backend/app/models/plan.py`, `PlanTaskUpdate.target_workspace_id` in `backend/app/schemas/plan.py`, and `DispatcherService`'s workspace resolution in `backend/app/services/dispatcher.py` lines 138-159), and `web/src/stores/plans.ts`'s `updateTask()` already PATCHes `/workspaces/{workspaceId}/plans/{planId}/tasks/{taskId}` — but `updateTask`'s TS parameter type only allows `{path?, spec?, verify?}` (no `target_workspace_id`), and `web/src/pages/PlanPage.vue` has zero UI to assign a task to a sibling workspace. Add `target_workspace_id?: string` to `updateTask`'s data type in `stores/plans.ts`, and add a per-task workspace picker in `PlanPage.vue` (populate options from the existing `GET /workspaces` call already used by `web/src/stores/workspace.ts` line 54) that calls `store.updateTask(..., { target_workspace_id })`. {cat: frontend; size: S; multifile: yes; research: none}
- [ ] [P3] [decomposed] VS Code extension: fix duplicate PlansTreeProvider — the real, API-wired `PlansTreeProvider` (`vscode-extension/src/views/plans-tree.ts`, constructor takes `GitLarkApiClient` and calls `apiClient.getPlans(workspaceId)` in `loadPlans()`) is never imported or registered anywhere except its own test (`views/plans-tree.test.ts`). Instead, `vscode-extension/src/extension.ts` defines and registers a second, dead-end `PlansTreeProvider` class inline (lines ~61-89, backed by a static `plans: PlanNode[] = []` with a `setPlans()` nobody calls) for the `plansView` tree view declared in `package.json`. Delete the inline duplicate class + its `new PlansTreeProvider()`/`createTreeView('plansView', ...)` block from `extension.ts`, and instead wire the real one into `vscode-extension/src/extension-core.ts`'s `registerTreeViews()` (which already follows this exact pattern for `WorkspacesTreeProvider`/`ConversationsTreeProvider`/`SnippetsTreeProvider`, lines 153-178): `new PlansTreeProvider(this.apiClient)` + `vscode.window.registerTreeDataProvider('plansView', plansProvider)`, plus a `gitlark.refreshPlans` command. {cat: vscode-extension; size: S; multifile: yes; research: none}

## P2/P3 — real bug/gap sweep against live code (added 2026-09-14, cycle 2)
- [ ] [P2] [decomposed] Wire Team Management page to the real workspace-members API — `web/src/pages/TeamManagementPage.vue`'s `loadTeamMembers()` and `sendInvite()` are 100% fake: hardcoded `members.value = [{ id: '1', name: 'Mark Hintermeister', ... }]` and literal `// API call would go here` comments, and the page imports `useApi` (`import { useApi } from '@/composables/useApi'`) but never actually calls it; it's also not registered in `web/src/router.ts` at all (grep for `TeamManagement` in router.ts returns nothing), so it's unreachable from the app today. Meanwhile a complete, mounted backend already exists for this exact purpose: `backend/app/routers/collaborative_workspace.py`'s `GET/POST /api/workspaces/{workspace_id}/members` (`get_members`/`add_member`), `PUT .../members/{user_id}/role` (`update_member_role`), and `DELETE .../members/{user_id}` (`remove_member`), all backed by real DB-backed methods on `backend/app/services/workspace.py` (`get_workspace_members` line 646, `add_member` line 233, `update_member_role` line 264, `remove_member` line 302). Rewrite the page's two functions to call these via `useApi`, and add a route for it in `router.ts`. {cat:frontend; size:M; multifile:yes; research:repo}
- [ ] [P3] [decomposed] Wire Code Insights page to a real analysis endpoint (or drop it) — `web/src/pages/CodeInsightsPage.vue`'s `analyzeRepository()` does `await new Promise(resolve => setTimeout(resolve, 1000))` then assigns hardcoded `categories`/`topIssues` literals (e.g. a fixed "SQL injection in app/services/auth_service.py" finding that never changes); the page is also never imported by `router.ts` or any other component (grep for `CodeInsightsPage` outside its own file returns nothing), so it's dead and unreachable. There's a matching, fully-built but equally unused backend service, `backend/app/services/ml_code_insights.py`'s `MLCodeInsightsService.analyze_repository(repository_id, file_paths, code_samples)` returning a `CodeQualityReport` (quality_score, categories, top_issues) — it has zero callers anywhere in `backend/app/routers/*.py`. Add a `GET/POST /api/workspaces/{workspace_id}/code-insights` route in a small new router calling `MLCodeInsightsService.analyze_repository`, register the page in `router.ts`, and replace the page's fake `setTimeout` logic with a real fetch. {cat:backend; size:M; multifile:yes; research:repo}
- [ ] [P2] [decomposed] Mount workspace conversation search and fix its route collision with per-conversation search — `web/src/stores/conversation.ts`'s `searchConversations()`/`getSearchSuggestions()` (with an in-code comment dated 2026-09-05: "KNOWN BROKEN ... the backend doesn't implement at all") call `GET /conversations/{workspaceId}/search?q=...` and `.../search/suggestions` (resolved to `/api/conversations/{workspaceId}/search` via the `/api` baseURL in `web/src/services/api.ts`). That comment is now stale: `backend/app/services/search.py`'s `ConversationSearchService.search()`/`get_suggestions()` implement exactly this (workspace-scoped, `agent_type`/`days` filters) behind a router at `@router.get("/{workspace_id}/search")` / `.../search/suggestions` — but that router is never imported or mounted in `backend/app/main.py` (grep for `search` in main.py's imports/include_router calls returns nothing), AND its `prefix="/conversations"` (not `/api/conversations`) would collide path-for-path with the already-mounted, already-fixed single-conversation search at `backend/app/routers/conversations.py`'s `GET /api/conversations/{conversation_id}/search` (both are `/api/conversations/{id}/search` once `/api` is added). Rename `search.py`'s route to a non-colliding path (e.g. `/api/workspaces/{workspace_id}/conversations/search`), mount its router in `main.py`, and update `conversation.ts` + `ConversationSearch.vue`'s call sites to match. {cat:backend; size:M; multifile:yes; research:repo}
- [ ] [P2] [decomposed] Build a notification bell/panel UI for the already-built notification APIs — two complete, mounted backend notification systems have zero frontend consumers today (grep for `notifications` across `web/src/**/*.vue`/`*.ts` only matches unrelated text in `PrivacyPage.vue`): (1) per-user `backend/app/routers/notifications.py`'s `GET /api/notifications` (`list_notifications`, returns `{notifications, unread_count}`), `POST /api/notifications/{id}/read` (`mark_notification_read`), `POST /api/notifications/read-all` (`mark_all_read`), backed by `NotificationService`; and (2) per-workspace `backend/app/routers/notifications_feed.py`'s `GET /api/workspaces/{workspace_id}/notifications-feed` (`list_notification_events`), `.../unread-count` (`get_unread_count`), `PATCH .../{event_id}/read` (`mark_read`). Add a `NotificationBell.vue` component (badge from the unread count, dropdown list, mark-read on click) mounted in `web/src/App.vue`'s header, calling the per-user endpoints first since they need no workspace context. {cat:frontend; size:M; multifile:yes; research:repo}
- [ ] [P3] [decomposed] Consolidate duplicate dead FeatureFlag service implementations — `backend/app/services/feature_flags.py`'s `FeatureFlagService` (hash-bucketed rollout_percent, `create_flag`/`is_enabled`/`update_rollout`/`get_all_flags`) and `backend/app/services/feature_flags_service.py`'s `FeatureFlagsService` (a hardcoded dict of 5 always-on flags) are two competing, near-identically-named implementations of the same concept — both are completely dead code (grep for `FeatureFlagService`/`FeatureFlagsService` outside their own defining files, and across `routers/*.py`, returns nothing; no router, no frontend caller of any kind exists for either). Delete `feature_flags_service.py`, keep `feature_flags.py`'s rollout-percent version (the more capable of the two), and add a minimal `GET /api/feature-flags` route in a new `backend/app/routers/feature_flags.py` backed by it so the dead code either becomes real or is removed rather than sitting as confusing duplication. {cat:backend; size:S; multifile:yes; research:repo}
- [ ] [P3] [decomposed] Fix ReviewCollaborationManager's WebSocket path mismatch and mount it in the review UI — `web/src/components/ReviewCollaborationManager.vue` builds `wsUrl = ws://${window.location.host}/api/ws/reviews/${props.reviewId}`, but the real backend route is `backend/app/routers/review_collab.py`'s `router = APIRouter(prefix="/api/ws/reviews")` + `@router.websocket("/ws/{review_id}")`, i.e. the full path is `/api/ws/reviews/ws/{review_id}` — the frontend is missing the `/ws` segment, so the socket will always fail to connect. The component is also never imported anywhere else in `web/src` today (grep for `ReviewCollaborationManager` outside its own file returns nothing), so this bug has never been hit in practice. Fix the URL to include the missing `/ws` segment, and mount `<ReviewCollaborationManager :review-id="props.reviewId" :user-id="..." :user-name="..." />` inside `web/src/components/EnhancedCodeReview.vue` (which already receives a matching `reviewId` prop and calls `/api/code-reviews/${props.reviewId}` at line 98) to actually surface live multi-user review presence. {cat:frontend; size:M; multifile:yes; research:repo}
- [ ] [P3] [decomposed] Wire VS Code snippet star/delete into commands and context menu — `vscode-extension/src/services/api-client.ts`'s `starSnippet(snippetId)` (posts to `/api/snippets/${snippetId}/star`) and `deleteSnippet(snippetId)` (deletes `/api/snippets/${snippetId}`) both wrap real, mounted backend endpoints (`backend/app/routers/snippet.py`'s `toggle_star`/`delete_snippet`), but neither method is called anywhere else in the extension — no `vscode.commands.registerCommand` in `extension-core.ts`, no context-menu entry in `package.json` (`view/item/context` is absent entirely), and `views/snippets-tree.ts`'s `SnippetTreeItem` only wires an "Open Snippet" command. Also fix `starSnippet` itself: `toggle_star`'s handler requires a `SnippetStarRequest` body with a `starred: boolean` field, but the client currently POSTs with no body at all, so even a wired-up call would 422. Add `gitlark.starSnippet`/`gitlark.deleteSnippet` commands, register them in `package.json`'s `view/item/context` menu for `gitlarkSnippets` tree items, and fix the missing request body. {cat:vscode-extension; size:S; multifile:yes; research:repo}

## P2/P3 — live-code audit refuel: roadmap was dry, backlog re-fed (added 2026-09-17)
- [ ] [P2] [decomposed] Route the already-built Connector Runs page — `web/src/pages/ConnectorRunsPage.vue` is a complete, functional page (header, refresh button, loading/error/empty states, `runs` list) but is never imported or referenced by `web/src/router.ts` (grep for `Connector` in `router.ts` returns nothing across all 158 lines; grep for `ConnectorRunsPage` outside its own file returns nothing anywhere in `web/src`), so it's unreachable in the running app even though the backend route it needs (`GET /api/connectors/runs`, `backend/app/routers/connectors.py`) already exists. Add an import + route (e.g. `/dashboard/workspace/:id/connector-runs`) in `router.ts` and a nav link, following the exact pattern of the neighboring `PullRequestsPage`/`ReviewRulesPage` routes already there (lines 85-101). {cat:frontend; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Delete the dead duplicate `GitHubFileService` — `backend/app/services/github_file_service.py`'s `GitHubFileService` class (`get_file_content` line 25, `create_or_update_file` line 45) re-implements the exact same GitHub Contents API GET/PUT that's already live and wired: `backend/app/routers/github.py`'s `get_file`/`put_file` endpoints (lines ~182/203) call `GitHubClient.get_file_content`/`create_or_update_file` from `backend/app/services/github.py` (imported line 8), not `GitHubFileService`. Grep confirms `GitHubFileService`/`github_file_service` has zero callers anywhere in `backend/app` outside its own file (`grep -rln 'GitHubFileService|github_file_service' backend/app --include='*.py'` matches only the defining file), and its only consumer is its own unit test (`backend/tests/test_github_file_service.py`). Delete the orphaned service + its test, since the real, tested, endpoint-backed implementation already covers this (this is the same "two competing implementations of one concept" pattern already fixed once for `FeatureFlagService`/`FeatureFlagsService` on 2026-09-14). {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Delete the dead duplicate `RealTimeUpdateService` — `backend/app/services/realtime_update_service.py`'s `RealTimeUpdateService` (48 lines, in-memory `Dict[str, List[Any]]` connection tracker, class at line 9) is a second, much cruder real-time-updates implementation that duplicates the concept already fully built and wired as `backend/app/services/realtime_service.py`'s `RealtimeService` (310 lines, operator/publish pattern, module-level singleton `realtime_service = RealtimeService()` at line 310, actually used by `backend/app/routers/realtime.py` at 8 call sites starting line 14). `RealTimeUpdateService` has zero test file (unlike every other orphaned pure-module in this repo, which at least has a `backend/tests/test_*.py`) and zero callers anywhere in `backend/app` — it is the most completely dead file found in this audit. Delete it. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Wire the built-and-tested severity scoring + Markdown formatting into the AI code review endpoint — `backend/app/services/review_score.py`'s `aggregate_severity_score(findings)` (line 17, rolls a findings list into `{score, rating, counts_by_severity}` using the exact `critical/high/medium/low` severity vocabulary) and `backend/app/services/review_formatting.py`'s `format_review_summary(findings)` (line 6, renders a grouped, collapsible Markdown PR-comment) are both complete, unit-tested pure functions with zero callers anywhere in `backend/app` (grep for `review_score|ReviewScore|review_formatting|ReviewFormatting` outside their own defining files returns nothing). Meanwhile `backend/app/services/code_review_agent.py`'s `CodeReviewResult` (line 25) already carries an `issues: list[dict]` field (line 29) whose items the LLM prompt (line ~103) is instructed to tag with the identical `"severity": "critical|high|medium|low"` vocabulary, and `analyze_code_for_review` (line 185, the `POST /conversations/{conversation_id}/code-review` handler) returns that raw LLM output with only a free-text `overall_rating` ("excellent"/"good"/"needs-work") — no deterministic score, no Markdown summary for a PR comment. Call `aggregate_severity_score(result.issues)` and `format_review_summary(result.issues)` inside `analyze_code_for_review` (or `CodeReviewAgent.review_pull_request`) and add `score`/`rating`/`markdown_summary` fields to `CodeReviewResult`, so two already-built, already-tested capabilities stop sitting unused next to the exact feature that needs them. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Replace github_webhook's hand-rolled HMAC check with the shared, already-tested `webhook_signature` helper — `backend/app/routers/github_webhook.py` defines its own inline `_verify_signature(raw_body, signature_header)` (lines 35-41: builds `"sha256=" + hmac.new(...).hexdigest()` and compares) and calls it at line 160 to gate the whole webhook handler, but a more rigorous, unit-tested equivalent already exists and is never called: `backend/app/services/webhook_signature.py`'s `verify_signature(payload, secret, header)` / `compute_signature(payload, secret)` (adds header-format validation — non-str/empty header and missing `sha256=` prefix both fail closed — and type-checks its inputs, none of which the router's inline copy does). Grep confirms `webhook_signature` has zero callers anywhere in `backend/app` outside its own file. Security-relevant logic should have exactly one implementation: replace the router's inline `_verify_signature` with a call to `webhook_signature.verify_signature`, and delete the now-redundant local copy. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Consolidate the duplicate, fully-dead in-memory vector-search stubs — `backend/app/services/semantic_search.py`'s `SemanticSearch` (52 lines: `index_document`/`search_semantic`, in-memory `Dict[str, List[float]]`, cosine similarity) and `backend/app/services/vector_store.py`'s `VectorStore` (52 lines: `add_vector`/`search_similar`, same in-memory dict + cosine-similarity pattern, plus a `metadata` field `SemanticSearch` lacks) are near-identical, non-persistent implementations of the same embedding-search concept, and grep confirms both have zero callers anywhere in `backend/app` outside their own files and their own unit tests (`test_semantic_search.py`, `test_vector_store.py`) — no router, no other service, nothing in `web/src` calls either. Neither is wired to anything that could produce real embeddings (no Claude/OpenAI embeddings call, no DB table). Pick one (VectorStore's `metadata` support is strictly more useful), delete the other, and either wire the survivor into snippet search (`backend/app/routers/snippet.py`'s `search_public_snippets`, currently keyword-only) as a real feature or delete both if there's no near-term embeddings-generation plan — don't leave two competing stubs. {cat:backend; size:M; multifile:yes; research:repo}
- [ ] [P3] [decomposed] Port the VS Code "Enhanced Code Review" command out of stranded Python pseudocode — `vscode-extension/src/commands/enhanced-review.ts.py` (235 lines, note the `.ts.py` double extension) contains a fully-designed feature — `VSCodeEnhancedReviewCommand` with `get_git_original`/`generate_diff_for_vscode` for an inline-diff-plus-comments review view bound to command `gitlark.requestEnhancedReview` / keybinding Cmd+Shift+Alt+R per its own docstring — written entirely in Python inside the TypeScript extension's `commands/` directory, so it is never compiled, never imported (`grep -rn 'enhanced-review' vscode-extension/src` matches only the file itself), and `gitlark.requestEnhancedReview` appears nowhere in `vscode-extension/package.json`'s command contributions (only `gitlark.requestCodeReview` is declared, e.g. lines 34/77/162) or in `vscode-extension/src/extension-core.ts`'s `registerCommands` (line 93 registers `requestCodeReview` only, no enhanced variant). Either port the git-diff/inline-comment logic to real TypeScript, register the command + keybinding + package.json contribution following the existing `requestCodeReview` pattern, or delete the stranded file if the feature was superseded by `web/src/components/CodeReviewPanel.vue`. {cat:vscode-extension; size:M; multifile:yes; research:repo}

## Newly discovered (2026-09-18 refuel round 2 — backlog dry again)
- [ ] [P3] [decomposed] Consolidate three competing dead AST-based code-analysis services — `backend/app/services/code_analysis.py`'s `CodeAnalyzer` (line 14: real, non-stub AST scanner — `detect_code_patterns`, `calculate_complexity`, `detect_vulnerabilities` for eval/exec/os.system/pickle.loads/yaml.load/weak-hash/assert findings), `backend/app/services/code_quality_metrics.py`'s `CodeQualityMetrics` (line 6: overlapping cyclomatic-complexity + code-smell + maintainability-index calculator), and `backend/app/services/security_audit.py`'s `SecurityAuditor` (line 4: a pure hardcoded-return stub — `scan_code_vulnerabilities` always returns the same fake SQL-injection finding, `generate_security_report` always returns `score: 85`) are three independent, non-identical implementations of "analyze this code and report issues," and grep confirms all three classes have zero callers anywhere in `backend/app` outside their own defining files (`grep -rn 'CodeAnalyzer\|CodeQualityMetrics\|SecurityAuditor' backend/app --include='*.py'` matches only lines 14/6/4 themselves), despite each having its own dedicated test files (`test_code_analysis*.py` x5, `test_code_quality_metrics.py`, `test_security_audit.py`). A fourth related orphan, `backend/app/services/severity_mapping.py`'s `FINDING_TYPE_TO_SEVERITY` dict (line 8) and `map_finding_severity` (line 48), uses a vocabulary (`eval_use`, `os_system`, `pickle_loads`, `bare_except`, `deep_nesting`, ...) that looks purpose-built to consume `CodeAnalyzer`'s pattern/vulnerability output, and is also a zero-caller orphan. This is the same "two-to-many competing implementations of one concept" pattern already fixed once for `FeatureFlagService`/`FeatureFlagsService` (2026-09-14) and again this morning for `GitHubFileService`. Note: this is a *different* cluster from the already-tracked `ml_code_insights.py`/`CodeInsightsPage.vue` item (roadmap line 47, 2026-09-14 cycle 2, still undone) — that item wires a *fourth*, separate `MLCodeInsightsService`. Pick one real implementation (`CodeAnalyzer` is the most complete, genuinely-AST-based, non-stub option), wire `map_finding_severity`/`severity_rank` from `severity_mapping.py` onto its output, delete `CodeQualityMetrics` and `SecurityAuditor`, and either back the CodeInsights work with the survivor or keep them as clearly-separate concerns if `ml_code_insights.py` is kept for a different (ML-based) analysis path. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P3] [decomposed] Wire `pr_label_classifier.classify_pr_size` into the PR detail response — `backend/app/services/pr_label_classifier.py`'s `classify_pr_size(additions, deletions)` (line 1, pure function returning XS/S/M/L/XL) has zero callers anywhere in `backend/app` (`grep -rn classify_pr_size backend/app --include='*.py'` matches only its own file and `backend/tests/test_pr_label_classifier.py`), even though `backend/app/routers/pull_requests.py`'s `get_pull_request` handler (line 188) already fetches the full GitHub PR object (`bundle["pr"]` at line ~204, which the GitHub REST API populates with top-level `additions`/`deletions`/`changed_files` fields for a single-PR fetch) and builds a `PullRequestDetail` response (`class PullRequestDetail` line 74, constructed at line 213) that has no size field at all. Add a `size_label: str` field to `PullRequestDetail`, compute it via `classify_pr_size(pr.get("additions", 0), pr.get("deletions", 0))` in `get_pull_request`, and surface it as a badge in `web/src/pages/PullRequestsPage.vue`. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Make PR diff pagination real — it currently computes metadata but never slices — `backend/app/routers/pull_requests.py`'s `get_pull_request` handler (line 188) always returns the PR's *entire* file list (`files = [PullRequestFile(**f) for f in bundle["files"]]`, line 209) regardless of size, then separately computes `pagination_meta = build_diff_pagination_meta(total_lines, page_size=50)` (line 211, using a router-local helper at line 145 that only returns `{total_lines, page_size}` — it never slices anything). The handler accepts no `page` query parameter at all. Meanwhile a real, tested pagination helper already exists and is completely unused: `backend/app/services/diff_paginate.py`'s `page(files, page, per_page)` (line 1, 1-based slicing with bounds handling), covered by `backend/tests/test_diff_paginate.py`, with zero callers anywhere in `backend/app` (`grep -rn 'diff_paginate\|from app.services.diff_paginate' backend/app --include='*.py'` matches only its own file and test). Frontend never reads `diff_pagination_meta` either (`grep -rn 'diff_pagination_meta\|page_size' web/src --include='*.vue' --include='*.ts'` returns nothing outside `__tests__`), so a large PR's full diff is always shipped in one response despite the API pretending to support paging. Add a `page: int = 1` query param to `get_pull_request`, use `diff_paginate.page(bundle["files"], page, 50)` to actually slice before building `PullRequestFile` objects, and wire `PullRequestDetail.vue`/whatever renders the diff to request subsequent pages. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P2] [decomposed] Wire `auto_merge.can_auto_merge` into the merge endpoint — it exists, is tested, and gates nothing today — `backend/app/services/auto_merge.py`'s `can_auto_merge(findings, checks_passed)` (line 1: blocks merge if `checks_passed` is False or any finding has `severity` in `critical`/`high`) has zero callers anywhere in `backend/app` — the only other repo-wide match for the string `auto_merge` is an unrelated event-type literal `"strategy": "auto_merge"` in `backend/app/services/realtime_service.py` line 164. Meanwhile `backend/app/routers/pull_requests.py`'s `merge_pull_request` handler (line 226) merges unconditionally via `github_client.merge_pull_request(...)` with no severity or review-finding gate at all — it only surfaces whatever GitHub itself reports as `mergeable`. `backend/app/services/code_review_agent.py`'s `CodeReviewResult` (line 27) already carries an `issues: list[dict]` field (line 31) whose items use the same `critical/high/medium/low` severity vocabulary `can_auto_merge` expects. Call `can_auto_merge(latest_review.issues, checks_passed)` in `merge_pull_request` (fetching the PR's latest stored code-review result, or via a query param the frontend passes) and return 409 with `reasons` when `allowed` is False, so critical/high-severity AI review findings actually block a merge instead of being purely informational. {cat:backend; size:M; multifile:yes; research:repo}
- [ ] [P4] [decomposed] Delete or finish the entirely-dead, internally-incomplete `api_versioning.py` — `backend/app/core/api_versioning.py`'s `APIVersion` class (line 4) is never imported or referenced anywhere in `backend/app` outside its own file and `backend/tests/test_api_versioning*.py` (`grep -rn 'api_versioning\|APIVersion' backend/app --include='*.py'` confirms this), so `register_endpoint`/`get_handler`/`get_version_info` are all unreachable dead code — no router calls `get_version_info()`, and `app/main.py`'s `FastAPI(... version="0.1.0")` (line ~65) is a hardcoded, disconnected string that doesn't match `APIVersion.VERSION = "1.0.0"` (line 7). It's also internally unfinished: `_get_legacy_handler` (line 37) is a literal `# TODO: Implement legacy handler mapping` / `pass` stub (line 39-40) that would silently return `None` for any deprecated-version lookup even if the class were wired up. Either delete the module + its two test files as unused scaffolding, or finish it minimally: implement `_get_legacy_handler`, add a `GET /api/version` route backed by `get_version_info()`, and make `main.py`'s `FastAPI(version=...)` read from `APIVersion.VERSION` instead of a separate hardcoded string. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Review-collaboration WebSocket trusts a client-supplied user_id with zero token verification — `backend/app/routers/review_collab.py`'s `websocket_review_collaboration` handler (line 20) imports `get_current_user` (line 13) but never calls it; instead it reads an unauthenticated JSON handshake (`handshake = await websocket.receive_json()`, line 48) and does `user_id = UUID(handshake.get("user_id"))` (line 49) directly off client input, per the handler's own acknowledging comment "In production, validate token from query param / For now, use basic connection tracking" (line 44). This means any WebSocket client can claim to be any user UUID and have `review_collab_manager.start_review_session` broadcast cursor/status/presence updates under that identity to every other connected reviewer on that review — a real identity-spoofing gap in a multi-user collaboration feature, not a hypothetical one. `app/core/dependencies.py`'s `verify_token(token)` (imported via `app.core.security`) is already usable standalone (it's what `get_current_user` calls internally, `app/core/dependencies.py` line 18) and doesn't require the `HTTPBearer` header flow, so it can be called directly against a `?token=` websocket query param. Read `websocket.query_params.get("token")`, call `verify_token` to derive the real user id, and reject the connection (close with a policy-violation code) if it's missing/invalid, instead of trusting the handshake body. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Normalize LLM-generated snippet tags before they hit the DB — `backend/app/services/tag_normalizer.py`'s `normalize_tags(tags, max_tags=10)` (line 1: strips whitespace, lowercases, dedupes, caps at `max_tags`) has zero callers anywhere in `backend/app` (`grep -rn normalize_tags backend/app --include='*.py'` matches only its own file and `backend/tests/test_tag_normalizer.py`), even though `backend/app/services/snippet.py`'s `create_snippet` takes `ai_tags` straight from an unvalidated Claude JSON response (`_generate_ai_metadata`, line ~408) and loops over it unfiltered (`for tag_name in ai_tags:`, line 99) calling `_get_or_create_tag` (line 449), which only lowercases (`tag_name.lower()`, line 457) — it does not strip whitespace, so `"react"` and `" react"` become two distinct `Tag` rows, and it does not dedupe the incoming list before inserting, so a LLM response like `["react", "React", "react"]` inserts multiple redundant rows into the `snippet_tags` association table (`backend/app/models/snippet.py` line 16 — no unique/composite-key constraint on `(snippet_id, tag_id)` to catch this). There's also no cap, so a verbose LLM response can attach an unbounded number of tags to one snippet. Call `tag_normalizer.normalize_tags(ai_tags)` before the tag-linking loop in `SnippetService.create_snippet`. {cat:backend; size:S; multifile:no; research:none}

## Newly discovered (2026-09-18 refuel round 3 — full roadmap exhaustion)
- [ ] [P1] [decomposed] Fix NotificationBell's response-shape crash — `web/src/api/notifications.ts`'s `fetchNotifications()` (line 11-13) does `const response = await api.get<Notification[]>('/notifications'); return response.data`, treating the response body as a bare array, but the real backend, `backend/app/routers/notifications.py`'s `list_notifications` (line 22, mounted at `prefix="/api/notifications"`), returns `{"notifications": items, "unread_count": unread}` (line 33) — an object, not an array. `web/src/components/NotificationBell.vue` (wired into `App.vue` today, zero test file anywhere for it — `find web/src -iname '*NotificationBell*'` matches only the component itself) does `notifications.value = await fetchNotifications()` (line 23) then computes `unreadCount` via `notifications.value.filter(...)` (line 16), so on every real load `.filter` is called on a plain object and throws, caught by the surrounding try/catch and silently swallowed into "Failed to load notifications" (line 25) — the bell never shows real notifications in production. Fix `fetchNotifications` to return `response.data.notifications` (and optionally also return `unread_count` to avoid recomputing it client-side). {cat:frontend; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Wire up the VS Code extension's dead `loadStoredTokenAsync` so login survives an editor restart — `vscode-extension/src/services/auth-service.ts`'s constructor (line 12) calls `this.loadStoredToken()` (line 17), a private no-op stub (line 92-94, body is just the comment "Token will be loaded asynchronously on demand"); the real implementation, `loadStoredTokenAsync()` (line 96, reads `context.secrets.get('gitlark.token')` and verifies it against `/api/auth/me`), is never called anywhere in the extension (`grep -rn loadStoredTokenAsync vscode-extension/src` matches only its own definition). `vscode-extension/src/extension-core.ts`'s `initialize()` (line 30) calls `this.authService.checkAuthentication()` (line 32) immediately after construction, which only checks `if (this.token)` (auth-service.ts line 21) — since `this.token` was never populated from secret storage, this is always false on a fresh activation, so a user who logged in during a previous VS Code session is silently treated as logged out every time the editor restarts, even though their token is still sitting in `context.secrets`. Call `await this.authService.loadStoredTokenAsync()` in `GitLarkExtension.initialize()` before `checkAuthentication()`. {cat:vscode-extension; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Add missing vscode-extension unit tests for the two untested tree providers — `vscode-extension/src/views/conversations-tree.ts` (`ConversationsTreeProvider`, 82 lines) and `vscode-extension/src/views/workspaces-tree.ts` (`WorkspacesTreeProvider`, 94 lines) have zero test files (`find vscode-extension/src -name '*.test.ts'` only returns `views/plans-tree.test.ts` and `views/snippets-tree.test.ts` — the two sibling tree providers registered in `extension-core.ts`'s `registerTreeViews()` right alongside these two, lines 173-204, both have tests; these two do not). Add `conversations-tree.test.ts` and `workspaces-tree.test.ts` following the exact pattern already established in `plans-tree.test.ts` (mock `GitLarkApiClient`, assert `getChildren()`/`getTreeItem()`/`refresh()` behavior, including the "no conversations"/"Create a workspace" empty-state branches at conversations-tree.ts line 39-46 and workspaces-tree.ts line 36-45). {cat:vscode-extension; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Wire `TemporalNavigationService.create_audit_event` into real message actions — the entire audit/replay REST surface in `backend/app/routers/audit.py` (6 endpoints: `/history`, `/audit-trail`, `/user-activity/{user_id}`, `/statistics`, `/audit-report`, `/replay-timeline`) is fully implemented and mounted, but `TemporalNavigationService.create_audit_event` (`backend/app/models/audit.py` line 125) has zero callers anywhere in `backend/app` outside its own definition and its own test file (`grep -rn create_audit_event backend/app --include='*.py'` confirms this) — no router, and critically none of `backend/app/services/conversation.py`'s `add_message` (line 333), `edit_message` (line 556), or `delete_message` (line 587) ever create an `AuditEvent` row. This means for any real conversation, `/audit-trail` and `/statistics` will always return `count: 0` / empty stats — a fully-built, already-tested compliance/audit feature that silently never has any data to show because the one write path that should populate it was never connected. Call `TemporalNavigationService(db).create_audit_event(...)` from `add_message`/`edit_message`/`delete_message` with the appropriate `event_type` (`message_created`/`message_edited`/`message_deleted`) so the audit trail actually reflects real usage. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P2] [decomposed] Route the vscode-extension's Agent Library and Conversations page through the shared API client instead of raw `fetch('/api/...')` — three places bypass axios/`VITE_API_URL` entirely with hardcoded-relative `fetch()` calls that only work when the frontend and backend share an origin (not true in this org's actual Vercel-frontend/Railway-backend topology): `web/src/components/AgentLibrary.vue`'s `fetchTemplates()` (line 82, `fetch('/api/agents/marketplace/templates')`) and `fetchChains()` (line 98, `fetch('/api/agents/marketplace/chains')`) send no Authorization header at all; `web/src/pages/ConversationsPage.vue`'s `loadConversations()` (line 39, `fetch('/api/conversations')`) and its archive action (line 60, `fetch('/api/conversations/${threadId}/archive', ...)`) likewise send no auth header; `web/src/pages/AgentChatPage.vue`'s `loadAgents()` (line 45) and `sendMessage()` (line 94) hand-roll `localStorage.getItem('token')` (lines 44/92) instead of using the shared axios instance. Every other page in the app goes through `web/src/services/api.ts`'s `api` instance (baseURL = `VITE_API_URL || '/api'`, auth header + 401-redirect via interceptors, lines 3-15) or `useApi()` — these five call sites are the only ones in `web/src` using raw `fetch` against `/api/...` (`grep -rn "fetch('/api" web/src` confirms exactly these 3 files). Replace all five with `api.get`/`api.post` from `@/services/api`. {cat:frontend; size:M; multifile:yes; research:none}
- [ ] [P3] [decomposed] Wire `ArchitectureAnalysisAgent` into a real endpoint — `backend/app/services/architecture_analysis_agent.py`'s `ArchitectureAnalysisAgent` (line 64, a complete 238-line service: `analyze_architecture` at line 70, prompt building, response parsing into `ArchitectureAnalysisResult`/`ArchitectureIssue`/`ArchitectureRecommendation` Pydantic models) has zero callers anywhere in `backend/app` outside its own file and its own parsing-only test (`test_architecture_agent_parse.py`) — `grep -rn 'ArchitectureAnalysisAgent\|analyze_architecture' backend/app/routers/*.py` returns nothing. Meanwhile the VS Code extension's `CodeReviewCommand` already advertises an "Architect" review type (`vscode-extension/src/commands/code-review.ts` line 9: `{ label: 'Architect', description: 'Architecture and design review', value: 'architect' }`) that today just gets forwarded as a generic string into the general-purpose code-review prompt (`code_review_agent.py`), never reaching this dedicated agent. Add a `POST /api/conversations/{conversation_id}/architecture-review` route (or extend `backend/app/routers/code_review.py`) that calls `ArchitectureAnalysisAgent.analyze_architecture`, and have the VS Code "Architect" option call it instead of falling through to the generic reviewer. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P3] [decomposed] Surface a real error message when adding repositories to a workspace fails — `web/src/pages/SelectRepositoriesPage.vue`'s `handleAddRepositories()` (line 165) wraps `workspaceStore.addRepositoriesToWorkspace(...)` in a try/catch whose catch block only does `console.error('Failed to add repositories:', error)` (line 179) — no user-facing error state is set, no toast/banner shown, unlike the page's own read path which already has a dedicated `workspaceStore.error` banner (lines 39-41) for the load-repos case. On a real failure (409 repo-already-added, network error, 401), the "Add Selected Repositories" button just stops spinning with zero feedback and the user is left not knowing whether it worked. Set a local `submitError` ref in the catch block and render it with the same error-banner pattern already used earlier in the template (lines 39-41). {cat:frontend; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] Dead-code note: `ConnectorLockManager`/`ConnectorRegistryService.dispatch()` implement per-resource locking but are wired to nothing — `backend/app/services/connector_registry.py`'s `ConnectorLockManager` (line 194, `acquire`/`release`/`is_locked`) and `ConnectorRegistryService.dispatch(resource_id, task_id, func)` (line 173, which acquires `_lock_manager` around `func()`) look like they close the still-open "per-repo connector run lock" gap (this roadmap's own P2 item from 2026-09-11: "nothing serializes two connector runs against the same target repo/branch"), but `grep -rn '_lock_manager\|ConnectorLockManager' backend/app --include='*.py'` shows they're only referenced inside their own defining file — the real dispatch path, `DispatcherService.dispatch()` (`backend/app/services/dispatcher.py` line 78, called from `backend/app/routers/plans.py` line 295 and `backend/app/routers/fleet_status.py` line 111), never touches `ConnectorRegistryService` or `_lock_manager` at all. The underlying race this was meant to close is therefore still 100% open in the actual dispatch path — worse, the existence of this unused class could mislead someone into thinking it's already fixed. Either call `ConnectorRegistryService(...).dispatch(resource_id, task_id, ...)` from inside `DispatcherService.dispatch()` around the per-task queue-file write, or delete `ConnectorLockManager`/`ConnectorRegistryService.dispatch` and re-open the original 2026-09-11 item cleanly. {cat:backend; size:M; multifile:yes; research:none}

## Newly discovered (2026-09-20 refuel — roadmap fully [decomposed], 0 backlog items left; Claude research pass)
- [ ] [P2] [decomposed] Finish P5: wire the already-built conversational control-plane agent into the real chat path + a minimal ops UI — `backend/app/services/control_plane_agent.py`'s `ControlPlaneAgent` (Claude native tool-use over `enqueue`/`dispatch`/`status`/`check_deploys`/`look_at_failure`/`promote`/`pause_fleet`, with a real confirm-token gate on the destructive pair) and the REST surface `backend/app/routers/control_plane.py` (mounted at `/api/workspaces/{id}/control-plane/*` in `main.py`) both exist, are tested, and the `promote`/`pause_fleet` triggers already have a real consumer on the fleet side (`gitlark_control_consumer.sh`, deployed 2026-09-19, reads `.gitlark/fleet-control.jsonl` commits and calls `promote_to_prod.sh --yes` / touches `state/PAUSED`) — this is further along than the 2026-09-18 portfolio audit assumed. But `ControlPlaneAgent.decide()`/`execute_tool_call()` have zero callers anywhere in `backend/app` outside their own tests (verified via grep) — the real chat message-send path, `backend/app/services/conversation.py`'s `add_message`, never invokes them, so a user cannot actually type "pause the fleet" or "what's the queue status" in gitlark's chat UI and have it do anything; and `web/src` has zero references to `control-plane` or `controlPlane` at all — no button, panel, or store calls the REST surface either. This is the last mile of this roadmap's own P2 "Conversational control plane" item (already `[decomposed]` 2026-09-07) that the original decomposition apparently never reached. Scope: (1) add an "ops mode" branch in `add_message` (or a sibling endpoint) that routes to `ControlPlaneAgent.decide()` when the workspace has an active `ProjectConfig` and renders `execute_tool_call()`'s result back into the conversation as a structured message; (2) a minimal `web/src/components/ControlPlanePanel.vue` (queue depth, last deploy check, a "pause fleet"/"promote" button pair that surfaces the confirm-token round-trip) backed by a new `web/src/stores/controlPlane.ts` hitting the existing REST routes, mounted on the workspace dashboard. {cat: backend+web; size: M; multifile: yes; research: repo}

## Newly discovered (2026-09-21 refuel — natural follow-on to the P5 control-plane last-mile wiring, 0 [ready] items left)
- [ ] [P2] [decomposed] Build the still-unconsumed workspace Notifications Feed panel — now that the control-plane chat integration (this roadmap's own most-recently-landed item) is set to surface fleet activity conversationally, the parallel *structured* feed it should also draw on already has a complete, tested backend with zero UI consumer: `backend/app/routers/notifications_feed.py`'s `GET /api/workspaces/{workspace_id}/notifications-feed` (`list_notification_events`), `.../unread-count` (`get_unread_count`), and `PATCH .../{event_id}/read` (`mark_read`) are all real, DB-backed, and covered by `backend/tests/test_notifications_feed_router.py` — but `grep -rln 'notifications-feed|notificationsFeed|NotificationEvent' web/src` returns zero hits: no store, no component, no route anywhere in `web/src` calls any of the three endpoints. This is a DIFFERENT feature from the already-fixed, per-user `NotificationBell.vue`/`web/src/api/notifications.ts` (that one hits `/api/notifications`, the comments/mentions feature) — confirmed by reading both routers' docstrings, which explicitly note the naming split (`notifications_feed.py`'s own header: "Named notifications_feed (not notifications) - app/routers/notifications.py already owns /api/notifications for the pre-existing, distinct per-user notification feature"). The receiving/ingest side (`backend/app/routers/notifications_ingest.py`, a shared-secret-authenticated endpoint for headless callers like the fleet) is also already built and tested, but its own docstring explicitly flags that wiring up the EMITTING side (teaching shrike-ai-lab's `cycle_notify.sh` to POST here) is a deliberately separate, higher-risk change to a different repo's live production automation and should not happen silently in the same pass — leave that cross-repo half alone; this item is scoped to gitlark's own code only. Scope: add `web/src/stores/notificationsFeed.ts` (list/unread-count/mark-read against the three existing endpoints) and a `NotificationsFeedPanel.vue` (or extend the existing per-user `NotificationBell.vue` dropdown with a second "Fleet activity" tab) surfaced on the workspace dashboard, so there's a real consumer ready the moment a later, separate change starts actually posting events into it. {cat: frontend; size: M; multifile: yes; research: none}

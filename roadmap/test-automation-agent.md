# Roadmap — SpecPilot / test-automation-agent
**Maturity:** MEDIUM (backend + frontend exist; Fly deploy). **Stopping point:** a solid test-run + flake-management product; don't over-expand into a full observability suite.

## P1 — core test-run product (next ~2 quarters)
- [ ] [P1] [decomposed] Test-run summaries, status, retry/rerun decisions, flake ratios (pure logic + endpoints) {cat: backend; size: M; multifile: yes; research: repo}
- [ ] [P1] [decomposed] Run dashboard UI: status colors, durations, pass/flake surfaces {cat: web; size: M; multifile: yes; research: repo}
- [x] [P1] [resolved 2026-09-09] Fix the Fly deploy (specpilot routes) — VERIFIED live: https://specpilot.fly.dev/health -> 200 and / -> 200 with a real API response ({"name":"Test Automation Agent API",...}). This was already fixed (see shared/CLAUDE.md's 2026-09-01 full re-verification) but the roadmap never got updated, so it kept showing as the #1 blocker with nothing left to actually research. {cat: infra; size: S; research: web}
## P2 — integrations (~q3-4)
- [ ] [P2] [decomposed] CI integrations (GitHub Actions/Jenkins) result ingestion — researched 2026-09-09: GitHub needs a GitHub App (checks:write scope — plain OAuth apps/PATs can't create check runs) that POSTs to /repos/{owner}/{repo}/check-runs on push/PR and updates it to completed+conclusion when the SpecPilot run finishes (docs.github.com "Building CI checks with a GitHub App"). Jenkins side: the Outbound Webhook plugin (NOT Generic Webhook Trigger, which triggers INTO Jenkins, the wrong direction) sends build-event payloads OUT to an external endpoint. Decomposes into: (1) a generic `POST /api/ci/webhook` ingestion endpoint accepting {provider, repo, commit_sha, status, run_id}, (2) a GitHub-specific outbound path using the Checks API to report SpecPilot's own run status back onto the PR, (3) a docs page with copy-paste GitHub Actions YAML + Jenkins plugin config. {cat: backend; size: M; research: web; sources: docs.github.com/en/free-pro-team@latest/developers/apps/creating-ci-tests-with-the-checks-api, plugins.jenkins.io/outbound-webhook}
- [ ] [P2] [decomposed] Knowledge-base / test-selection helpers {cat: backend; size: M; multifile: no; research: repo}
- [ ] [P2] [decomposed] Flake-state Slack/webhook alerts — When a test's rolling flake ratio crosses a configured threshold (or a previously-stable test starts failing intermittently), push a message to a Slack/Discord/generic webhook URL configured per-project, and post a follow-up when it stabilizes. This reuses the flake-ratio computation and CI ingestion events already planned (GitHub Checks/Jenkins webhook) as the trigger source — no new data source, just a notification sink off existing state transitions. Stays test-run scoped because it fires on flake-ratio state changes only, not generic error/log alerting; it's the single highest-leverage feature for getting a first team to check the tool daily instead of only visiting the dashboard. {cat: backend+web; size: S; multifile: yes; research: none — completed 2026-09-10, sources: Trunk Flaky Tests Slack integration (trunk.io/flaky-tests), BuildPulse per-repo flake notifications (docs.buildpulse.io)}
- [ ] [P2] [decomposed] "Fix-first" ranking view — Add a dashboard view/API endpoint that ranks tests by (flake-rate × run-frequency) so a team can see which handful of flaky tests are costing the most CI time/trust, rather than an undifferentiated flaky-tests list. Pure derived-data feature computed entirely from run records and flake ratios already ingested via the CI integrations; no new collection surface. Stays scoped to test-run triage (a prioritized to-do list for existing flake data) rather than sliding into general analytics/observability. {cat: backend+web; size: S; multifile: yes; research: none — completed 2026-09-10, sources: CircleCI Test Insights "most failed/flakiest tests" ranking (circleci.com/docs/guides/insights/insights-tests), Buildkite Test Analytics slowest/least-reliable dashboard}
- [ ] [P2] [decomposed] Per-test flake-rate trend chart — On each test's detail page, render a time-series/sparkline of its flake ratio and pass/fail history over recent runs (last N days or last N runs), so a team can see whether a test is getting better, worse, or newly-flaky at a glance instead of only a point-in-time ratio. Requires storing/aggregating flake ratio per run-window rather than just a rolling scalar, but the underlying run records already come from the planned CI ingestion pipeline — this is a read-model/charting addition, not a new data source. Stays in scope as a visualization of test-run pass/fail history, not logs or infra metrics. {cat: backend+web; size: M; multifile: yes; research: none — completed 2026-09-10, sources: Buildkite Test Analytics historical/graph trend views (buildkite.com/resources/blog/fixing-flaky-tests), Datadog Flaky Test Management history-across-runs (docs.datadoghq.com/tests/flaky_management)}
- [ ] [P3] [decomposed] PR-comment run summary — Post a single upserted PR comment (via the same GitHub App/token used for the planned Checks API integration) summarizing the run: pass/fail counts, which tests are newly flaky, and a link to the run's dashboard page — distinct from the Checks API status badge, since a comment is scannable in the PR conversation without opening a details link. Reuses the existing GitHub integration's auth and webhook events; only adds a comment-upsert call keyed on PR number. Stays scoped to summarizing this repo's test-run results, not general CI/build commentary. {cat: backend; size: S; multifile: no; research: none — completed 2026-09-10, sources: BuildPulse PR comments/blocking on new flakes (buildpulse.io/products/flaky-tests), general flaky-test PR-comment pattern across CircleCI/Buildkite integrations}
## P2 — flake/CI wiring gaps found in code audit (2026-09-11: several past decompositions landed only pure-logic utils that were never wired into a real code path — verified via grep, zero callers outside their own tests)
- [ ] [P2] [decomposed] Rerun-failed-only endpoint — Add `POST /api/test-runs/{run_id}/rerun-failed` (backend/app/routers/test_runs.py) that creates a new run scoped to only the failed test names from a prior run instead of requiring a full new YAML test plan. Wires up backend/app/utils/rerun_budget.py (can_rerun/remaining) and backend/app/utils/retry_decision.py (should_retry) — both fully unit-tested pure helpers with zero callers anywhere in backend/app/services or routers today — to enforce a per-run rerun budget in backend/app/services/execution_engine.py. {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Auto-release stale quarantines — backend/app/services/flake_detection.py's record_result() only ever sets `quarantined = True` (the code comment literally says "sticky: never auto-unquarantines"), so a test that stabilizes stays on the fix-first list forever. Extend backend/app/utils/quarantine_policy.py with a `should_release(consecutive_passes, release_after)` pure function, add a `flake_quarantine_release_after` setting, and call it from record_result() to auto-clear `quarantined` after N consecutive passes since quarantine. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] Wire PR-comment upsert into the GitHub API — backend/app/utils/pr_comment_body.py (build_pr_comment_body) and backend/app/utils/pr_comment_marker.py (get_pr_comment_marker) are tested pure helpers that are never imported outside their own test files — no code path actually posts a PR comment today. Add `post_or_update_pr_comment()` to backend/app/services/github_ci.py that GETs `/repos/{repo}/issues/{pr_number}/comments`, finds an existing comment containing the marker, and PATCHes it (or POSTs new) with build_pr_comment_body() output; call it from backend/app/services/execution_engine.py next to the existing update_check_run() call sites (~line 398-454) for CI-linked runs. {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P3] [decomposed] Visual-regression diff flagging — backend/app/utils/visual_diff.py (diff_ratio, is_visual_regression) has zero callers, but test runs already capture per-step screenshots to artifact storage (backend/app/services/execution_engine.py ~line 109-115 via artifact_storage.save_base64). Add a comparison step that, when a test has a screenshot from both the current run and the immediately-preceding run of the same test_plan_name, computes a pixel-diff ratio (Pillow) and stores an `is_visual_regression` flag on the TestResult; surface a "visual regression" badge on frontend/src/pages/TestRunDetailPage.vue via frontend/src/stores/testRuns.js. {cat: backend+web; size: M; multifile: yes; research: none}
- [ ] [P3] [decomposed] Remove dead fix-first stub router — backend/app/routers/fix_first.py is a leftover stub (`APIRouter()` with no prefix, no auth) defining `GET /fix-first` that returns a hardcoded `{"status": "success"}`; it's dead scaffolding from an earlier decomposition pass since the real, fully-implemented fix-first ranking feature already lives at `GET /api/flakes/fix-first` in backend/app/routers/flakes.py. Delete backend/app/routers/fix_first.py and its import/include_router call in backend/app/main.py (lines 12 and 71) so there's no unauthenticated fake endpoint left in the API surface. {cat: backend; size: S; multifile: no; research: none}

## P2 — flake/CI wiring gaps found in code audit round 2 (2026-09-13: re-ran the zero-caller grep audit across backend/app/utils; the 2026-09-11 batch is now wired except visual_diff.py (already queued below as "Visual-regression diff flagging"). Found a much larger set of still-orphaned pure helpers than expected — picking the ones directly on the test-run/flake-management stopping point, not the full list.)
- [ ] [P2] [decomposed] Surface overall suite health badge on Flake Dashboard — backend/app/utils/suite_health.py's `health(pass_rate, flake_rate)` is a fully-tested pure classifier (returns healthy/flaky/degraded) with zero callers anywhere in backend/app (verified via grep). GET /api/flakes/analysis (backend/app/routers/flakes.py) already computes `overall_pass_rate` and `avg_flake_score` for the customer but doesn't classify them. Add an `overall_health` field to that endpoint's response using `health()`, fetch it in frontend/src/stores/flakes.js, and render it on frontend/src/pages/FlakeDashboardPage.vue's header using the also-orphaned frontend/src/utils/suiteBadge.ts (zero callers, already has healthy/flaky/degraded label+color mapping) — today the dashboard shows a per-test table with no aggregate health signal at all. {cat: backend+web; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Wire tiered flake-severity badges into the Flake Dashboard — backend/app/utils/flake_indicator.py's `get_flake_badge(flake_score)` (5-tier Stable/Low/Medium/High/Critical label+color, fully unit-tested) has zero callers anywhere in backend/app. routers/flakes.py's `_serialize()` (~line 19) currently only returns the raw `flake_score` float. Add a `badge` field ({label, color}) to `_serialize()` via `get_flake_badge()`, and update frontend/src/pages/FlakeDashboardPage.vue to render `flake.badge.label`/`flake.badge.color` in place of its own hardcoded 3-tier `severityClass()` function (~line 95). {cat: backend+web; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] Consolidate duplicate status-color helpers and wire flaky coloring into the run list — three fully-tested, near-duplicate status-to-color implementations exist with zero callers outside their own tests: backend/app/utils/status_color.py, frontend/src/utils/statusColor.ts, and frontend/src/utils/runStatusColor.ts (the only one of the three that also handles a `'flaky'` status). frontend/src/pages/TestRunsPage.vue's `getStatusBadge()` (~line 100) and TestRunDetailPage.vue instead hardcode their own Tailwind class maps that don't have a 'flaky' case at all. Wire frontend/src/utils/runStatusColor.ts into both pages' status badges (apply its hex color as an inline `background-color` style alongside the existing Tailwind text classes), then delete the now-fully-redundant frontend/src/utils/statusColor.ts and backend/app/utils/status_color.py plus their dedicated test files. {cat: web; size: S; multifile: yes; research: none}
- [ ] [P3] [decomposed] Remove dead duplicate select_by_tag helper — backend/app/utils/test_selection.py's `select_by_tag(tests, tag)` is a fully-tested pure function with zero callers anywhere in backend/app; it's a strict subset of the already-wired `select_tests(tests, include_tags=[tag])` (used in routers/test_plans.py and services/execution_engine.py). Delete `select_by_tag` and its dedicated test to remove the confusing duplicate surface, following the same precedent as the earlier dead `fix_first` router removal. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P2] [decomposed] Add unit tests for the flakes Pinia store — frontend/src/stores/flakes.js (fetchFlakes, setQuarantine, loading/error state) has zero test coverage today; frontend/src/stores/__tests__/testRuns.test.js is the only existing store test in the repo and establishes the mocked-axios + Pinia setup pattern to follow. Add frontend/src/stores/__tests__/flakes.test.js covering: a successful fetch populates `flakes`, a fetch error sets `error` and leaves data empty, and `setQuarantine` both updates the matching entry in place on success and surfaces the API error message on failure. {cat: test; size: S; multifile: no; research: none}

## P2/P3 — round 3 audit (2026-09-14: fleet ran dry — 0 ready, 0 backlog. Re-ran the zero-caller grep audit across backend/app/utils and backend/app/agents; found two live wiring gaps plus four dead/duplicate/inconsistent helpers not covered by the round-1/round-2 batches above.)
- [ ] [P2] [decomposed] Wire flake-trend direction into the per-test trend endpoint — `GET /api/flakes/{test_name}/trend` (backend/app/routers/flakes.py, function `get_test_trend`) returns the raw pass/fail `history` list (list[bool], see backend/app/models/test_run.py's `history = Column(JSON, ...)` ~line 158) but no trend direction. backend/app/utils/flake_trend.py's `trend_direction(scores: list[float])` has zero callers anywhere in backend/app (verified via grep). backend/app/utils/flake_score.py's `compute_flake_score(history: list[bool]) -> float` is already imported and used in backend/app/services/flake_detection.py. In `get_test_trend`, split `stat.history` into earlier/recent halves, run `compute_flake_score()` on each half to get a 2-value `[earlier_score, recent_score]` list, pass it to `trend_direction()`, and add a `"trend"` field ("improving"/"worsening"/"stable") to the response dict. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Wire selector_scoring.score_selector into the optimizer's ranking — backend/app/utils/selector_scoring.py's `score_selector(success_rate, robustness)` (fully unit-tested composite scorer that adds a robustness bonus on top of success_rate) has zero callers anywhere in backend/app (verified via grep). backend/app/agents/optimizer.py's `SelectorOptimizer.find_element()` (~line 90) currently sets `"confidence": success_rate` directly, using `robustness` only as a separate sort tie-breaker via the hardcoded `_ROBUSTNESS_RANK` dict — so two selectors with identical success_rate but different robustness get the same confidence score. Replace that line with `"confidence": score_selector(success_rate, strategy.get("robustness", "unknown"))` so stronger (id/data-testid) selectors actually score higher, not just tie-break. Existing tests backend/tests/test_optimizer_find_element.py and test_optimizer_dedup.py assert membership/counts, not exact confidence values, so this is a safe swap. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Remove dead duplicate rbac.py — backend/app/utils/rbac.py's `has_permission`/`role_rank` (fully tested, zero callers anywhere in backend/app per grep) duplicate the real, wired-in permission check: backend/app/models/tenancy.py's `role_at_least()` (used at backend/app/routers/orgs.py line 99), backed by `ROLE_RANK = {"member": 0, "admin": 1, "owner": 2}`. rbac.py invents an incompatible 4-tier `_ROLE_RANKS = {"owner": 4, "admin": 3, "member": 2, "viewer": 1}` — a "viewer" role that doesn't exist anywhere else in the codebase (models/tenancy.py's `VALID_ROLES` is only member/admin/owner). Delete backend/app/utils/rbac.py and backend/tests/test_rbac.py to remove the confusing, inconsistent dead duplicate. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Remove dead org_scoping.py — backend/app/utils/org_scoping.py's `can_access`/`require_owner` (fully tested, zero callers anywhere in backend/app per grep) implement a plain `owner_id == requester_id` ownership check that was never adopted. The actual authorization pattern used throughout the codebase is customer_id-scoped, e.g. backend/app/routers/webhooks.py's `_owned_endpoint()` (~line 82) does `if endpoint.customer_id != customer_id: raise HTTPException(403, ...)` directly — a different shape than org_scoping's two-arbitrary-string-IDs signature. Delete backend/app/utils/org_scoping.py and backend/tests/test_org_scoping.py. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Delete duplicate get_relevant_tests method in knowledge_base.py — backend/app/services/knowledge_base.py defines `async def get_relevant_tests(self, feature_name)` TWICE with byte-identical bodies (verified via grep+read: definitions at line 50 and line 244, both calling `query_knowledge_base(feature_name, top_k=10)` and returning the same test_ids list), so the second silently shadows the first at class-definition time — dead, confusing duplication in a class where every other method is defined once. Delete the second (~line 244) definition. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Remove dead duplicate duration_format.py — backend/app/utils/duration_format.py's `format_duration(ms)` (fully tested, zero callers anywhere in backend/app per grep) duplicates backend/app/utils/duration.py's `format_duration_ms(ms)`, which is the real, wired-in formatter (imported and called at backend/app/routers/test_runs.py line 18 and ~320 to populate the `duration_human` field). The two disagree on edge cases — duration_format.py raises `ValueError` on negative input, duration.py clamps to `"0ms"` — so keeping both risks a future edit picking the wrong one. Delete backend/app/utils/duration_format.py and backend/tests/test_duration_format.py. {cat:backend; size:S; multifile:no; research:none}

## Stopping point
Test-run tracking + flake detection + CI ingestion + a clean dashboard = COMPLETE. Maintain.

## P2/P3 — round 4 audit (2026-09-14: fleet ran dry again — 0 ready, 0 backlog. Re-ran zero-caller grep across the full backend/app/utils tree with a stricter non-test-dir grep; the round-1/2/3 batches undercounted because several test files use a test_utils_X.py naming pattern that a loose grep skipped. Found one real endpoint bug, one inline-duplicate-vs-tested-util gap, and three duplicate-implementation clusters — including a genuine three-way secret-masking divergence.)
- [ ] [P2] [decomposed] Fix test-plan selection endpoint that always returns empty — `POST /api/test-plans/{plan_id}/select` (backend/app/routers/test_plans.py line 75, `select_tests` handler) calls `tps.get_relevant_tests(plan_id, body.query)` which is backend/app/services/test_plan_selection.py's `get_relevant_tests(plan_id, query)` — a stub that unconditionally `return []` (comment: "plan storage is not yet implemented"). Meanwhile backend/app/services/knowledge_base.py's `KnowledgeBaseService.get_relevant_tests(feature_name)` (line 85) already does real work via `query_knowledge_base()` (backend/app/utils/kb_query.py) and is instantiated elsewhere as `KnowledgeBaseService(db, context.customer_id)` (backend/app/services/execution_engine.py line 420). Change test_plan_selection.get_relevant_tests to accept db+customer_id, delegate to KnowledgeBaseService.get_relevant_tests(query), and update the router call site (add `db: AsyncSession = Depends(get_db)` like other routers, e.g. backend/app/routers/flakes.py) so the endpoint returns real matches instead of always `[]`. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P2] [decomposed] Wire step_histogram helpers into orchestrator's difficulty heuristic — backend/app/agents/orchestrator.py's `_classify_difficulty_heuristic` (line 53) hand-rolls `action_counts = Counter(actions)` (line 60) and `has_wait = any(a.startswith("wait_") for a in actions)` (line 63) inline, duplicating the already-written, fully-tested backend/app/utils/step_histogram.py functions `action_counts(steps)` and `has_wait(steps)` (verified zero callers anywhere in backend/app via grep — only its own test file references them). Replace the two inline lines with `from app.utils.step_histogram import action_counts as _action_counts, has_wait as _has_wait` and call them directly on `steps` (they already do their own `.get("action")`/`.lower()` handling), removing the duplicate logic and giving step_histogram.py real production callers. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Remove dead duplicate webhook event-matcher — backend/app/utils/event_pattern.py's `event_matches(event, subscriptions)` has zero callers anywhere in backend/app (verified via grep; only backend/tests/test_event_pattern.py references it). The real, wired implementation is backend/app/services/notification_dispatcher.py's own `event_matches(event, subscribed)` (defined line 32, called line 146 to filter WebhookEndpoint subscriptions before delivery) — which is a superset (also supports the bare `"*"` catch-all that event_pattern.py's version doesn't handle). Delete backend/app/utils/event_pattern.py and backend/tests/test_event_pattern.py to remove the confusing, weaker duplicate. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P2] [decomposed] Remove dead duplicate result-summarization helpers — backend/app/utils/allure_summary.py's `summarize(results)` (passed/failed/broken/skipped counts) and backend/app/utils/run_summary.py's `summarize()`/`all_green()`/`build_run_summary()` all have zero callers anywhere in backend/app (verified via grep; only their own test files — backend/tests/test_allure_summary.py, backend/tests/test_run_summary.py — reference them). Both duplicate functionality already wired in production: backend/app/utils/results_summary.py's `summarize_results()` (used at backend/app/routers/public_api.py line 75) and backend/app/utils/dashboard_metrics.py's `calculate_dashboard_summary()` (used at backend/app/routers/test_runs.py line 211). Delete allure_summary.py, run_summary.py, and their two test files. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Remove three-way duplicate/conflicting secret-masking helpers — backend/app/utils/mask.py's `mask_secret(value, keep=4)`, backend/app/utils/mask_secret.py's `mask_secret(value, visible=4)`, and backend/app/utils/redact.py's `redact_secret(value, keep=4)` (line 44) are three independently-written implementations of "mask a secret string" with zero callers anywhere in backend/app (verified via grep — each is referenced only by its own test: test_mask.py, test_mask_secret.py/test_utils_mask_secret.py, test_utils_redact.py). They actively disagree on behavior: mask.py's version returns a short value UNMASKED when `len(value) <= keep`, while redact.py's `redact_secret` FULLY MASKS a value of the same length in the same situation — a real correctness/security divergence that risks picking the wrong one later. mask_secret.py's version masks the middle instead of the prefix (different semantic entirely) and its own docstring already cross-references redact.redact_secret, showing the duplication was known and not cleaned up. Delete backend/app/utils/mask.py and backend/app/utils/mask_secret.py entirely (plus test_mask.py, test_mask_secret.py, test_utils_mask_secret.py); remove just the `redact_secret` function (and its dedicated test cases) from backend/app/utils/redact.py and backend/tests/test_utils_redact.py, keeping that file's unrelated `redact_secrets()`/`redact_all()` text-scrubbing functions intact. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Add p50/p95 duration percentiles to the dashboard summary — backend/app/utils/dashboard_metrics.py's `calculate_dashboard_summary()` (wired at backend/app/routers/test_runs.py line 211, endpoint `get_dashboard_summary` line 188) already builds a `durations` list (line 31) but only returns `avg_duration_s`. backend/app/utils/percentile.py's `percentile(values, p)` — docstring literally says "for run-duration widgets" — is a fully-tested pure function with zero callers anywhere in backend/app (verified via grep). Add `p50_duration_s`/`p95_duration_s` fields to calculate_dashboard_summary's return dict using `percentile(durations, 50)`/`percentile(durations, 95)` (guard the empty-runs early-return path with 0.0 defaults), and add the two fields to the `get_dashboard_summary` response dict in test_runs.py. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Consolidate duplicate duration-bucket helpers and add a duration-distribution histogram to the dashboard — backend/app/utils/duration_bucket.py's `bucket(seconds)` (fast <5s / ok <30s / slow <120s / very_slow) and backend/app/utils/elapsed_bucket.py's `elapsed_bucket(seconds)` (<1s / 1-5s / 5-30s / 30s-2m / >2m) are two independent, zero-caller implementations of the same seconds-to-label bucketing concept (verified via grep — each referenced only by its own test file). Delete elapsed_bucket.py and its test (backend/tests/test_elapsed_bucket.py), then wire the survivor into backend/app/utils/dashboard_metrics.py's `calculate_dashboard_summary()`: bucket every value in the existing `durations` list (line 31) with `duration_bucket.bucket()` and add a `duration_distribution` dict (e.g. `{"fast": 3, "ok": 5, "slow": 1, "very_slow": 0}`) to the returned summary, surfaced in the `get_dashboard_summary` response (backend/app/routers/test_runs.py line 188). {cat:backend; size:M; multifile:yes; research:none}

## P2/P3 — round 5 audit (2026-09-17: fleet ran dry again — 0 ready, 0 backlog on this repo. Confirmed the earlier 2026-09-17 fixes (0-byte test debris in backend/tests/, dead root-level tests/+pytest.ini scaffold) are in and did not re-find those two issues. Went beyond the zero-caller-util pattern already mined heavily by rounds 1-4: traced the actual upload/confirm flow end-to-end, checked frontend Pinia store test coverage directly, and found two more zero-caller utils rounds 1-4's audits missed plus one dead orphaned Vue file.)
- [ ] [P2] [decomposed] Fix the non-functional upload confirm/status/list/delete flow — `POST /api/uploads/test-plan/{upload_id}/confirm` (backend/app/routers/uploads.py, `confirm_upload`) unconditionally raises `404 "Upload not found"` for every upload_id (comment: "For demo, we don't track uploads, so return 404"); `GET /api/uploads/test-plan/{upload_id}/status` always returns hardcoded placeholder data (`status: "processed"`, `test_count: 5`, `app_url: "https://example.com"`) regardless of the real upload_id passed in; `GET /api/uploads` always returns `[]`; `DELETE /api/uploads/{upload_id}` always reports success without persisting anything. Root cause: no Upload model exists at all (verified: `grep -rn 'class Upload' backend/app/models/` -> no match). The dedicated tests (backend/tests/test_upload_model.py, test_upload_persistence.py) literally assert this placeholder/broken behavior as correct (e.g. `test_confirm_upload_returns_404` asserts the 404), so the gate is green while the sub-API is dead. The dashboard already works around it entirely — frontend/src/pages/UploadPage.vue (~lines 148-183) reads the YAML client-side and calls `store.createTestRun()` directly instead of ever calling confirm — so this is misleading dead surface for anyone building against it later (e.g. a future public API consumer, or the CI-integration work already on this roadmap). Either add a real `Upload` model (customer_id, filename, parsed test_plan, status) and wire confirm/status/list/delete to it, or delete the four endpoints if the upload flow is meant to stay client-side-only. {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Wire kb_index.build_index() into a real endpoint — backend/app/utils/kb_index.py's `build_index(tests)` (aggregates a list of test dicts into total/by_tag/high_risk/failed_recently/avg_duration_s) is a fully-tested pure function with zero callers anywhere in backend/app (verified: `grep -rn 'kb_index\|build_index' backend/app --include='*.py'` matches only the file itself; only backend/tests/test_kb_index.py references it) and no router exposes anything equivalent today. The data it needs already exists: backend/app/models/test_run.py's `TestResult` (test_name, status, duration_ms) and `TestFlakeStat` (test_name, flake_score) map onto build_index's expected input shape (`name`, `last_status`, `duration_s` = duration_ms/1000, `risk_score` = flake_score). Add a `GET /api/flakes/index` endpoint (backend/app/routers/flakes.py, alongside the existing `/api/flakes/analysis`) that joins the two tables per customer_id, builds the list, and returns build_index()'s output so the dashboard can show tag-grouped high-risk/recently-failed tests instead of only a flat per-test list. {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Add unit tests for the webhooks Pinia store — frontend/src/stores/webhooks.js (121 lines: fetchWebhooks, fetchAvailableEvents, createWebhook, updateWebhook, deleteWebhook, testWebhook, fetchDeliveries, retryDelivery) has zero test coverage today (verified: `find frontend/src -iname '*webhooks*test*' -o -iname '*webhooks*spec*'` returns nothing). It's the largest of seven untested Pinia stores in the repo — apiKeys.js, billing.js, ci.js, orgs.js, selfHealing.js, usageMetrics.js, and webhooks.js all have zero coverage; only flakes.js and testRuns.js are tested today (frontend/src/stores/__tests__/). Follow the mocked-axios + Pinia setup pattern already established in frontend/src/stores/__tests__/flakes.test.js: cover a successful fetchWebhooks populating `webhooks`, createWebhook/updateWebhook/deleteWebhook mutating local state on success and setting `error` on API failure, and retryDelivery updating the matching delivery's status in place. {cat: test; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Remove dead flakes_quarantine.py helpers — backend/app/utils/flakes_quarantine.py's `apply_quarantine_update(state, id, status)` and `quarantine_error_message(status_code)` have zero callers anywhere in backend/app (verified: `grep -rn 'flakes_quarantine' backend/app --include='*.py'` matches only the file itself and its own tests, backend/tests/test_flakes_quarantine.py / test_flakes_quarantine_validation.py). The fleet spent three separate commits building this out (8cdc225 "add quarantine_error_message and tests", b4f1370 "add input validation to apply_quarantine_update") without ever wiring it in — the real quarantine flow is `POST /api/flakes/{test_name}/quarantine` (backend/app/routers/flakes.py's `set_quarantine`), which calls the DB-backed `flake_detection.set_quarantine()` directly and has no natural use for a list-of-dicts immutable-update helper. Delete backend/app/utils/flakes_quarantine.py and its two test files. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Consolidate divergent, zero-caller sanitize-filename helpers — backend/app/utils/sanitize_filename.py defines TWO functions with different behavior and neither has any caller anywhere in backend/app (verified: `grep -rn 'sanitize_filename\|sanitize(' backend/app --include='*.py'` matches only the file itself; only the two test files test_sanitize_filename.py / test_utils_sanitize_filename.py reference it — the same duplicate-test-naming pattern flagged in the round-4 audit note). `sanitize(name)` (line 16) strips everything up to the last path separator (`"../etc/passwd" -> "passwd"`), while `sanitize_filename(name)` (line 47) instead replaces `/`/`\` with `_` in place and truncates to 200 chars (`"../etc/passwd" -> ".._etc_passwd"`) — genuinely different outputs for the same input. Neither is used by backend/app/routers/uploads.py, which accepts and echoes back `file.filename` directly today. Pick one behavior, delete the other function plus its now-redundant test file, and wire the survivor into uploads.py's `upload_test_plan` before the filename is echoed back in the response. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P3] [decomposed] Remove dead orphaned frontend/src/views/TestRunDetailPage.vue — this 17-line file (the only file in frontend/src/views/) is a stub component (hardcoded `status = ref('passed')`, no props, no data fetching) that is never imported anywhere: `grep -rn "views/TestRunDetailPage" frontend/src` and `grep -rn "'\.\./views\|/views/" frontend/src --include='*.js' --include='*.vue' --include='*.ts'` both return zero hits. The real, routed component is frontend/src/pages/TestRunDetailPage.vue (212 lines, imported at frontend/src/router/index.js line 5, used at line 67). Delete frontend/src/views/TestRunDetailPage.vue and the now-empty frontend/src/views/ directory. {cat: web; size: S; multifile: no; research: none}

## Newly discovered (2026-09-18 refuel round 2 — backlog dry again)
- [ ] [P2] [decomposed] Wire the Upload model into the upload confirm/status/list/delete endpoints — round-5's finding ("no Upload model exists at all") is now half-fixed: backend/app/models/upload.py's `Upload` SQLAlchemy model (customer_id, filename, parsed_test_plan, status) exists, is registered in backend/app/models/__init__.py, and has a real migration (backend/alembic/versions/0379c2620417_add_uploads_table.py) — but backend/app/routers/uploads.py never imports or uses it (verified: `grep -rn 'from app.models.upload\|import Upload' backend/app --include='*.py'` matches only the __init__.py re-export). `POST /api/uploads/test-plan` (upload_test_plan, ~line 15) still just returns a random `uuid4()` without writing a row; `POST /api/uploads/test-plan/{upload_id}/confirm` (confirm_upload, line 96) still unconditionally `raise HTTPException(404, ...)` (line 104); `GET /api/uploads/test-plan/{upload_id}/status` (get_upload_status, line 108) still returns hardcoded placeholder data (`status: "processed"`, line 117); `GET /api/uploads` (list_uploads, line 124) still always `return []` (line 133); `DELETE /api/uploads/{upload_id}` (delete_upload, line 137) still reports success without touching the DB. Wire upload_test_plan to persist an `Upload` row and have the other four endpoints query/update it by `id` + `customer_id`. {cat: backend; size: M; multifile: no; research: none}
- [ ] [P2] [decomposed] Finish wiring kb_index.build_index()/build_index_from_rows() — round-5's "Wire kb_index.build_index() into a real endpoint" item got auto-credited (a commit added `build_index_from_rows` and `GET /api/flakes/index` now exists at backend/app/routers/flakes.py line 127, calling `flake_detection.get_flake_index_data()`), but `get_flake_index_data` (backend/app/services/flake_detection.py line 180) reimplements its own aggregate math from scratch (total_tests/quarantined_count/avg_flake_score/overall_pass_rate/overall_flake_ratio) and never calls `build_index()` or `build_index_from_rows()` — verified via `grep -rn 'build_index_from_rows\|build_index(' backend/app backend/tests --include='*.py'`: both functions are still referenced only by their own test file, backend/tests/test_kb_index.py. The originally-intended tag-grouped/high-risk/recently-failed-tests view (build_index's actual output shape) was never delivered — the new endpoint returns a different, simpler shape instead. Either replace get_flake_index_data's body with a call to build_index_from_rows() on the real TestResult/TestFlakeStat rows, or add the tag-grouped fields build_index() produces to the endpoint's response. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Fix the dead-code-removal mechanism leaving empty tracked stub files instead of deleting them — backend/app/utils/flakes_quarantine.py and backend/app/utils/rbac.py were both targeted for full deletion by round-1 items #17/#33 (worded "Delete backend/app/utils/X.py and its test file(s)"), and their test files (test_flakes_quarantine.py, test_flakes_quarantine_validation.py, test_rbac.py) are correctly gone — but the two util files themselves are now 1-byte empty files (just a trailing newline, verified via `cat -A` and `ls -la`) still tracked in git (`git ls-files backend/app/utils/flakes_quarantine.py backend/app/utils/rbac.py` returns both; `git log -1 -- <path>` shows commit ebf27de "chore: remove dead code from flakes_quarantine.py"). The aider "remove dead code" pipeline step empties file *contents* instead of `git rm`-ing the file when a DELETE-flavored item targets an entire file, which will keep happening for the 6 other still-queued "delete this whole file" items (allure_summary.py, duration_format.py, elapsed_bucket.py, event_pattern.py, mask.py, sanitize_filename.py) unless the removal step is changed to detect an empty resulting file and `git rm` it. {cat: infra; size: S; multifile: no; research: none}
- [ ] [P2] [decomposed] Remove dead duplicate PR-comment helpers left behind in execution_engine.py — the real, wired PR-comment call sites (backend/app/services/execution_engine.py lines 491, 520, 545, 571) use `should_post_pr_comment()` (line 104, requires `pr_number` + a string `repository`) and `_build_pr_comment_payload()` (line 111), both imported/called correctly alongside `post_or_update_pr_comment` from backend/app/services/github_ci.py. But the same file also defines two unused near-duplicate siblings: `_should_post_pr_comment()` (line 99, a looser "pr_number or pr_url" check) has zero callers anywhere including its own tests (verified: `grep -rn '_should_post_pr_comment' backend --include='*.py'` matches only its own definition), and `build_pr_comment_payload(run_id, status, summary)` (line 135, a totally different 3-key dict shape) is never called in production but IS still unit-tested at backend/tests/test_execution_engine.py:209 (`test_build_pr_comment_payload_public`) — giving false confidence it matters. It also collides in name with the real, different-signature `build_pr_comment_payload(body, existing_comment_id)` in backend/app/services/github_ci.py:220, which is genuinely wired via `post_or_update_pr_comment`. Delete `_should_post_pr_comment` and `execution_engine.build_pr_comment_payload` plus the `test_build_pr_comment_payload_public` test case. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P2] [decomposed] Add unit tests for the Vue Router auth guard — frontend/src/router/index.js's `router.beforeEach` navigation guard (lines 143-153) is the sole authorization boundary for all 13 `requiresAuth: true` routes (redirects unauthenticated users to `/login` with a `redirect` query param, and redirects already-authenticated users away from Login/Signup/Marketing to `/dashboard`) and has zero test coverage anywhere in the repo (verified: `find frontend/src -iname '*router*test*' -o -iname '*router*spec*'` returns no results). This is more security-relevant than the currently-untested Pinia stores since it's the actual gate deciding page access. Add frontend/src/router/__tests__/index.test.js using vue-router's createRouter with a memory history + a mocked/pinia-testing authStore, covering: an unauthenticated user hitting a `requiresAuth` route gets redirected to Login with the right `redirect` query, an authenticated user hitting Login/Signup/Marketing gets redirected to Dashboard, and a normal authenticated navigation to a protected route calls `next()` with no redirect. {cat: test; size: S; multifile: no; research: none}
- [ ] [P2] [decomposed] Add unit tests for the ci Pinia store — frontend/src/stores/ci.js (109 lines: fetchIntegrations, createIntegration, deleteIntegration, fetchSchedules, createSchedule, deleteSchedule, runDueSchedules) has zero test coverage today (verified: `find frontend/src -iname '*ci*test*' -o -iname '*ci*spec*'` under stores/ returns nothing; only flakes.js and testRuns.js have tests in frontend/src/stores/__tests__/). It's the frontend half of the CI-integrations feature already backed by 6+ backend test files (test_ci_router.py, test_ci_schedules_endpoint.py, etc.) but the store driving CiIntegrationsPage.vue has none. Follow the mocked-axios + Pinia pattern in frontend/src/stores/__tests__/flakes.test.js: cover fetchIntegrations/fetchSchedules populating state on success and setting `error` on failure, createIntegration/createSchedule pushing the new item into local state, deleteIntegration/deleteSchedule removing the matching item, and runDueSchedules returning the response without mutating `integrations`/`schedules`. {cat: test; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Wire plan_normalizer.normalize_test_plan() into the upload/test-plan flow — backend/app/utils/plan_normalizer.py's `normalize_test_plan(plan)` (strips `app_url`, defaults each test's `steps`/`assertions` to `[]` if missing/wrong-type, lowercases+trims each step's `action` string, raises `ValueError` on malformed input) is a fully-implemented, fully-tested pure function with zero callers anywhere in backend/app (verified: `grep -rln 'normalize_test_plan' backend --include='*.py'` matches only its own file and backend/tests/test_plan_normalizer.py). backend/app/routers/uploads.py's `upload_test_plan` (the real ingestion point for YAML test plans) does its own manual field-by-field validation today with no action-string normalization, so a step written as `"Action: Click"` or `" click "` in the uploaded YAML is never normalized before being stored/executed. Call `normalize_test_plan()` on the parsed YAML in `upload_test_plan` after the existing manual validation passes, and use the normalized dict (not the raw parsed one) when building the response/handing off to execution. {cat: backend; size: S; multifile: no; research: none}

## Newly discovered (2026-09-18 refuel round 3 — full roadmap exhaustion)
- [ ] [P1] [decomposed] Remove the live-request-path call to process_dead_code_removal() from the orchestrator's YAML parser — backend/app/agents/orchestrator.py line 150 calls `process_dead_code_removal()` (imported at line 10 from backend/app/services/self_healing.py line 117) whenever a customer-uploaded YAML test plan parses to a non-dict (e.g. a plain YAML list) — a common, benign user mistake reachable through the real upload/parse flow (`TestOrchestrator.parse_test_plan`). That function walks the ENTIRE repo tree via `os.walk(repo_root=".")`, and for every `.py` file strips any `import `/`from ` line whenever the module name isn't found as a raw substring anywhere else in the file (a naive check that false-positives constantly — e.g. a module named `os` matches the substring "os" inside dozens of unrelated words like "cross"/"most"/"post"), silently overwriting files in place, then runs `git ls-files` + `git rm --cached` on every zero-byte tracked file repo-wide. Verified live and unmocked: backend/tests/test_orchestrator_parsing.py::test_parse_non_dict_yaml_returns_invalid (line 378, `raw_yaml = "- item1\n- item2\n"`) exercises this exact branch with no monkeypatching of process_dead_code_removal, so every real test run mutates the actual working tree — this exactly matches the ~300-file spurious "unused import" diff observed in a fresh local checkout of this repo before this fix (dead giveaway: `from contextlib import asynccontextmanager` silently stripped from backend/app/main.py). Delete the call site (orchestrator.py lines 10 and 150) so a malformed upload just returns the validation error, and delete `process_dead_code_removal()` from self_healing.py (lines 117-203) entirely since it has no legitimate caller and must never be reachable from a live request path regardless of the original quarantine-file-cleanup intent it was built for. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P1] [decomposed] Fix the SSRF hole in webhook URL validation — backend/app/routers/webhooks.py's `validate_url()` (line 38) explicitly accepts `localhost`, any private/loopback IPv4 address, and even `0.0.0.0` as a valid webhook target. Verified by the repo's own test suite: backend/tests/test_webhooks_validate_url.py asserts `validate_url("http://localhost:8000")` (line 16), `validate_url("http://192.168.1.1")` (line 19), `validate_url("http://10.0.0.1:8080")` (line 22), and `validate_url("http://0.0.0.0")` (line 61) are all `True`. `POST /api/webhooks` (webhooks.py ~line 99-123) uses this as its sole gate before persisting a `WebhookEndpoint.url`, and backend/app/services/notification_dispatcher.py's `_send_request` later makes a real outbound HTTP request to that stored URL on every matching event with no further check — so any authenticated customer can register a webhook pointing at an internal-only host (loopback, RFC1918 ranges, or a cloud metadata endpoint like 169.254.169.254) and have the server issue requests to it on their behalf. Add a private/loopback/link-local host check (Python's `ipaddress` module, resolving the hostname first if it isn't already a literal IP) to `validate_url()` and reject those hosts; update the now-incorrect acceptance assertions in test_webhooks_validate_url.py to expect rejection instead. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] Wire mask_email() into the three log lines that print raw customer email addresses — backend/app/utils/mask_email.py's `mask_email(addr)` (fully tested, e.g. `john.doe@example.com` -> `j***@example.com`) has zero callers anywhere in backend/app (verified: `grep -rn 'mask_email' backend/app --include='*.py'` matches only the file itself and its own test). Three call sites still log the full plaintext address: backend/app/routers/auth.py line 104 (`logger.info(f"New user {user.email} signed up; created org {org.slug}")`), backend/app/services/email_service.py line 80 (`logger.info("SMTP not configured; skipping email to %s for %s", to_address, event)`), and line 93 (`logger.warning("Email send to %s failed: %s", to_address, exc)`). Replace each raw `user.email`/`to_address` interpolation with `mask_email(...)` so customer PII doesn't land in plaintext application logs. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] Add jitter to webhook delivery retry backoff — backend/app/utils/backoff_jitter.py's `jittered(delay_ms, jitter_ratio, rand=random.random)` (fully tested pure helper) has zero callers anywhere in backend/app (verified via grep — only its own test references it). backend/app/services/notification_dispatcher.py's `_attempt_delivery()` (line 97) builds its retry delay schedule via `retry_schedule()` (backend/app/utils/retry_backoff.py) with no jitter, so when many webhook deliveries fail at once (e.g. a customer endpoint has a brief outage), every pending retry for every affected delivery wakes up and re-fires at exactly the same capped-exponential intervals — a thundering-herd pattern against the customer's own endpoint. Wrap each computed delay in `jittered(delay_ms, jitter_ratio=0.2)` before the `await asyncio.sleep(delay_ms / 1000)` call at line 109. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P2] [decomposed] Wire GET /api/test-runs/dashboard/summary into DashboardPage.vue instead of computing wrong stats from a 10-row page — backend/app/routers/test_runs.py's `get_dashboard_summary` endpoint (line 188) already returns real aggregated stats (total_runs, pass_rate, avg/p50/p95 duration_s, total_failed, total_skipped, duration_distribution) computed across ALL of the customer's runs, but nothing in the frontend calls it (verified: `grep -rn 'dashboard/summary\|dashboard_summary' frontend/src` returns no hits). Instead, frontend/src/pages/DashboardPage.vue (lines 25, 35, 45, 55) derives "Total Runs"/"Active Runs"/"Passed"/"Failed" via `testRuns.value.length`/`.filter()`/`.reduce()` over `store.testRuns`, which frontend/src/stores/testRuns.js's `fetchTestRuns()` populates from `GET /api/test-runs` with no `limit` param — silently capped at the backend's default `Query(10, ...)` (backend/app/routers/test_runs.py line 404). For any customer with more than 10 runs, every stat tile on the main Dashboard is simply wrong (computed over only the 10 most recent runs, not the real total). Fetch `/api/test-runs/dashboard/summary` in DashboardPage.vue (or a new store action) and use its real totals for the four stat tiles instead. {cat: web; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Add unit tests for the auth Pinia store — frontend/src/stores/auth.ts (signup, login, switchOrg, logout, isAuthenticated, the axios Bearer-token interceptor) is the sole session/authorization boundary for the entire frontend and has zero test coverage today (verified: `find frontend/src -iname '*auth*test*' -o -iname '*auth*spec*'` under frontend/src/stores returns nothing; only frontend/src/stores/__tests__/flakes.test.js and testRuns.test.js exist as store tests in this repo). It's more security-relevant than the already-queued router-guard/ci-store/webhooks-store test items on this roadmap since it's the code that actually mints and persists the session (isAuthenticated requires both `token` and `user` per the inline comment at lines 36-39, which notes a real prior regression where user was never persisted). Add frontend/src/stores/__tests__/auth.test.ts using the same mocked-axios + Pinia pattern as flakes.test.js: cover a successful login/signup populating user+token+orgId and writing all three localStorage keys, a failed login/signup setting `error` from `err.response.data.detail` and leaving `isAuthenticated` false, `switchOrg` replacing the persisted session on success, and `logout` clearing all three localStorage keys and store fields. {cat: test; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Wire the orphaned pct() util into testRuns.js's duplicate passRate calculation — frontend/src/utils/pct.ts's `pct(part, whole)` (rounds part/whole to a percentage, returns 0 when whole is 0 — fully tested) has zero callers anywhere in frontend/src (verified: `grep -rn "from ['\"].*pct['\"]" frontend/src` matches only its own test file). frontend/src/stores/testRuns.js's `passRate` computed (lines 207-213) reimplements the identical logic inline: `total > 0 ? Math.round((passed / total) * 100) : 0`. Replace the inline expression with `pct(passed, total)` (import from `../utils/pct`) to remove the duplicate, following the same consolidation precedent as the earlier status-color/duration-bucket cleanups on this roadmap. {cat: web; size: S; multifile: no; research: none}

## Refuel 2026-09-19 — roadmap ran dry again (0 [ready], everything [decomposed]; backlog/doable still had runway at refuel time, so this is a get-ahead pass, not a stuck-repo recovery). 4 new gaps verified by direct code read.
- [ ] [P1] [decomposed] `is_safe_url()`'s blocking DNS resolution runs synchronously inside async request/delivery handlers, on the hot path of every webhook send — `backend/app/utils/ssrf_guard.py`'s `is_safe_url()` calls `socket.getaddrinfo()` (line ~25), a blocking syscall with no timeout, directly (no `await`, no `asyncio.to_thread`/executor offload). It is called from two places: `backend/app/routers/webhooks.py`'s `register_webhook` (an `async def` route, line 102: `elif not validate_url(url):` -> `validate_url()` -> `is_safe_url()`), and — worse — `backend/app/services/notification_dispatcher.py`'s `_send_request()` (line 49, `if not is_safe_url(url):`), which is the actual network-send function invoked on **every webhook delivery attempt and every retry** for every customer's every subscribed event (confirmed via `grep -n 'is_safe_url' backend/app/services/notification_dispatcher.py backend/app/routers/webhooks.py`). A slow-to-resolve or non-responding hostname (attacker-controlled or just a flaky DNS server) blocks this app's single-threaded asyncio event loop for the full DNS timeout on every single delivery, stalling every other concurrent request/response in the process — an availability bug hiding inside the code that's supposed to be a security hardening measure. Wrap the `is_safe_url()` call in `await asyncio.to_thread(is_safe_url, url)` at both call sites (or switch to an async-native resolver), and add a regression test that a slow/blocking fake resolver doesn't stall a concurrent request during delivery. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Dead duplicate scheduling-due-check in `utils/schedule.py`, superseded by `cron_match.py` — `backend/app/utils/schedule.py`'s `next_run_after(interval_minutes, from_dt)` and `is_due(scheduled_dt, now)` (both fully tested in `backend/tests/test_schedule.py`) are confirmed via `grep -rn 'is_due(|next_run_after(|utils.schedule' backend --include='*.py'` to have zero callers anywhere outside their own test file. The real, wired scheduling logic uses a completely different module instead: `backend/app/utils/cron_match.py`'s `cron_matches()`, called from `backend/app/routers/ci.py` (lines 301/317) and `backend/app/services/scheduling.py` (line 82, inside `run_due_schedules()` which is itself wired via `backend/app/services/schedule_dispatcher.py` and `POST /ci/schedules/run-due`). `schedule.py`'s interval-based due-check was apparently an earlier, since-abandoned approach to the same "is this scheduled thing due" problem the cron-based system now solves for real. Delete `backend/app/utils/schedule.py` and `backend/tests/test_schedule.py` as superseded dead code. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Dead duplicate `event_matches()` in `utils/event_pattern.py` that also disagrees with the real, wired implementation — `backend/app/utils/event_pattern.py`'s `event_matches(event, subscriptions)` (fully tested in `backend/tests/test_event_pattern.py`, presumed) is confirmed via `grep -rn 'event_matches\|event_pattern' backend/app --include='*.py'` to have zero callers outside its own file. The real webhook-dispatch code defines and uses its OWN separate `event_matches()` (`backend/app/services/notification_dispatcher.py` line 33, called at line 151 inside `_send_request`'s caller) — and the two disagree on behavior: the wired version explicitly supports a bare `"*"` catch-all subscription (`backend/tests/test_notification_dispatcher.py` line 49: `event_matches("test.failed", ["*"]) is True`), while the dead `utils/event_pattern.py` version only recognizes namespace wildcards ending in `".*"` (e.g. `"test.*"`) and has no bare-`"*"` catch-all case at all — the same "two implementations, real behavioral divergence" pattern already flagged elsewhere on this roadmap for `mask`/`redact`/duration-bucket helpers. Delete `backend/app/utils/event_pattern.py` and its test as the dead, less-capable duplicate. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Wire `throughput.tests_per_minute()` into the dashboard summary, following the exact precedent of this roadmap's earlier p50/p95-duration-percentile item — `backend/app/utils/throughput.py`'s `tests_per_minute(count, duration_s)` (fully tested, zero-safe) has zero callers anywhere in `backend/app` outside its own test (`grep -rn 'tests_per_minute' backend --include='*.py'` matches only the definition and `backend/tests/test_utils_throughput.py`). `backend/app/utils/dashboard_metrics.py`'s `calculate_dashboard_summary()` already imports and wires two previously-dead pure-math utils this same way (`percentile` and `duration_bucket.distribution`, per this roadmap's earlier "Add p50/p95..." and "...duration-distribution histogram..." items) and already has both the run count (`total_runs`) and per-run `duration_s` values on hand. Add a `tests_per_minute` field to `calculate_dashboard_summary()`'s return dict (total tests = `total_passed + total_failed + total_skipped`, divided across total duration across runs) and surface it in `get_dashboard_summary`'s response (`backend/app/routers/test_runs.py` line 188), or delete `throughput.py` + its test if a per-run-set throughput figure isn't actually wanted. {cat:backend; size:S; multifile:yes; research:none}

## Newly discovered (2026-09-20 refuel — roadmap fully [decomposed], 0 backlog items left; Claude research pass)
- [ ] [P2] [decomposed] Actually wire visual-regression diffing — the earlier "Visual-regression diff flagging" decomposition (2026-09-11, this file's line 19) landed two of its steps but never the comparison itself: `backend/app/utils/visual_diff.py`'s `diff_ratio`/`is_visual_regression` are fully tested pure functions, `TestResult.is_visual_regression` (backend/app/models/test_run.py line 71) exists on the model (`default=False`, confirmed via `grep` it is never assigned anywhere outside the migration/model default), and `frontend/src/pages/TestRunDetailPage.vue` already renders a "Visual Regression" badge off that field (commit ab3089b) — but nothing in `backend/app/services/execution_engine.py` (which already saves every step's screenshot via `artifact_storage.save_base64`, lines ~193-199) ever loads a screenshot from the immediately-preceding run of the same `test_plan_name`+`test_name`, runs `diff_ratio`/`is_visual_regression` against it, or sets the column — so the badge is permanently `false` in production today. Scope: add a `compare_to_previous_screenshot(db, run_id, test_name, current_screenshot_path)` helper (Pillow, per-pixel byte compare -> `diff_ratio`) called from the same execution_engine code path that already handles screenshot storage, looking up the most recent prior `TestRun` with the same `test_plan_name` for the customer, and set `TestResult.is_visual_regression` from `is_visual_regression(ratio, threshold)` using a configurable threshold (mirror the existing `flake_quarantine_release_after`-style settings pattern). {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Harden test coverage on the three lowest-covered, highest-risk routers — measured directly via `pytest --cov=app --cov-report=term-missing` (2026-09-20, 2451 passed / 86% overall app coverage): `app/routers/test_runs.py` sits at **27%** (161 of 220 statements missing) despite being the product's core surface — `create_test_run`, `get_test_run_report`, `pause_test_run`/`resume_test_run`, and `rerun_failed_tests` are almost entirely untested (missing ranges 51-137, 285-346, 543-624); `app/routers/orgs.py` sits at **40%** (104 of 172 missing) and is the multi-tenant RBAC/invite surface — `_require_role`, `update_member_role`, `remove_member`, and the invite-token `accept_invite` flow (lines 236-331) are untested, the same class of gap (auth boundary with no test coverage) already found and fixed once this portfolio in gitlark's review-collaboration WebSocket; `app/routers/self_healing.py` sits at **41%** (19 of 32 missing) — `get_healing_history`/`get_healing_stats`, the only visibility into what the self-healing selector optimizer has actually changed on a customer's behalf, are untested. Add integration tests (matching the existing `tests/routers/` pattern already used for `flakes.py`) for each: test_runs.py should cover a full create→start→cancel/pause/resume→report lifecycle plus rerun-failed's budget-limit path; orgs.py should cover a non-member/wrong-role request getting rejected at each of `_require_member`/`_require_role`'s call sites plus a valid vs. expired/reused invite token; self_healing.py should cover the empty-history case and a multi-run aggregation in `get_healing_stats`. {cat: test; size: M; multifile: yes; research: none}

## Newly discovered (2026-09-21 refuel — backlog dry again; direct code read against origin/develop @ a2aae91)
**Status check on the two items directly above (no new checkboxes needed):** "Actually wire visual-regression diffing" is still genuinely open — `backend/app/utils/visual_diff.py` has since grown real `load_image()`/`compute_pixel_diff()` Pillow helpers (commits `4141fe9`/`f52a660`), but `grep -rn "compare_to_previous_screenshot\|load_image\|compute_pixel_diff" backend/app/services/execution_engine.py` still returns nothing — the comparison is still never actually invoked from the run pipeline, so the badge is still permanently `false` in production. "Harden test coverage..." is partially landed, not done — a fresh `pytest --cov` run today shows real progress but the three routers are still well below a reasonable bar: `test_runs.py` 27%→**47%**, `orgs.py` 40%→**51%**, `self_healing.py` 41%→**50%**. Neither needs a new roadmap entry; both should keep being pulled from as-is.
- [ ] [P3] [decomposed] Two aider-authored files landed inside `backend/app/utils/` instead of `backend/tests/`, so they silently never run and hide two dead orphaned utilities — `backend/app/utils/test_pct.py` (a `TestPct` class with 9 real assertions against `app.utils.pct.pct`) and `backend/app/utils/test_test_runs_query.py` (4 real `test_*` functions against `app.utils.test_runs_query.buildTestRunsQueryParams`) are genuine pytest test files, but `backend/pytest.ini` scopes collection to `testpaths = tests` (confirmed: `grep -n testpaths backend/pytest.ini`), so pytest never discovers or runs either one — confirmed via `pytest --cov=app --cov-report=term-missing`, which shows both files at **0% covered** as plain (uncollected) app source, not as executed tests. This is a variant of this portfolio's known "misplaced/empty test file gives false confidence" bug class (see the shared memory note on the 2026-09-17 empty-test-file false-red), except inverted here: instead of a red gate, it's a SILENT gap — a green suite that looks like it covers `pct()`/`buildTestRunsQueryParams()` but never actually runs those assertions at all. Consequently their subjects are undetected dead code: `backend/app/utils/pct.py::pct(part, whole)` and `backend/app/utils/test_runs_query.py::buildTestRunsQueryParams(...)` (git history: `cf9cfbf`/`79962bf`/`ab7dcd7`, all "staged step N" aider commits) both have zero callers anywhere in `backend/app` outside their own misplaced test files (confirmed via `grep -rn "buildTestRunsQueryParams\|from app.utils.pct import" backend/app`) — no router or service ever imports either. Note `buildTestRunsQueryParams`'s camelCase name and dict-of-query-params shape look like they were meant for the frontend (`frontend/src/stores/testRuns.js` builds its own query params inline today) rather than a Python backend consumer, which may explain why nothing backend-side ever picked it up. Fix: `git mv backend/app/utils/test_pct.py backend/tests/test_pct.py` and `git mv backend/app/utils/test_test_runs_query.py backend/tests/test_test_runs_query.py` so they actually run, then decide `pct()`/`buildTestRunsQueryParams()`'s fate on their merits — `pct()` duplicates the already-flagged, similarly-dead `frontend/src/utils/pct.ts` orphan (see this roadmap's earlier item) and a backend caller may not be needed; `buildTestRunsQueryParams()` should either get a real backend caller (e.g. `backend/app/routers/test_runs.py`'s list endpoint, which currently builds its own query params inline) or be deleted alongside its now-relocated test. Also worth a broader one-time check while in there: `find backend/app -iname 'test_*.py'` for any further stray files following this exact shape (only these two were confirmed by this pass). {cat: backend; size: S; multifile: yes; research: none}

## Newly discovered (2026-09-22 refuel — backlog dry again, 0 doable in OVERNIGHT_PROGRESS.md; direct code read against origin/develop)
- [ ] [P1] [decomposed] Fix a live PII-masking divergence bug: `email_service.py` imports the wrong `mask_email` — `backend/app/services/email_service.py` line 18 does `from app.utils.mask import mask_email` (used at lines 81/94 to mask `to_address` in two log lines), but `backend/app/routers/auth.py` line 13 correctly imports the canonical `from app.utils.mask_email import mask_email`. The two implementations genuinely disagree: `mask.py`'s `mask_email()` (line 15) returns a **1-character local part completely UNMASKED** (`if len(local) <= 1: masked_local = local`, e.g. `"a@example.com"` stays `"a@example.com"`), while `mask_email.py`'s canonical version fully masks it (`"a@example.com"` -> `"***@example.com"`) — confirmed by reading both files directly. Root cause traced via `git log --follow -- backend/app/utils/mask.py`: `mask.py` was correctly emptied by commit `c3f501a` ("Delete backend/app/utils/mask.py"), but a later "repair staged item" commit (`ee59ba2`) resurrected `mask_secret()` into it, and then the 2026-09-18 roadmap item "Wire mask_email() into the three log lines" (commit `0b37efc`) added a SECOND, divergent `mask_email()` directly into the wrong file instead of importing the already-established canonical one. Fix: change `email_service.py`'s import to `from app.utils.mask_email import mask_email` (matching auth.py), verify its 2 call sites (lines 81, 94) are unaffected (same call signature), and add a regression test asserting a 1-character local part IS masked through `email_service.py`'s actual log call. Stops short of touching `mask_secret()`/`mask.py`'s continued existence — that's the separate item below. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Delete `backend/app/utils/mask.py` for real this time — after the fix above, `mask.py`'s `mask_email()` has zero remaining callers, and its other function `mask_secret()` already has zero callers today (confirmed: `grep -rn 'from app.utils.mask import\|utils\.mask\b' backend/app --include='*.py'` matches nothing once email_service.py is repointed). This file was already targeted for full deletion once (2026-09-15, commit `c3f501a`, itself following an earlier "Remove three-way duplicate secret-masking helpers" item) but a subsequent "repair staged item" commit (`ee59ba2`) re-added `mask_secret()` into it, and it was never actually `git rm`-ed. There is no dedicated test file left for it today (`test_mask.py` was deleted by `4809220`; only a stale `__pycache__` entry remains) so this is a clean, test-free deletion. Depends on (must land after) the item above. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Remove dead duplicate `backend/app/utils/ssrf.py`, superseded by `ssrf_guard.py` — `ssrf.py`'s `is_safe_url(url)` has zero callers anywhere in `backend/app` (verified: `grep -rn 'from app.utils.ssrf import\|utils\.ssrf\b' backend/app --include='*.py'` matches nothing; only `backend/tests/test_ssrf.py` references it). The real, wired implementation is `backend/app/utils/ssrf_guard.py`'s `is_safe_url`/`is_safe_url_async`, imported by `backend/app/routers/webhooks.py` line 15 and `backend/app/services/notification_dispatcher.py` line 29. The two aren't even identical: `ssrf.py`'s version additionally blocks the CGN range (`100.64.0.0/10`) and IPv6 site-local/ULA ranges that `ssrf_guard.py` doesn't — a real behavioral gap worth noting, but since `ssrf.py` is provably unreachable from any live code path, it isn't protecting anything today. Delete `backend/app/utils/ssrf.py` and `backend/tests/test_ssrf.py`. Deliberately does NOT port the extra CGN/IPv6 checks into `ssrf_guard.py` — that would be a real behavior change to the wired security gate and deserves its own reviewed item, not a bundled dead-code deletion. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] `git rm` the still-empty `backend/app/utils/schedule.py` stub — confirmed via `cat -A`/`wc -l` that this file is 0 bytes today, but is still tracked in git (`git ls-files backend/app/utils/schedule.py` returns a match). History (`git log --oneline -- backend/app/utils/schedule.py`): `8d13abd` added it, `ccf2c5a` "remove unused schedule utilities and tests" emptied its contents (and correctly deleted `backend/tests/test_schedule.py`'s test bodies), and the immediate follow-up `d988ff5` ("remove file(s) per DELETE trailer") `git rm`-ed `backend/tests/test_schedule.py` for real but did NOT do the same for `backend/app/utils/schedule.py` itself — an exact repeat of this portfolio's known "dead-code removal empties instead of deleting" bug (previously found on `flakes_quarantine.py`/`rbac.py`), except this instance slipped through the mechanism's own fix. `backend/app/utils/cron_match.py` remains the real, wired scheduling logic (used by `backend/app/routers/ci.py` and `backend/app/services/scheduling.py`) — nothing depends on `schedule.py`. {cat: infra; size: S; multifile: no; research: none}
- [ ] [P2] [decomposed] Wire `run_status.derive_run_status()` into `execution_engine.py`'s hand-rolled run-status computation so a flaky run is no longer misreported as "completed" — `backend/app/utils/run_status.py`'s `derive_run_status(passed, failed, flaky=0)` (fully tested pure function that returns `"failed"`/`"flaky"`/`"passed"`) has zero callers anywhere in `backend/app` (verified via grep). Meanwhile `backend/app/services/execution_engine.py` sets its run's terminal status inline at 4 separate call sites (e.g. `run_status="completed" if failed == 0 else "failed"`) — a hardcoded 2-way check that has no "flaky" case at all, even though this repo's whole flake-detection feature exists specifically to identify tests that failed-then-passed within the same run. Replace the inline ternary at each of those 4 call sites with `derive_run_status(passed=passed, failed=failed, flaky=flaky_count)` (the run's already-computed flaky-test count, sourced the same way `flake_detection.py`'s existing flaky-tracking does). Stops short of touching `is_terminal()`/`can_transition()` (also zero-caller in `run_status.py`) — those would require validating the pause/resume/cancel endpoints' state machine, a larger, separate wiring job. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Remove dead duplicate `kb_index` builders `build_index()`/`build_index_from_objects()`, superseded by `build_index_from_rows()` — `backend/app/utils/kb_index.py` defines THREE separate index-building functions; only `build_index_from_rows()` is wired into production (`backend/app/services/flake_detection.py` line 21/232, powering `GET /api/flakes/index`). `build_index()` (the original) has zero callers anywhere in `backend/app` outside its own tests (verified via grep) — it's exercised only by `backend/tests/test_kb_index.py`'s `TestBuildIndexStructure` class. `build_index_from_objects()` is worse: zero callers AND zero test coverage at all (confirmed: `grep -n build_index_from_objects backend/tests/test_kb_index.py` returns nothing) — a completely orphaned third variant, likely a leftover from an earlier failed decomposition attempt. Delete both functions and the now-unused `TestBuildIndexStructure` test class (keep `TestBuildIndexFromRows`, which covers the real, wired function). {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Remove dead `format_dashboard_summary()`/`format_priority_score()`/`_format_duration()` from `dashboard_metrics.py` — these three pure formatters (which turn a raw summary dict into a UI-ready "card" list with pre-formatted strings like `"1h 30m"`/`"45.2%"`) have zero callers anywhere in `backend/app` (verified via grep — `GET /api/test-runs/dashboard/summary` in `backend/app/routers/test_runs.py` line 188 returns `calculate_dashboard_summary()`'s raw dict directly, and `frontend/src/pages/DashboardPage.vue` formats values itself client-side). Despite being fully dead in production, they have redundant test coverage spread across TWO separate files — `backend/tests/test_dashboard_metrics.py` (the `test_format_priority_score*`/`test_format_dashboard_summary*`/`test_format_duration*` functions) AND a second, entirely dedicated `backend/tests/test_dashboard_metrics_format.py` — a duplicate-test-file pattern matching this portfolio's earlier `mask_secret.py`/`test_utils_mask_secret.py` divergence. Delete the three dead functions from `dashboard_metrics.py`, delete `backend/tests/test_dashboard_metrics_format.py` entirely, and remove just the format-related test functions from `test_dashboard_metrics.py` (keep its `calculate_dashboard_summary` tests, which cover the real, wired function). {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P3] [decomposed] Remove dead `backend/app/utils/pagination.py` (`clamp_page_size`/`offset_for`) — zero callers anywhere in `backend/app` (verified via grep; only `backend/tests/test_utils_pagination.py` references it). This repo's list endpoints (e.g. `GET /api/test-runs` in `backend/app/routers/test_runs.py` line 434) use FastAPI's own `Query(0, ge=0)`/`Query(10, ge=1, le=100)` skip/limit bounds directly — confirmed via `grep -rn 'page:\|page_size\|Query.*page' backend/app/routers` returning zero matches anywhere in the repo — so there is no page/size-style pagination convention anywhere for these helpers to naturally join; they were written for a pagination style this codebase doesn't use. Delete `backend/app/utils/pagination.py` and `backend/tests/test_utils_pagination.py`. {cat: backend; size: S; multifile: no; research: none}
- [ ] [P3] [decomposed] Remove dead sync `validate_url()` wrapper in `webhooks.py`, superseded by the async `is_safe_url_async()` already used at the real call site — `backend/app/routers/webhooks.py`'s `validate_url(url)` (line 38, a thin `return is_safe_url(url)` wrapper) has zero callers in production: `register_webhook` (line 105) calls `await is_safe_url_async(url)` directly instead (per this roadmap's earlier "blocking DNS resolution" fix), leaving `validate_url()` reachable only from its own dedicated test file, `backend/tests/test_webhooks_validate_url.py` (confirmed via grep — no other caller anywhere in `backend/app`). Since `validate_url()` is a pure passthrough to `ssrf_guard.is_safe_url()`, its ~20 existing assertions remain valid unchanged against the underlying function — retarget `test_webhooks_validate_url.py`'s import from `from app.routers.webhooks import validate_url` to `from app.utils.ssrf_guard import is_safe_url as validate_url` (one-line change, zero assertion rewrites needed), then delete the now-fully-dead `validate_url()` function from `webhooks.py`. {cat: backend; size: S; multifile: yes; research: none}

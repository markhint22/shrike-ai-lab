# Roadmap — shrike-monitor (uptime + heartbeat brick)
**Maturity:** MEDIUM (monitors/results/incidents exist). **Stopping point:** a COMPLETE, dogfooded v1 that replaces the ad-hoc deploy_watch scripts. INFRASTRUCTURE — reach v1 and freeze.

## P1 — complete v1 (next quarter, then STOP)
- [ ] [P1] [decomposed] Heartbeat dead-man logic, uptime ratio, http status classifier, incident transitions (pure + wired) {cat: backend; size: M; multifile: yes; research: repo}
- [ ] [P1] [decomposed] Monitor create validation + ping-token-shown-once security {cat: backend; size: S; multifile: no; research: repo}
- [ ] [P1] [blocked-ops] Dogfood: replace the fleet's deploy_watch/deploy_health scripts with real monitors {cat: infra; size: M; research: web} -- RESEARCHED 2026-09-09 (Claude): the CODE side is already done (MonitorScheduler actively polls http monitors on interval, checker.py + heartbeat dead-man logic + notifier.py already publishes status transitions to a shrike-notify topic). Nothing left to decompose into a fleet backlog item -- nothing in shrike-monitor's OWN repo needs writing. What remains is pure ops/integration, out of the fleet's per-repo-code-edit model: (1) shrike-monitor isn't deployed anywhere live yet -- deploy it (Railway backend, same pattern as the other 7 apps); (2) register the real production health-check URLs from deploy_watch.sh's SURFACES list as monitors via POST /monitors; (3) decide whether to retire deploy_watch.sh's ad-hoc `hc()` polling in favor of reading GET /monitors/status, or keep deploy_watch.sh for what shrike-monitor CANNOT do (Railway/Vercel deploy-status + auto-enqueue-a-fix, which needs the Railway/Vercel CLIs, not just an HTTP health check). Tagged blocked-ops (not needs-research/ready) so the planner stops re-surfacing this as a fleet gap -- it is a deployment decision for a human/Claude session, not code the 27B can write.
## P2 — light surface (only if needed)
- [ ] [P2] [decomposed] Minimal status page/API for the fleet dashboard to read {cat: backend; size: S; multifile: no; research: repo}
- [ ] [P2] [decomposed] Seed scheduler status baseline from persisted results on restart — currently MonitorScheduler._last_status/_incident_state start empty every process start (app/services/scheduler.py), and rehydrate() (app/store.py) only restores monitors/results, not scheduler state; the first tick after a restart always treats each monitor's status as a fresh baseline (old_status is None) and skips notify, so a monitor that goes down or recovers during shrike-monitor's own restart window silently misses its transition alert. Seed _last_status (computed via overall_status/heartbeat_status against rehydrated results) in app/main.py's lifespan or MonitorScheduler.start() before the first tick, plus a regression test in backend/tests/test_scheduler.py. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] Wire downtime duration into notifier alert bodies — app/utils/duration.py's humanize_seconds() is fully implemented and unit-tested (backend/tests/test_utils.py) but never imported by app/services/notifier.py; a down transition alert currently says only changed from up to down with no indication of how long the prior state lasted. Compute elapsed time since the last transition in MonitorScheduler._maybe_notify (app/services/scheduler.py) and pass it through Notifier.notify_transition (app/services/notifier.py) to include a humanized duration in the alert body/title, with tests added to backend/tests/test_notifier.py and backend/tests/test_scheduler.py. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] Heartbeat monitors never record down/up CheckResults, leaving uptime_ratio and incident history blind — app/routers/heartbeat.py's ping() only ever records CheckResult(ok=True); a missed heartbeat is detected live in MonitorScheduler.tick()/_maybe_notify() (app/services/scheduler.py) via heartbeat_status(), but no CheckResult(ok=False) is ever stored, so uptime_ratio() (app/models.py) and GET /monitors/{id}/incidents (app/routers/monitors.py, which also hard-returns [] for non-http types) both report near-100% uptime and zero incidents for a heartbeat monitor that has been down for hours. Have MonitorScheduler record a synthetic CheckResult on each heartbeat status transition (down/up) so uptime_ratio and incident history reflect real downtime, plus regression tests in backend/tests/test_scheduler.py and backend/tests/test_heartbeat.py. {cat: backend; size: M; multifile: yes; research: none}
- [ ] [P2] [decomposed] Notifier retries hammer shrike-notify with no backoff — app/services/notifier.py's notify_transition() loops up to max_retries+1 times with zero delay between attempts (no asyncio.sleep, no jitter), so a shrike-notify outage gets every retry fired in the same instant instead of spaced out; app/utils/jitter.py's with_jitter() is fully implemented and unit-tested (backend/tests/test_utils.py) but never imported anywhere in app/. Add an awaited backoff delay (base delay scaled by with_jitter()) between retry attempts in notify_transition, with tests in backend/tests/test_notifier.py asserting backoff behavior via a fake/injected sleep. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P2] [decomposed] GET /health never checks whether MonitorScheduler's background loop is alive — app/main.py's health() returns a static Health(status="ok") regardless of whether scheduler._task has died (e.g. crashed and never restarted); this is exactly the "silent failure" class of bug shrike-monitor exists to catch in other services, applied to itself. Expose a liveness signal (e.g. MonitorScheduler.is_running() checking self._task.done()/cancelled, plus a last-tick timestamp) and surface it on GET /health (extend app/schemas.py's Health) so a dead scheduler shows up as unhealthy instead of masquerading as ok, with tests in backend/tests/test_scheduler.py and an extended health test. {cat: backend; size: S; multifile: yes; research: none}
## Stopping point (HARD)
Once monitors + heartbeats + incidents work and the fleet dogfoods it: **v1 COMPLETE — freeze.** No PagerDuty-scale features. It's a dead-man's-switch + uptime brick.
## P3 — new gaps found by reading current code (2026-09-14 research pass)
- [ ] [P1] [decomposed] Wire heartbeat down/up CheckResults into the actual tick loop — `MonitorScheduler._record_synthetic_result()` (backend/app/services/scheduler.py:178) already exists and is directly unit-tested (backend/tests/test_scheduler.py: test_record_synthetic_result_persists, test_heartbeat_transition_records) but is never called from `MonitorScheduler.tick()` (backend/app/services/scheduler.py:122, heartbeat branch at line 136 only calls `heartbeat_status()`/`_maybe_notify()`); confirmed via grep that its only callers repo-wide are its own tests. Result: `uptime_ratio()` and `GET /monitors/{id}/incidents` (backend/app/routers/monitors.py:102) are still blind to heartbeat outages exactly as suspected. In `tick()`, capture `old_status = self._last_status.get(monitor.id)` before calling `_maybe_notify` for heartbeat monitors, and call `await self._record_synthetic_result(monitor, now)` when `old_status is not None and old_status != new_status` (i.e. on a real transition, not every tick). Also update the now-stale claim at backend/app/routers/monitors.py:107 ("heartbeat monitors ... always return an empty list") and the README.md incidents row ("http monitors only") since incidents will now be derivable for heartbeats too once results exist. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P2] [decomposed] Notifier alert bodies show raw seconds instead of the existing humanizer — `Notifier.notify_transition()` (backend/app/services/notifier.py:82-84) builds `body_text += f" Duration in new state: {duration_seconds:.1f}s."`, so a 2-hour outage alert reads "7200.0s" instead of something readable; `app/utils/duration.py`'s `humanize_seconds()` is fully implemented and unit-tested (backend/tests/test_utils.py) but confirmed via grep to have zero callers outside its own test file. Import `humanize_seconds` in notifier.py and use it to render `duration_seconds` in the body text (keep the raw `duration_seconds` field in the JSON payload as-is for machine consumers), with an assertion added to backend/tests/test_notifier.py. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Expose total downtime duration on monitor status — `app/utils/downtime.py`'s `downtime_seconds(checks: list[tuple[float, bool]]) -> float` is fully implemented and unit-tested (backend/tests/test_downtime.py) but confirmed via grep to have zero callers outside its own test file, so `GET /monitors/{id}/status` (backend/app/routers/monitors.py:78, `StatusOut` schema at backend/app/schemas.py:77) reports `uptime_ratio` but never total elapsed downtime. In `monitor_status()`, build `[(r.checked_at, r.ok) for r in results]` and pass it to `downtime_seconds()`, add a `downtime_seconds: float` field to `StatusOut`, and add a regression test in backend/tests/test_monitors_api.py. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Remove dead/misleading `MonitorResponse` schema — `app/schemas.py:49`'s `MonitorResponse` (fields: `id: int`, `name`, `url`, `token`) is confirmed via grep to have no router or app callers (only referenced by its own test, backend/tests/test_schemas.py) and models an `id` as `int`, inconsistent with the real `Monitor` dataclass (backend/app/models.py:54) which mints `id` as a `uuid.uuid4().hex` string (backend/app/store.py `Store.create()`) — a future contributor/LLM could easily mistake this for the real monitor-creation response shape (that's actually `MonitorCreated`, schemas.py:64). Delete the unused class and its test. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Heartbeat ping-token comparison is not constant-time — `routers/heartbeat.py:31`'s `ping()` does `token != mon.ping_token` (plain `!=`), a timing side-channel on the same secret the P1 "ping-token-shown-once security" item was written to protect. Swap in `secrets.compare_digest(token, mon.ping_token)` (guard the None case first, since `compare_digest` requires both args to be str/bytes of str), with a regression test added to backend/tests/test_ping_token.py asserting a wrong-length/wrong-value token is still rejected. {cat:backend; size:S; multifile:no; research:none}
## P4 — new gaps found by reading current code (2026-09-14 second research pass)
- [ ] [P1] [decomposed] Fix TypeError crash in MonitorScheduler._maybe_notify's notifier call — `MonitorScheduler._maybe_notify()` (backend/app/services/scheduler.py:235-245) calls `self.notifier.notify_transition(monitor.id, monitor.name, old_status, new_status, duration_seconds=duration, sleep_func=self._sleep_func)`, but the real `Notifier.notify_transition()` (backend/app/services/notifier.py:61-68) only accepts `(monitor_id, monitor_name, old_status, new_status, duration_seconds=None)` — no `sleep_func` parameter (that's accepted only by `Notifier.__init__`, notifier.py:43, and used internally at notifier.py:111). Confirmed via grep that every scheduler test (backend/tests/test_scheduler.py) exercises this path with a hand-rolled fake (`_RecordingNotifier`/`_SpyNotifier`/`FakeNotifier`) whose `notify_transition()` signature accepts `sleep_func=None`, masking the mismatch — the real `Notifier` class is never driven through `_maybe_notify()` in any test. In production, any real status transition with `NOTIFY_URL` configured raises `TypeError: notify_transition() got an unexpected keyword argument 'sleep_func'` inside `tick()` (swallowed by `run_forever()`'s broad except and logged, but the notification never actually sends). Fix: drop the unused `sleep_func=self._sleep_func` kwarg from the call in scheduler.py (MonitorScheduler's own `self._sleep_func` attribute, scheduler.py:109, has no other production use), and add a regression test in backend/tests/test_scheduler.py that drives `_maybe_notify()`/`tick()` against the real `app.services.notifier.Notifier` (with an injected fake `publish` callable) to prove a transition actually delivers. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P1] [decomposed] Wire Settings.max_results/max_monitors into the actual Store instance — `Settings.max_results` (default 100) and `Settings.max_monitors` (default 1000), backend/app/config.py:26-27, are read from `MAX_RESULTS`/`MAX_MONITORS` env vars and covered by backend/tests/test_config.py, but the module-level singleton `store = Store()` (backend/app/store.py:144) is constructed with zero arguments, so it always falls back to `Store.__init__`'s own hardcoded defaults (`_MAX_RESULTS=100`, `max_monitors=1000`, store.py:33) — confirmed via grep that backend/app/main.py never reads `settings.max_results`/`settings.max_monitors` into `store` anywhere. A deployer setting `MAX_RESULTS=1000` (to retain longer check history) or `MAX_MONITORS=20` (to cap fleet size) gets silently no-effect config. Fix: since `Store.__init__` bakes `max_results` into the `_results` defaultdict's `deque(maxlen=...)` factory closure (store.py:38-39), add a `Store.configure(max_results, max_monitors)` method that updates `self.max_monitors` and rebuilds `self._results` as a new defaultdict with the new maxlen (copying any already-recorded per-monitor deques into it, truncated to the new maxlen), then call `store.configure(settings.max_results, settings.max_monitors)` in `app/main.py` before `lifespan` starts serving. Add a regression test in backend/tests/test_store.py or backend/tests/test_config.py asserting the app's store instance actually respects a non-default `MAX_RESULTS`/`MAX_MONITORS`. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] Wire dead sla_met() helper into a real SLA-compliance field — `app/utils/sla_met.py`'s `sla_met(results: list[CheckResult], target: float) -> bool` (utils/sla_met.py:4) is fully implemented, imports the real `app.models.uptime_ratio`, and is unit-tested (backend/tests/test_sla_met.py), but confirmed via grep to have zero callers outside its own test file — there is no way for an API consumer to ask "is this monitor meeting its uptime target" even though `GET /monitors/{id}/status` (backend/app/routers/monitors.py:79, `StatusOut` schema at backend/app/schemas.py:68-76) already returns `uptime_ratio`. Add an optional `sla_target: float | None = Query(None, ge=0.0, le=1.0)` param to `monitor_status()` (routers/monitors.py:79) and an `sla_met: bool | None = None` field to `StatusOut`; when `sla_target` is provided, compute it via `sla_met(results, sla_target)` and populate the field (leave it `None` when no target is passed). Add a regression test in backend/tests/test_monitors_status_api.py covering both the with-target and without-target cases. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Delete dead duplicate is_overdue() heartbeat helper — `app/services/heartbeat.py`'s `is_overdue(last_ping_ts, interval_s, grace_s, now_ts)` (services/heartbeat.py:11) is a logical duplicate of the wired `app.models.heartbeat_overdue()` (app/models.py:317, called from `heartbeat_status()` at models.py:342 and from `MonitorScheduler._record_synthetic_result()` in scheduler.py) — confirmed via grep that `is_overdue`'s only caller repo-wide is its own test file, backend/tests/test_heartbeat.py; its module docstring already admits it exists only "per the backlog spec" alongside the real one. Delete `app/services/heartbeat.py` and `backend/tests/test_heartbeat.py` since the real heartbeat-overdue logic already lives in, and is exercised via, `app.models.heartbeat_overdue`. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Delete dead duplicate consecutive_failures(list[bool]) util — `app/utils/consecutive_failures.py`'s `consecutive_failures(results: list[bool])` (utils/consecutive_failures.py:4) duplicates the wired `app.models.consecutive_failures(results: list[CheckResult])` (app/models.py:192), which is the one actually called from `MonitorScheduler.tick()` (app/services/scheduler.py:159 and :228) and `app.models.should_alert()`. Confirmed via grep that the utils version's only callers repo-wide are its own test file, backend/tests/test_consecutive_failures.py (backend/tests/test_core.py and test_models.py both exercise the `app.models` version instead). Delete `app/utils/consecutive_failures.py` and `backend/tests/test_consecutive_failures.py`. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Delete two dead duplicate HTTP-status classifiers — `app/services/checker.py`'s `classify_status(code)` (checker.py:72) and `app/services/classify.py`'s `http_status(code, latency_ms, degraded_ms)` (classify.py:13) both re-implement status-code banding that the wired `app.models.classify_http()` (app/models.py:308, called from `summarize_status()` at models.py:332, which every http-monitor status computation goes through) already covers. Confirmed via grep that neither `classify_status` nor `http_status` has any caller outside its own test coverage (backend/tests/test_core.py for `classify_status`, backend/tests/test_service_helpers.py for `http_status`) — both are pure dead code duplicating the one function actually driving monitor status. Delete both functions and their corresponding test cases (leave the rest of test_core.py/test_service_helpers.py intact). {cat:backend; size:S; multifile:yes; research:none}
## P5 — new gaps found by reading current code (2026-09-17 third research pass, roadmap refuel)
- [ ] [P1] [decomposed] Negative MAX_RESULTS/MAX_MONITORS env var crashes the store on the first write — `Settings._fallback_invalid_int` (backend/app/config.py:53-63) only falls back to the field default when an env value isn't parseable as an int at all; a value like `MAX_RESULTS=-5` parses fine as `-5` and is accepted as-is, so `Settings(MAX_RESULTS="-5").max_results == -5`. `app/main.py`'s `store.configure(max_results, max_monitors)` (main.py:37) feeds that straight into `Store.configure()`, which rebuilds `self._results = defaultdict(lambda: deque(maxlen=max_results))` (backend/app/store.py:55-57). `defaultdict`'s factory is lazy, so nothing errors at startup — but the FIRST time any monitor's result is ever recorded via `Store.record()` (store.py:116-118, hit by the very first HTTP probe or heartbeat ping), `self._results[monitor_id]` invokes the factory and raises `ValueError: maxlen must be non-negative`, taking down that call (and, inside `MonitorScheduler.run_forever()`, silently swallowing every future tick's probe/record attempts for every monitor forever, logged only as "Scheduler tick failed; continuing"). Reproduced directly: `s = Store(); s.configure(max_results=-5, max_monitors=1000); mon = await s.create(name="t", type="heartbeat"); await s.record(mon.id, CheckResult(ok=True, checked_at=time.time()))` raises exactly this. Confirmed via grep that neither backend/tests/test_config.py nor backend/tests/test_store.py exercises a negative `MAX_RESULTS`/`MAX_MONITORS` value. Fix: extend `_fallback_invalid_int` (or add a dedicated validator) to also fall back to the field default when the parsed int is `< 1`, matching the existing "invalid config never crashes startup" contract the function already documents, plus a regression test in backend/tests/test_config.py (negative env value falls back to default) and backend/tests/test_store.py (`Store.configure()` with a non-positive value doesn't produce a broken `deque`). {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Three more dead exports in app/services/checker.py never reach production — `determine_incident_transition()` (backend/app/services/checker.py:72), `calculate_uptime_ratio()` (checker.py:116), and `generate_ping_token()` (checker.py:60, whose own docstring already admits "Not currently wired into Store.create()") are confirmed via grep to have zero callers anywhere in app/ or app/routers/ — their only references anywhere in the repo are in backend/tests/test_checker_import.py, which merely asserts `callable(checker.determine_incident_transition)` etc. without exercising real behavior. Production actually uses three different implementations instead: incident transitions go through `MonitorScheduler.evaluate_incident_transitions()` (scheduler.py:24), fleet-wide health uses `app.models.uptime_ratio()`/`calculate_fleet_status()` (checker.py:102, which IS wired into routers/monitors.py:12/59), and ping tokens are minted by routers/monitors.py's own separately-defined `generate_ping_token()` (monitors.py:18-20). Same "duplicate implementation nobody wired up" pattern as the classify_status/http_status cleanup already tracked on this roadmap, just three more instances in the same file. Delete `determine_incident_transition()` and `calculate_uptime_ratio()` plus their `callable()` assertions in test_checker_import.py; either delete `generate_ping_token()` too or, if kept as the canonical implementation, switch routers/monitors.py to import and use it instead of maintaining a second copy. {cat:backend; size:S; multifile:yes; research:none}
- [x] [P2] [decomposed] Entire app/utils/status_streak.py module (3 functions) built and unit-tested but never wired into any endpoint — `describe_status()` (backend/app/utils/status_streak.py:6) and `current_streak()` (status_streak.py:29) are now wired: `routers/monitors.py` imports both and populates `StatusOut.status_label`/`current_streak` (commits `ec049b9`/`f48312c` on develop, verified live 2026-09-18 via `git show origin/develop:backend/app/routers/monitors.py`). {cat:backend; size:S; multifile:yes; research:none} <!-- 2026-09-18: checked off by monitoring session to stop repeated cheap ALREADY-DONE re-checks; see line below for the one function this item covered that is still genuinely unwired -->
- [ ] [P3] [decomposed] Delete the still-orphaned `compute_initial_status()` from status_streak.py — unlike its two siblings above, `compute_initial_status(results, monitor_id)` (backend/app/utils/status_streak.py:46) still has zero callers anywhere outside its own test (verified via grep across the whole repo, not just backend/app). It duplicates, less accurately, what `SchedulerService.seed_baseline()` (backend/app/services/scheduler.py:215) already does at startup via `overall_status()`/`heartbeat_status()` (which handle "degraded" and consecutive-failure counting; `compute_initial_status` only ever returns "up"/"down"/None) — seed_baseline is the real startup initial-status logic and does not call this function. Delete `compute_initial_status()` and its test (`test_status_streak.py::test_compute_initial_status` per OVERNIGHT_PROGRESS.md line 183-184) as dead/superseded code, following this roadmap's existing precedent for orphaned-helper cleanup. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] app/utils/alert_key.py's alert_key() dead — Notifier's real dedup mechanism (`Notifier._last_notified: dict[str, str]`, backend/app/services/notifier.py:55, checked at notifier.py:79) keys only on `monitor_id -> last-sent-status`, so it cannot distinguish two separate incidents on the same monitor that happen to end in the same status (down→up→down→up: the second "up" notification is suppressed as a dup of the first, even though it closed a completely different incident). `alert_key()` (backend/app/utils/alert_key.py:4) is a normalized SHA-256 dedup key built to include an `incident_id` for exactly this case, but confirmed via grep to have zero callers anywhere outside its own test file (backend/tests/test_alert_key.py). `MonitorScheduler` already tracks `_incident_state` per monitor (scheduler.py:104) and `_last_status_change_at` (scheduler.py:105) that could source an incident identifier. Thread an incident id (e.g. the transition's `_last_status_change_at` timestamp, or an incrementing per-monitor counter) through to `Notifier.notify_transition()`, and switch `_last_notified` to be keyed by `alert_key(monitor_id, new_status, incident_id)` instead of bare `monitor_id`, with a regression test in backend/tests/test_notifier.py proving two distinct incidents that both end in "up" each produce a notification instead of the second being deduped away. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P3] [decomposed] app/utils/severity_from_code.py's severity_from_code() dead — `Notifier.notify_transition()` (backend/app/services/notifier.py:90) hand-codes alert priority as a binary `"high" if new_status == "down" else "default"`, discarding any HTTP status-code detail from the underlying `CheckResult`. `severity_from_code()` (backend/app/utils/severity_from_code.py:4) already maps a raw status code into `ok`/`redirect`/`client_error`/`server_error`/`unknown` bands, but is confirmed via grep to have zero callers outside backend/tests/test_severity_from_code.py. As it stands, a monitor down from a 500 and one down from a 404 (client misconfiguration, not a server outage) generate identical "high priority, down" alerts. Thread the latest `CheckResult.status_code` through to `notify_transition()` (only meaningful for http monitors — heartbeat CheckResults carry `status_code=None`, so guard accordingly) and use `severity_from_code()` to add a more granular severity tag/priority, with a regression test in backend/tests/test_notifier.py. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Two more dead uptime-ratio-family duplicates, on top of the wired app.models.uptime_ratio() — `uptime_pct()` (backend/app/services/uptime.py:10, "up/total as a rounded 0-100 percentage") and `uptime_ratio(up_checks, total_checks)` (backend/app/utils/uptime.py:11, "count-based, distinct from app.models.uptime_ratio") are both confirmed via grep to have zero callers outside their own test files (backend/tests/test_service_helpers.py and backend/tests/test_uptime.py/test_utils.py respectively) — every real caller (routers/monitors.py's `/status` and `/monitors/status` endpoints) uses `app.models.uptime_ratio(results: list[CheckResult])` instead, which is the only one of the three ever fed real data. Either delete both dead siblings and their tests, or — since `uptime_pct()`'s 0-100 display format is genuinely different from the 0.0-1.0 fraction `StatusOut.uptime_ratio` exposes today — wire `uptime_pct()` into `StatusOut` as a `uptime_percent: float` display field computed from the already-fetched `results`, and delete the still-unused count-based `utils/uptime.py` variant since no caller anywhere has plain `(up, total)` counts to hand it. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] parse_comma_separated() dead — duplicates config.py's inline CORS parsing verbatim — `app/utils/__init__.py`'s `parse_comma_separated(value)` (utils/__init__.py:15-27, "split on comma, strip whitespace, drop empties") is confirmed via grep to have zero callers outside backend/tests/test_utils.py, while `Settings._parse_cors_origins` (backend/app/config.py:65-72) re-implements the exact same logic inline as a `field_validator` (`[origin.strip() for origin in v.split(",") if origin.strip()]`) instead of importing and calling the already-tested utility. Two independent implementations of the same one-liner, only one of which is exercised by real traffic (env var parsing at startup). Replace the body of `_parse_cors_origins` with a call to `parse_comma_separated(v)`, and add/keep one regression test in backend/tests/test_config.py asserting `CORS_ORIGINS` parsing still behaves identically after the swap. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] Five pure-math utility modules with full test coverage and zero production callers — `clamp()` (backend/app/utils/clamp.py:5), `ema()` (utils/ema.py:5), `mean_stdev()` (utils/mean_stdev.py:4), `pct_change()` (utils/pct_change.py:5), and `round_sig()` (utils/round_sig.py:7) are each confirmed via grep to have zero callers anywhere in app/ outside their own dedicated test files (backend/tests/test_clamp.py, test_ema.py, test_mean_stdev.py, test_pct_change.py, test_round_sig.py) — a fifth "utils graveyard" cluster on top of the other dead-code items on this roadmap, seemingly built as building blocks for latency-trend/anomaly-detection features (`ema`/`mean_stdev` smoothing a monitor's latency series, `pct_change` flagging a latency spike vs. the prior window, `round_sig`/`clamp` for display formatting) that were never actually added to `app/models.py`'s `latency_stats()` (models.py:137-157, which currently only computes min/avg/max/p95 with no trend/smoothing) or any router. Either wire at least one real consumer (e.g. an EMA-smoothed latency trend or a pct_change-based "latency regressed" flag surfaced on `GET /monitors/{id}/status`), or delete all five modules and their tests as unused speculative scaffolding — leaving them un-wired indefinitely just grows the surface area future readers have to rule out as dead. {cat:backend; size:M; multifile:yes; research:none}

## Grooming review — 2026-09-18 (dry-since-14:24 batch, reports/grooming-shrike-monitor-20260918.md)
Re-evaluated the 04:00 grooming pass's 11 "Proposed" candidates against the live repo (today had a `status_label`/`current_streak` StatusOut landing plus other test-refactor commits). **All 11 dropped — none applied.** No net-new roadmap items resulted; kept for audit trail only:
1-8. Duplicate re-proposals of items already marked done in the same report's own "Current Next Steps" section (`Store.results` limit/empty tests, `Store.record_probe`/`Store.latest`, `sla_met` boundary tests, `ema` tests, `CheckResult.__post_init__`/`health()`/`check_url_rules` return-type hints, `StoreFull` → 507 handling) — verified against `backend/tests/test_sla_met.py`, `test_ema.py`, `store.py`, `models.py`, `schemas.py`, `routers/monitors.py`: all present and passing as described. Stale LLM re-proposals, not real gaps.
9. `severity_from_code` tests — already wired: `backend/app/services/notifier.py` imports and calls `severity_to_priority` (sibling fn in the same module) at line 94, and `backend/tests/test_severity_from_code.py` already has the exact 2xx/3xx/4xx/5xx/unknown-band coverage proposed.
10. `status_streak` wiring/tests — landed today: `routers/monitors.py` imports `describe_status`/`current_streak` from `app.utils.status_streak` and populates `StatusOut.status_label`/`current_streak` (commits `ec049b9`/`f48312c` on develop). This closed the pre-existing P2 backlog item on this very roadmap (see the "Entire app/utils/status_streak.py module... never wired" entry above) — that item should now be considered resolved/committed even though its checkbox wasn't flipped by this pass.
11. `GET /api/v1/monitors/{id}/history` — functionally duplicate of the existing `GET /{monitor_id}/results` endpoint (`routers/monitors.py`), which already returns recent check results oldest-first with a `limit` query param.

## P3 — additional dead-code audit (2026-09-18/19, monitoring session, verified by direct code read)
- [ ] [P3] [decomposed] Delete the dead event-driven incident state machine `next_state()` in `app/services/incident.py` — this pure function (`next_state(current, result) -> str`, a "none"/"open"/"closed" transition step distinct from the retroactive-batch `app.models.derive_incidents()` that `routers/monitors.py:150` actually calls for `GET /monitors/{id}` incidents) is fully documented and unit-tested, but confirmed via `grep -rn "from app.services.incident import\|services.incident\|services import incident" backend/app/ backend/tests/` to have exactly one reference repo-wide: its own test file (`backend/tests/test_service_helpers.py`). No router, scheduler, or notifier ever imports it. Delete `app/services/incident.py` and its test coverage in `test_service_helpers.py` as unused speculative scaffolding, matching this roadmap's existing precedent for orphaned pure-logic helpers (`is_overdue`, `classify_status`/`http_status`). {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P3] [decomposed] Delete the dead `humanize_count()` helper in `app/utils/humanize.py` — this pure compact-count formatter (999->"999", 1200->"1.2k", 3400000->"3.4M") is fully unit-tested in `backend/tests/test_utils.py` but confirmed via `grep -rn "humanize_count" backend/app/ backend/tests/` to have zero callers outside that test file — unlike its sibling `humanize_seconds()` in `app/utils/duration.py`, which IS wired into `Notifier.notify_transition()` (`app/services/notifier.py:91`, verified) for the alert-body duration string. No router or service anywhere formats a count (uptime count, monitor count, etc.) with this helper. Delete `app/utils/humanize.py` and its `test_humanize_count_*` cases from `test_utils.py`, or wire it into an actual count display (e.g. a `GET /monitors/status` fleet-summary count) if one is wanted instead. {cat: backend; size: S; multifile: yes; research: none}
- [ ] [P4] [decomposed] Delete the dead `resolve_checker_dependencies()` introspection helper in `app/services/checker.py` — this function (lines 88-108) walks the module's own `globals()` to build a name->source-module mapping, but is confirmed via `grep -rn "resolve_checker_dependencies" backend/` to have exactly one reference repo-wide: its own definition. Unlike every other function flagged as dead code on this roadmap, it has ZERO test coverage either (no `test_checker*.py` reference) — pure unused scaffolding with no caller and no test proving anyone ever relied on its output shape. Verified it runs without error (`resolve_checker_dependencies()` returns a plain dict of its own imports) but produces nothing any router/service/test consumes. Delete it. {cat: backend; size: S; multifile: no; research: none}

## Refuel 2026-09-19 (roadmap ran dry — 0 [ready], everything below was [decomposed]; queue-health sweep also showed doable=10/parked=4, under the 15-item floor). 4 new verified gaps + 1 correction to an existing item found by direct code read.

**Correction (no new checkbox needed):** the existing P3 item above ("app/utils/alert_key.py's alert_key() dead... thread an incident id through to Notifier.notify_transition()") is STALE — already fully implemented and tested. `backend/app/services/scheduler.py`'s `MonitorScheduler` already has `_incident_id: dict[str, str | None]` (line 105), assigns `f"inc-{monitor_id}-{int(now)}"` on a new incident (tick(), lines 169-172), and passes it to `notify_transition(..., incident_id=self._incident_id.get(monitor.id))` (`_maybe_notify`, line 249). `backend/app/services/notifier.py`'s `Notifier.notify_transition()` builds its dedup key as `alert_key(monitor_id, new_status, incident_id)` (line 84) and keys `_last_notified` by it, not bare `monitor_id`. `backend/tests/test_notifier.py::test_two_distinct_incidents_...` (~line 307) proves two separate incidents that both end in "up" each produce a notification. Following this file's own established precedent (see the 2026-09-18 grooming-review note above for `status_streak`), this item should be considered resolved even though its checkbox was never flipped.

- [ ] [P2] [decomposed] `app/services/checker.py`'s `probe()` has a whole notifier/status_code-aware notify branch that is dead in production — `probe(url, fetch=None, ..., notifier=None, monitor_id="unknown", monitor_name="unknown", old_status="pending")` (line 29) calls `notifier.notify_transition(..., status_code=result.status_code)` on both its success path (line 69) and its network-error path (line 54, `status_code=None`) whenever a `notifier` is passed in — but its one and only production caller, `MonitorScheduler.tick()` (`backend/app/services/scheduler.py` line 143), calls it as bare `checker.probe(monitor.url, fetch=self._fetch)` with no `notifier`/`monitor_id`/`monitor_name`/`old_status` args at all (confirmed via `grep -rn "checker.probe" backend/app`: scheduler.py:143 is the only call site anywhere in `backend/app`). The real, only production notify call is `MonitorScheduler._maybe_notify()` -> `Notifier.notify_transition()` (scheduler.py line 246), which never passes `status_code` either — this is exactly why the still-open P3 item above ("Thread the latest CheckResult.status_code through to notify_transition()") remains unfixed: a parallel, better-looking implementation already exists in `probe()`, but it's wired to nothing and only ever exercised directly by `backend/tests/test_checker_import.py`. Delete `probe()`'s `notifier`/`monitor_id`/`monitor_name`/`old_status` params and its two `notifier.notify_transition(...)` call sites (they duplicate, in the wrong layer, what the still-open status_code-threading item asks for at the scheduler layer), OR thread `status_code` through `_maybe_notify()` directly instead (pull it from `self.store.latest(monitor.id)` before calling notify) and then delete probe()'s dead branch — either way, having both a dead and a live path for the same feature is what let the real item go unnoticed. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] `MonitorPersistence.save_result()` never prunes old rows — the `results` table grows without bound while the in-memory `Store` it backs is intentionally capped (`Store.max_results`, default 100, a `deque(maxlen=...)`, `backend/app/store.py` line 23/38-40). `backend/app/persistence.py`'s `save_result()` (line 123) does a bare `session.add(ResultRecord(...))` + commit on every single check with no corresponding delete of that monitor's oldest rows, and `load_all()` (line 131) loads every persisted row for every monitor at startup rehydrate — confirmed via `grep -rn 'delete(ResultRecord)' backend/app/persistence.py`: the only `ResultRecord` delete is in `delete_monitor()` (line 119, whole-monitor cleanup), never a per-write prune. For a long-running MONITOR_PERSIST=1 deployment (the durability feature this file exists for) this means an ever-growing `results` table and an ever-slower `rehydrate()` query at every restart, even though the in-memory store the DB is supposed to mirror only ever needs the most recent `max_results` rows per monitor. Add a prune step to `save_result()` (or a periodic sweep) that deletes rows for `monitor_id` beyond the newest `max_results`, matching the in-memory deque's own bound. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Three more zero-caller functions in `app/models.py` itself, on top of the checker.py/incident.py/utils dead-code already tracked on this roadmap — `worst_status()` (line 122), `should_alert()` (line 206), and `percentile()` (line 225) are each confirmed via `grep -rn '\bworst_status\(|\bshould_alert\(|\bpercentile\(' backend/app --include='*.py'` to have zero callers anywhere in `backend/app` outside their own definitions (only referenced by `backend/tests/test_models.py`/`test_percentile.py`). Notably `percentile()` genuinely disagrees with the p95 calculation `latency_stats()` (same file, line 137) computes inline three lines below it: `latency_stats()` uses a crude nearest-rank index (`int(0.95 * n)`), while the unused `percentile()` does proper linear interpolation (matching numpy's default) — the same "two implementations of the same stat, only one of which is real, and they disagree" pattern already flagged for `uptime_pct`/`uptime_ratio` elsewhere on this roadmap, just not yet caught for this pair. Either wire `percentile()` into `latency_stats()`'s p95 calculation (fixing the crude nearest-rank version) and delete the redundant inline math, or delete `percentile()`/`worst_status()`/`should_alert()` and their tests as unused. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Notifier fires on every single status flip with no consecutive-failure debounce, even though the dead `should_alert()` (see item above) implies one was intended — `MonitorScheduler._maybe_notify()` (`backend/app/services/scheduler.py` line 239) fires a notification the instant `new_status != old_status`, with no reference to `consecutive_failures`/threshold at all. Meanwhile `app/models.py`'s `should_alert(results, threshold)` (line 206, zero callers) implements exactly "only alert once N consecutive failures are seen AND the monitor was previously up" — and the scheduler already separately tracks a 3-consecutive-failure threshold for something else entirely (`evaluate_incident_transitions()`'s `_incident_state`, only used for incident-id/dedup bookkeeping, not for gating whether a notification fires at all). Net effect: a single transient blip (one bad probe that recovers on the very next tick) fires a full "went from up to down" AND "went from down to up" notification pair today, with no way to require a debounce window — exactly the alert-fatigue scenario `should_alert()` looks like it was built to prevent but was never wired in. Either wire `should_alert()`'s threshold check into `_maybe_notify()` (only notify a down-transition once `consecutive_failures >= N`), or delete it as abandoned scaffolding and document that single-blip alerting is the intended behavior. {cat:backend; size:M; multifile:yes; research:none}
- [ ] [P4] [decomposed] `app/config.py`'s `load_config_from_env()` is fully dead — zero callers anywhere, not even its own test. Confirmed via `grep -rn 'load_config_from_env' .` (from repo root, backend + tests + everything) matching only its own definition (line 127) — unlike its sibling `build_store_config()` in the same file, which IS still referenced by `backend/tests/test_config.py` and is deliberately kept per its own docstring ("kept here as a tested pure helper for now rather than deleted outright"), `load_config_from_env()` has no such note and no test at all exercising it. Delete it, or add the missing test if it's meant to replace `build_store_config()`/`Settings` for some future call site. {cat:backend; size:S; multifile:no; research:none}

## Newly discovered (2026-09-20 refuel — roadmap fully [decomposed], 0 backlog items left; Claude research pass)
**Re-confirmed (no new checkbox needed):** the P1 "blocked-ops" dogfood item above is still fully accurate as of today — `railway list` against the account shows no `shrike-monitor` project exists (billwatch/gitlark/chickadee-stream/flockworks/specpilot/voiceripple are all there; shrike-monitor is not), so it remains undeployed anywhere, and `shared/scripts/register_monitors.sh` (which already exists and correctly no-ops until `SHRIKE_MONITOR_URL` is set) has never had anything to register against. This stays a human/ops task, not a fleet-code gap.
- [ ] [P1] [decomposed] Add bearer-token authentication protecting the monitor-management API — confirmed via grep across `backend/app/` (excluding `.venv`) that no router or FastAPI dependency anywhere checks any `Authorization`/API-key header today: `POST /monitors` (create), `DELETE /{monitor_id}`, and every `GET` read (list/status/results/incidents) are all completely open to anyone who can reach the deployed URL — there is no `api_token`-style field on `app/config.py`'s `Settings` at all (only `notify_token`, which authenticates *outbound* calls shrike-monitor itself makes to shrike-notify, a different direction entirely). `shared/scripts/register_monitors.sh` (the fleet's own registration tooling, referenced by the P1 blocked-ops item above) already anticipates this and sends an `Authorization: Bearer ${SHRIKE_MONITOR_TOKEN}` header when set — but the server has nothing to check it against, so today that header would be silently ignored. Fix: extend `Settings` with an `api_token: str = ""` field (e.g. `MONITOR_API_TOKEN` env var, matching the same "empty string = feature is a no-op" gate already used for `notify_url`/`notify_token` in this same file), add a FastAPI dependency that 401s `POST`/`DELETE` requests under `/monitors*` when `api_token` is set and the request's bearer token doesn't match via `secrets.compare_digest` (the same constant-time-comparison fix already tracked elsewhere on this roadmap for the heartbeat ping-token) — leave every check a clean no-op when `api_token` is unset so local dev/tests are unaffected, and add regression tests covering both the gated-and-rejected and the gate-disabled cases. This is the one code-level gap standing between shrike-monitor and being safe to deploy publicly on Railway (this roadmap's own "reach v1" stopping point) — as it stands today, anyone with the URL could create noise monitors or delete real ones once it's live. {cat: backend; size: S; multifile: yes; research: none}

## Newly discovered (2026-09-21 refuel — backlog dry again; direct code read against origin/develop @ ab7d646)
**Status check (no new checkbox needed):** the P1 bearer-token item directly above is now fully LANDED and verified live — `backend/app/auth.py::require_api_token` (constant-time compare via `app.utils.constant_time_compare`) gates the whole `/monitors` router (`dependencies=[Depends(require_api_token)]`, `backend/app/routers/monitors.py:28`), `Settings.monitor_api_token`/`.api_token` exist with tests in `backend/tests/test_config.py`, and `backend/tests/test_monitors_api_token_auth.py`/`test_heartbeat_auth_separation.py` exercise both the gated-and-open-config cases. The P1 "blocked-ops" dogfood item above is unchanged — still no `shrike-monitor` Railway project provisioned — and remains a human/ops task, not fleet-actionable.
- [ ] [P4] [decomposed] Two zero-caller orphans left over from the bearer-token work itself, found by direct grep against the current tree — neither was ever wired in, and one is a fresh regression of an already-flagged pattern: (1) `backend/app/utils/auth.py::verify_bearer_token(provided, expected)` is a second, functionally-identical constant-time comparator (`secrets.compare_digest`) added in the same commit series as the real fix (`98e696a "add verify_bearer_token utility"`, `af76bd2 "add tests for verify_bearer_token"`), but the actual wiring commit (`fb7d5cc "require Authorization bearer token on /monitors CRUD"`) used the pre-existing `app.utils.constant_time_compare` instead — confirmed via `grep -rn "verify_bearer_token" backend/app backend/tests` that its only reference outside its own definition is its dedicated test file, `backend/tests/test_auth.py`; no router or dependency anywhere imports it. (2) `backend/app/utils/humanize_count.py::humanize_count(n)` is a relocated reincarnation of the `humanize_count()` this same roadmap already flagged once for deletion inside `app/utils/humanize.py` (see the still-open "Delete the dead `humanize_count()` helper" P3 item above, 2026-09-18 audit) — it now lives in its own file and is re-exported (unused) from `backend/app/utils/__init__.py` (`# noqa: E402,F401`), but unlike the version this roadmap already flagged, it now has ZERO test coverage anywhere (confirmed: no `test_humanize_count*` case exists in `backend/tests/`; `pytest --cov` shows `app/utils/humanize_count.py` at 8% covered — only the unused import line itself executes). Delete `verify_bearer_token()` + `backend/tests/test_auth.py`, and delete `humanize_count()` + its `app/utils/__init__.py` re-export (re-grep immediately before each deletion per this roadmap's own established precedent) — or, if a compact-count display is actually wanted somewhere (e.g. a fleet-size summary on `GET /monitors/status`), wire `humanize_count()` into that instead of deleting it, with real tests this time. {cat: backend; size: S; multifile: yes; research: none}

## Refuel 2026-09-21 (second pass same day — backlog dry again after the bearer-token/orphans batch consumed; direct code read against the live checkout, 8 fresh verified gaps)
- [ ] [P3] [decomposed] Two more dead time-math helpers in `app/services/scheduler.py`, on top of the already-tracked debounce/should_alert item — `calculate_transition_elapsed(start_time, end_time)` (scheduler.py:85) and `MonitorScheduler._calculate_elapsed_seconds()` (scheduler.py:229) are each confirmed via `grep -rn "calculate_transition_elapsed\|_calculate_elapsed_seconds" backend/app backend/tests` to have exactly one reference repo-wide outside their own definition: their own dedicated test cases (`test_calculate_transition_elapsed`/`test_calculate_elapsed_seconds`, `backend/tests/test_scheduler.py` lines ~380-412). The real production call site, `_maybe_notify()` (scheduler.py:275), computes elapsed time via the sibling *module-level* function `calculate_elapsed_seconds()` (scheduler.py:78) directly — never through either of these two. Delete `calculate_transition_elapsed()` and `MonitorScheduler._calculate_elapsed_seconds()` plus their two test functions in `test_scheduler.py`, and add one regression assertion (matching this roadmap's existing `test_no_humanize_count_export`-style precedent) proving both names are gone: `assert not hasattr(scheduler_module, "calculate_transition_elapsed")` and `assert not hasattr(MonitorScheduler, "_calculate_elapsed_seconds")`. Deliberately stops short of touching `calculate_elapsed_seconds()` itself (scheduler.py:78) or `_maybe_notify()` — both are live and correct. Tests: backend/tests/test_scheduler.py. VERIFY: `cd backend && .venv/bin/pytest tests/test_scheduler.py -v`. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] Two independent dead implementations of the same "rows to prune" calculation, neither one actually used by the pruning that ships — `app/utils/prune_threshold.py`'s `prune_threshold(total_rows, max_results)` (a pure `max(0, total_rows - max_results)`) and `app/persistence.py`'s `calculate_prune_threshold(session, monitor_id, threshold)` (the same formula, but querying `ResultRecord` count via SQLAlchemy) are each confirmed via `grep -rn "prune_threshold\|calculate_prune_threshold" backend/app backend/tests` to have zero callers anywhere outside their own dedicated test files (`backend/tests/test_prune_threshold.py`, `backend/tests/test_persistence.py::test_calculate_prune_threshold`). The real pruning that ships in production, `MonitorPersistence.save_result()` (persistence.py:137-162), does its own inline `keep_subq`/`delete(...).where(~ResultRecord.id.in_(keep_subq))` SQL and never calls either helper — a third, uncounted implementation of "how many/which rows to drop," the same "N implementations, only one wired" pattern already tracked elsewhere on this roadmap for `classify_status`/`http_status` and `uptime_pct`/`uptime_ratio`, just not yet caught for this pair. Delete both `prune_threshold()` and `calculate_prune_threshold()` plus their test files (`test_prune_threshold.py` in full, and the `test_calculate_prune_threshold` case in `test_persistence.py`). Deliberately stops short of touching `save_result()`'s own working prune logic, which stays exactly as-is. Tests: backend/tests/test_persistence.py (remove the one dead case; keep the rest of the file intact). VERIFY: `cd backend && .venv/bin/pytest tests/test_persistence.py -v` (and confirm `grep -rn "def prune_threshold\|def calculate_prune_threshold" backend/app` returns nothing once `app/utils/prune_threshold.py` is deleted). {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P2] [decomposed] `app/utils/auth.py` is an entirely dead module that also hides a live name-collision bug — confirmed via `grep -rn "from app.utils.auth import\|from app.utils import auth\|utils\.auth" backend/app` that **zero** files under `backend/app/` (routers, main.py, scheduler, notifier) import anything from this module; its only references anywhere are two of its own functions being exercised directly by `backend/tests/test_monitors_auth.py` (`test_verify_api_token_invalid_raises_401`/`test_verify_api_token_valid_passes`, calling `app.utils.auth.verify_api_token` — not through any real dependency wiring). The real, actually-wired bearer-token gate is `app/auth.py::require_api_token` (`Depends(require_api_token)` on the whole `/monitors` router, `backend/app/routers/monitors.py:28`) — a completely separate file. Worse, `app/utils/auth.py` **defines a function named `verify_bearer_token` twice** (line 10: `verify_bearer_token(provided: str, expected: str) -> bool`, a plain two-string compare; line 45: `verify_bearer_token(request: Request, expected_token: str) -> bool`, a request-parsing variant) — the second definition silently shadows the first at module load (confirmed by reading the file top to bottom: both are real `def` statements at module scope, no conditional/overload guard between them), so the first, differently-shaped version is permanently unreachable dead code with a name any importer would reasonably expect to behave like the first signature. Neither `verify_bearer_token` definition, nor the module's other two helpers (`verify_bearer_token_from_request` at line 21, `_compare_tokens` at line 38, itself a third constant-time-compare reimplementation), has ANY test coverage at all — `backend/tests/test_auth.py` (referenced by a prior roadmap pass as covering this file) does not currently exist in the repo (confirmed via `find backend/tests -iname '*auth*'`; only a stale `.pyc` remains in `__pycache__`, not a real source file). Delete the entire `app/utils/auth.py` module and the two `verify_api_token` test cases in `test_monitors_auth.py` that exercise it — the real, live, tested gate (`app.auth.require_api_token`) is unaffected and needs no change. Deliberately stops short of touching `app/auth.py` (the real, wired implementation) or any router. Tests: backend/tests/test_monitors_auth.py (remove the two dead-import cases; add one assertion that `app.utils.auth` no longer exists, e.g. `with pytest.raises(ModuleNotFoundError): import app.utils.auth`, in whichever test module is most appropriate — `test_monitors_auth.py` itself is fine). VERIFY: `cd backend && .venv/bin/pytest tests/test_monitors_auth.py -v` and `grep -rn "utils.auth\|utils import auth" backend/app` returns nothing. {cat:backend; size:S; multifile:yes; research:none}
- [ ] [P3] [decomposed] `app/main.py`'s `verify_token()` is a dead fourth reimplementation of heartbeat ping-token auth — `verify_token(monitor_id, token)` (main.py:60-73) duplicates, function-for-function, the real auth check that `app/routers/heartbeat.py::ping()` (heartbeat.py:34-48) already does inline via its own `coerce_token()` helper + `constant_time_compare()` — confirmed via `grep -rn "verify_token" backend/app backend/tests` that `verify_token`'s only reference outside its own definition is `backend/tests/test_ping_token.py::test_verify_token_behavior` (lines ~201-223), and confirmed via reading `heartbeat.py` end to end that the real `POST /heartbeat/{monitor_id}` route never imports or calls `main.verify_token` — it is not wired as a FastAPI `Depends()` anywhere, unlike what its docstring ("FastAPI dependency that verifies...") implies. This is the same "duplicate implementation, only one path is real" shape already tracked on this roadmap for the checker.py/incident.py/utils dead code, just in `main.py` this time. Delete `verify_token()` from `main.py` and its one test case in `test_ping_token.py`, and add a regression assertion in the same file proving it's gone (`assert not hasattr(app.main, "verify_token")`, matching this roadmap's established `test_no_X_export` precedent). Deliberately stops short of touching `heartbeat.py`'s real, working inline auth. Tests: backend/tests/test_ping_token.py. VERIFY: `cd backend && .venv/bin/pytest tests/test_ping_token.py -v`. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] `app/utils/humanize.py`'s `humanize_bytes()` is fully implemented and thoroughly unit-tested (8 cases in `backend/tests/test_utils.py` covering zero/small/KB/MB/GB/PB/negative/non-finite) but confirmed via `grep -rn "humanize_bytes" backend/app backend/tests` to have zero callers outside that test file — unlike its sibling `humanize_seconds()` (`app/utils/duration.py`), which IS wired into `Notifier.notify_transition()`, nothing in this codebase currently displays a byte count anywhere (no file-size, payload-size, or DB-size field exists on any schema/endpoint today, confirmed by grep across `backend/app/schemas.py` and `backend/app/routers/`). Delete `app/utils/humanize.py` and its `test_humanize_bytes_*` cases from `test_utils.py` as unused speculative scaffolding, matching this roadmap's existing precedent for `humanize_count()` in the same file — or, if a byte-count display is wanted somewhere in the future (e.g. a persisted-DB-size field on `/health` when `MONITOR_PERSIST` is on), wire it there instead of deleting, with the existing tests kept as-is either way. Tests: backend/tests/test_utils.py. VERIFY: `cd backend && .venv/bin/pytest tests/test_utils.py -v` (or, if deleted, `grep -rn "humanize_bytes" backend/app` returns nothing). {cat:backend; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] `Store.record_probe()` is a dead convenience wrapper nothing calls — `record_probe(self, monitor: Monitor, result: CheckResult)` (`backend/app/store.py:139-141`) exists solely to accept a `Monitor` object instead of a bare `monitor_id` before delegating to `self.record(monitor.id, result)`, but confirmed via `grep -rn "record_probe" backend/app backend/tests` to have zero callers outside its own test (`backend/tests/test_store.py::test_record_probe_records_result`) — the real, only production write path, `MonitorScheduler.tick()` (`backend/app/services/scheduler.py:157`), calls `self.store.record(monitor.id, result)` directly and has done so since the scheduler was first wired up. Delete `Store.record_probe()` and its one test case in `test_store.py`, and add a regression assertion (`assert not hasattr(Store, "record_probe")`) matching this roadmap's established dead-export precedent. Deliberately stops short of touching `Store.record()` itself, which stays exactly as-is (it's the real, live write path). Tests: backend/tests/test_store.py. VERIFY: `cd backend && .venv/bin/pytest tests/test_store.py -v`. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P4] [decomposed] `app/utils/backoff.py` has a dead, unreachable string-literal statement sitting AFTER the code, mistaken for the module docstring — the file's actual first statement is `import random` (line 1), followed immediately by `calculate_backoff_delay()`'s `def`; the string `"""Exponential backoff with jitter for retry logic."""` (line 34) sits as the very LAST line of the file, after the function's `return` — a bare, side-effect-free expression statement that Python evaluates and discards, not a real module docstring (which must be the first statement in the file to populate `__doc__`). Confirmed via `python3 -c "import app.utils.backoff as m; print(repr(m.__doc__))"` printing `None`, not that string. Every sibling file in `app/utils/` (`duration.py`, `downtime.py`, `clamp.py`, `ema.py`, `jitter.py`, `pct_change.py`, `round_sig.py`, `severity_from_code.py`) puts its one-line module docstring as the actual first statement, immediately setting `__doc__` — `backoff.py` is the one outlier, and the misplaced string reads exactly like an accidental leftover from a refactor (docstring text was probably meant to move to the top and never did). Move the string literal to the top of the file (as the real module docstring, before `import random`) and delete it from the bottom, matching every sibling file's convention. Deliberately stops short of any other change to `calculate_backoff_delay()`'s logic, which is unaffected either way. Tests: backend/tests/test_backoff.py (add one assertion that `app.utils.backoff.__doc__` is now the expected non-None string). VERIFY: `python3 -c "import app.utils.backoff as m; assert m.__doc__ and 'backoff' in m.__doc__.lower()"` (run from `backend/` with the venv active) and `cd backend && .venv/bin/pytest tests/test_backoff.py -v`. {cat:backend; size:S; multifile:no; research:none}
- [ ] [P3] [decomposed] `Settings.scheduler_tick_seconds` has no non-positive-value guard, unlike its three sibling int fields — `app/config.py`'s `_fallback_invalid_int` validator (config.py:61-83) already falls back to the field default for a non-positive or unparseable `max_results`/`max_monitors`/`alert_threshold`, with the validator's own docstring explaining exactly why ("a zero-size store is never a sane runtime config either"), but `scheduler_tick_seconds: float = 5.0` (config.py:38) is a plain unvalidated field — confirmed via reading `config.py` top to bottom that no `@field_validator` covers it. `MonitorScheduler.run_forever()` (`backend/app/services/scheduler.py:291-298`) does `await asyncio.sleep(self.tick_seconds)` every loop iteration; `asyncio.sleep()` treats a zero or negative delay as "return immediately" rather than raising, so a deployer setting `SCHEDULER_TICK_SECONDS=0` (or a negative value, or a non-numeric one — which Pydantic's plain `float` coercion would instead reject with a hard `ValidationError` at startup, an even worse outcome than the silent bad-config-survives-startup contract this file otherwise guarantees) either pegs the event loop in a tight busy-poll calling `store.list()`/probing every due monitor on every tick with no pacing at all, or crashes startup outright — both worse than the existing fields' "fall back to a sane default" contract. Add a `@field_validator("scheduler_tick_seconds", mode="before")` (or extend `_fallback_invalid_int`'s field list, generalizing it to handle a float default) that falls back to the field default (5.0) for a non-positive or unparseable value, matching the exact contract already documented for the other three fields, plus a regression test in `backend/tests/test_config.py` (a `0`/negative/non-numeric `SCHEDULER_TICK_SECONDS` env value falls back to `5.0`, matching the existing `test_config_alert_threshold.py`-style boundary tests already in this suite). Deliberately stops short of adding any upper bound (an absurdly large tick interval is a deployer choice, not a crash risk). Tests: backend/tests/test_config.py. VERIFY: `cd backend && .venv/bin/pytest tests/test_config.py -v`. {cat:backend; size:S; multifile:no; research:none}

## Refill 2026-09-22 (backlog format bug found: EVERY prior "27B-decomposed" backlog entry for this repo was a bare comment header with zero real `- [ ] [T1]`-`[T5]` checkbox lines beneath it -- queue_refill.sh's own dry-check had been silently seeing 0 available items every single run since 2026-09-05, even on nights the header claimed work was queued; only the already_satisfied/credited pre-check path was ever crediting real work in OVERNIGHT_PROGRESS.md, this backlog file itself never actually fed the queue)
Re-verified all P1-P4 items above against the live tree: the P1 heartbeat-tick-loop wiring, the `_maybe_notify` TypeError crash, `Settings.max_results/max_monitors` wiring, the negative-env-var crash guard, `is_debounced_down`/severity/incident-id notifier wiring, `load_config_from_env`, and `percentile()`/`should_alert()` (models.py) are all confirmed LANDED already -- stale, no action needed. 8 gaps below are confirmed STILL OPEN via direct grep against the live tree and got real, granular T1/T2 backlog entries this pass (48 lines total) in backlog/shrike-monitor.md: checker.py's dead probe() notifier branch; scheduler.py's calculate_transition_elapsed/_calculate_elapsed_seconds dead pair; persistence.py/utils prune_threshold dead pair; four dead pure-math utils (clamp/ema/pct_change/round_sig -- mean_stdev.py was already deleted); worst_status/record_probe/verify_token/utils.uptime.uptime_ratio dead singles; backoff.py's misplaced-docstring bug; scheduler_tick_seconds's missing non-positive-value guard; humanize_bytes + humanize_count.py (distinct from the already-queued app/utils/auth.py verify_bearer_token item -- not touched here).

## Refuel 2026-09-22 round 3 (deep exhaustive pass — full re-read of every backend/app/*.py file against the live tree, cross-checked against all 6 prior refuel rounds to avoid duplicates; 12 fresh verified gaps, several correcting stale "already done" claims)

- [ ] [P1] [needs-decompose] Heartbeat monitors' incident_id is permanently `None`, which combines with Notifier's non-expiring dedup cache to silently kill ALL future alerts for that monitor after its first down/up cycle ever — `MonitorScheduler.tick()`'s incident-transition state-building loop (`backend/app/services/scheduler.py`, the `states.append(...)` block ~line 171-179) hardcodes `cf = 0` for every heartbeat monitor regardless of its real consecutive-failure count (confirmed by direct read; the http branch computes real `cf = consecutive_failures(results)` two lines above it). `evaluate_incident_transitions()` only ever creates a non-None `_incident_id` when `new_incident = (status == "down") and (failures >= threshold)` — since `failures` is hardcoded 0 for heartbeats, this is never True, so `self._incident_id[monitor_id]` never gets set away from `None` for any heartbeat monitor (verified: `backend/tests/test_scheduler.py::test_seed_baseline_computes_incident_state` even asserts this directly, comment: "heartbeat monitor: cf is always 0 for heartbeats, so no incident even if down"). Downstream, `_maybe_notify()` passes `incident_id=self._incident_id.get(monitor.id)` (always `None` for heartbeats) to `Notifier.notify_transition()`, which builds its dedup key via `alert_key(monitor_id, new_status, incident_id)` (`backend/app/utils/alert_key.py`) — with `incident_id=None` always treated as an empty part, this key collapses to a stable hash of just `monitor_id|status`. `Notifier._last_notified` (`backend/app/services/notifier.py` line 57) is a plain dict that **entries are never removed from** (confirmed by grep: `_last_notified` is only ever read at line 85 and written at line 115, no `del`/`pop`/expiry anywhere) — so once a heartbeat monitor has fired one "down" alert and one "up" alert (ever, in the process's lifetime), `key in self._last_notified` will be True for every subsequent occurrence of that exact (monitor_id, status) pair, permanently suppressing every future down/up notification for that monitor for the life of the process. This is the exact "two distinct incidents that both end in up must both be sent" bug the alert_key/incident_id feature was built to fix (see `test_two_distinct_incidents_...` in test_notifier.py) — but it is completely unfixed for heartbeat monitors specifically, the type this whole tool exists to dead-man's-switch. Confirmed via direct read of `test_scheduler.py`/`test_notifier.py` that no test drives a heartbeat monitor through two full down→up cycles against the real `Notifier` (only `_RecordingNotifier`/`_SpyNotifier` fakes are used in scheduler tests, and `test_notifier.py`'s incident_id tests only ever pass hand-written non-None incident_ids). Fix: give heartbeat monitors a real per-incident id too — e.g. compute `cf` for heartbeats the same way `_record_synthetic_result`'s own consecutive-miss tracking could (or simplify: any down→up transition for a heartbeat should count as `failures >= threshold` once, since a heartbeat's "down" state has no partial/debounced concept the way a flaky http probe does), so `evaluate_incident_transitions` actually assigns a fresh, non-None incident_id per real heartbeat outage. Add a regression test in `backend/tests/test_scheduler.py` and/or `test_notifier.py` driving a heartbeat monitor through two full down→up cycles against a real `Notifier` instance and asserting BOTH cycles produce a sent "up" notification (not just the first). {cat: backend; size: M; multifile: yes; research: none}

- [ ] [P1] [needs-decompose] `Store.create()`'s `StoreFull` exception is never caught anywhere in the API layer — `POST /monitors` crashes with a raw unhandled 500 instead of a clean error when `max_monitors` is reached, directly contradicting the 2026-09-18 grooming review's claim that "StoreFull → 507 handling" was "present and passing" — confirmed FALSE by direct re-read of the live tree. `backend/app/store.py`'s `Store.create()` (line ~101) does `raise StoreFull(...)` (a `RuntimeError` subclass, line 25) when `len(self._monitors) >= self.max_monitors`, but `backend/app/routers/monitors.py`'s `create_monitor()` handler calls `await store.create(...)` with no `try/except StoreFull` around it, and `backend/app/main.py` registers exactly one `@app.exception_handler` — for `ValidationError` — with nothing for `StoreFull` (confirmed via `grep -rn "StoreFull" backend/app`: only its definition and raise-site exist outside tests). FastAPI's default behavior for an unhandled exception is a bare 500 Internal Server Error with no useful detail, not the clean 4xx/507-style response a capacity-limit error should produce. Confirmed via `grep -rn "StoreFull" backend/tests`: the ONLY test exercising this is `test_core.py`'s direct `Store`-level `pytest.raises(StoreFull)` — there is no HTTP-level test proving `POST /monitors` behaves sanely (or even returns a parseable JSON error body) once the fleet is at capacity. Fix: register a `@app.exception_handler(StoreFull)` in `app/main.py` (matching the existing `ValidationError` handler's pattern) returning a 507 Insufficient Storage (or 429/503, whichever this repo's error-code conventions prefer) with a clear `detail` message, and add a regression test in `backend/tests/test_monitors_api.py` that fills the store to `max_monitors` and asserts the next `POST /monitors` returns the new clean status code instead of a raw 500. {cat: backend; size: S; multifile: yes; research: none}

- [ ] [P2] [needs-decompose] `StatusOut.uptime_percent` is a real, tested-for-shape schema field that the API never actually populates — always returns `0.0` regardless of real uptime — `backend/app/schemas.py`'s `StatusOut` has `uptime_percent: float = 0.0` (added in commit `c834c1d`, "Add uptime_percent field to StatusOut schema"), but `backend/app/routers/monitors.py`'s `_build_status_out()` (the sole place that constructs a `StatusOut`) only ever passes `uptime_ratio=up_ratio` — it never computes or passes `uptime_percent` at all, so every `GET /monitors/{id}/status` response reports `uptime_percent: 0.0` even for a monitor with 100% uptime. Confirmed via `grep -rn "uptime_percent" backend/app backend/tests`: the only test, `test_schemas.py::test_status_out_has_uptime_percent_field`, checks the field exists on the Pydantic model with the right type/default — no test in `test_monitors_status_api.py` or `test_monitors_api.py` ever asserts a real, non-zero `uptime_percent` value from a live endpoint call. This is a genuinely misleading always-wrong field on a shipped API response (worse than a merely-unused field, since API consumers reading `uptime_percent` today are silently lied to). Fix: compute `uptime_percent = round(up_ratio * 100, 1)` (or similar) in `_build_status_out()` from the already-computed `up_ratio`, and add a regression test in `backend/tests/test_monitors_status_api.py` asserting e.g. 3 ok + 1 fail results produce `uptime_percent == 75.0`, not `0.0`. {cat: backend; size: S; multifile: yes; research: none}

- [ ] [P3] [needs-decompose] `Settings.service_version` is completely unused in production, and `GET /health` exposes no version field at all, making it impossible to verify what build is actually live from the health endpoint alone — `backend/app/config.py`'s `service_version: str = "0.1.0"` field is read from the environment and covered by shape tests in `backend/tests/test_config.py`, but confirmed via `grep -rn "service_version" backend/app`: its only reference outside `config.py`/tests is nothing — `app/main.py` constructs the FastAPI `app` with `version=__version__` (from the separate, hardcoded `app/version.py` module, also `"0.1.0"` today but with no mechanism keeping the two in sync), and `app/schemas.py`'s `Health` model has `status`/`service`/`scheduler_running`/`last_tick` fields but no `version` field whatsoever. For an ops/deploy-verification tool whose whole purpose is catching silent failures in OTHER services, its own `/health` endpoint can't answer "what version is actually running" — exactly the kind of blind spot this repo exists to catch elsewhere. Fix: add a `version: str` field to the `Health` schema, populate it from `settings.service_version` (or `app.version.__version__`, whichever is intended as canonical — recommend consolidating to one source) in `app/main.py`'s `health()` handler, and add a regression test in `backend/tests/test_config.py` or a new health test asserting `GET /health`'s JSON body includes the running version. {cat: backend; size: S; multifile: yes; research: none}

- [ ] [P4] [needs-decompose] `routers/monitors.py::monitor_incidents()`'s docstring is stale and actively misleading — still claims heartbeat monitors "always return an empty list" for incidents, a claim its own test suite already disproves — the docstring (`backend/app/routers/monitors.py`, the `GET /{monitor_id}/incidents` handler) reads: "Only meaningful for http monitors - heartbeat monitors have no discrete result stream to derive incidents from ... and always return an empty list." This was accurate before the P1 "wire heartbeat down/up CheckResults into the actual tick loop" roadmap item landed (`MonitorScheduler._record_synthetic_result()` is now called from `tick()` on every heartbeat status transition), but is now directly contradicted by `backend/tests/test_results_incidents.py::test_heartbeat_incidents_derived_from_synthetic_results`, which proves a heartbeat monitor's incidents ARE derived correctly once synthetic results exist — and by `README.md` itself, which already correctly says "(HTTP and heartbeat monitors)" for this same endpoint. Only the docstring/inline comment in the router file was never updated, and a future reader (human or LLM) trusting the docstring over the actual behavior could easily reintroduce a heartbeat-incidents regression believing it "was never supposed to work." Fix: update the docstring in `monitor_incidents()` to match the current, correct, already-tested behavior (incidents ARE derived for both monitor types now that heartbeat monitors record synthetic CheckResults on each transition), matching the README's existing accurate wording. {cat: backend; size: S; multifile: no; research: none}

- [ ] [P4] [needs-decompose] `MonitorScheduler.get_current_incident_id()` is a dead public method — zero callers anywhere, not even in its own tests — `backend/app/services/scheduler.py`'s `get_current_incident_id(self, monitor_id: str) -> str | None` (line ~114) is a public accessor for `self._incident_id`, but confirmed via `grep -rn "get_current_incident_id" backend/app backend/tests` to have exactly zero references anywhere outside its own definition — no router exposes it, no test calls it (unlike every other "dead helper" already tracked on this roadmap, which at minimum have a dedicated test exercising them directly). `_incident_id` itself IS live and wired (read internally by `_maybe_notify()`), so this is a genuinely orphaned public-API surface on an otherwise-fully-wired internal dict, not a duplicate-implementation case like the rest of this roadmap's dead-code items. Delete `get_current_incident_id()` as unused surface area, or if incident-id visibility is wanted externally (e.g. surfaced on `StatusOut` or the incidents endpoint), wire it into an actual response instead of leaving it unreachable. {cat: backend; size: S; multifile: no; research: none}

- [ ] [P2] [needs-decompose] `downtime_seconds()` structurally cannot count an ongoing/current outage — `GET /monitors/{id}/status` undercounts downtime for any monitor that is down as of its most recent recorded check — `backend/app/utils/downtime.py`'s `downtime_seconds(checks: list[tuple[float, bool]])` sums the time span BETWEEN consecutive checks where the earlier one was down (`zip(ordered, ordered[1:])`), which means the trailing span after the LAST check in the list is never counted at all — confirmed directly by `backend/tests/test_downtime.py::test_single_check_returns_zero`, which asserts `downtime_seconds([(100.0, False)]) == 0.0` (a monitor with exactly one recorded down check reports zero downtime, even though it may have been down for hours since that check). `backend/app/routers/monitors.py`'s `_build_status_out()` calls `downtime_seconds([(r.checked_at, r.ok) for r in results])` with no `now` timestamp appended as a synthetic final checkpoint, so a monitor that is CURRENTLY down (its most recent result is a failure) reports a `downtime_seconds` value that excludes all downtime accrued since that last check — potentially hours of an ongoing outage silently missing from the one field meant to answer "how long has this been down." Fix: in `_build_status_out()` (or inside `downtime_seconds()` itself via an optional `now` parameter), append a synthetic `(now, current_status == "up")`-shaped final checkpoint before computing `downtime_seconds`, or otherwise account for the open-ended trailing span when the most recent result is down. Add a regression test in `backend/tests/test_monitors_status_api.py` proving a monitor whose most recent (and only) result is down, checked 300 seconds ago, reports a `downtime_seconds` close to 300 rather than 0. {cat: backend; size: S; multifile: yes; research: none}

- [ ] [P2] [needs-decompose] `latency_stats()` — a fully-built, already-correctly-wired-to-`percentile()`, and thoroughly (double-)unit-tested min/avg/max/p95 latency feature — has zero production callers anywhere; no endpoint exposes it — `backend/app/models.py`'s `latency_stats(results: list[CheckResult]) -> dict[str, float | None]` (line ~124) computes min/avg/max, and a proper linear-interpolation p95 via the real `app.utils.percentile.percentile()` (already imported at the top of `models.py` and used correctly here — this part of the "percentile duplicate" item elsewhere on this roadmap has ALREADY been resolved, just never surfaced). Confirmed via `grep -rn "latency_stats" backend/app backend/tests`: it is tested in BOTH `backend/tests/test_core.py` (6 cases: empty/all-none/ignores-non-ok/single/multiple/mixed) AND `backend/tests/test_models.py` (5 near-duplicate cases) — real, duplicated investment in a feature that has no caller in `app/routers/monitors.py`, no field on `StatusOut`, and nothing anywhere surfaces a monitor's latency distribution via the API. For a monitoring tool, "what's this endpoint's p95 latency" is a core, expected piece of status information — this is the single largest unwired feature found in this pass (vs. the many small dead pure-math helpers already tracked). Fix: add a `latency: dict[str, float | None] | None` (or four flat fields: `latency_min_ms`/`latency_avg_ms`/`latency_max_ms`/`latency_p95_ms`) to `StatusOut`, compute it via `latency_stats(results)` in `_build_status_out()` (guard `None`/omit for heartbeat monitors, which never have latency data), and add a regression test in `backend/tests/test_monitors_status_api.py` proving `GET /monitors/{id}/status` returns real latency stats for an http monitor with recorded results. While there, consider consolidating the duplicate `test_core.py`/`test_models.py` coverage into one file. {cat: backend; size: M; multifile: yes; research: none}

- [ ] [P3] [needs-decompose] `app/utils/jitter.py`'s `with_jitter()` is a fully-implemented, unit-tested, zero-caller dead utility — distinct from the already-tracked "four dead pure-math utils" (clamp/ema/pct_change/round_sig) and from backoff.py's OWN separate ad-hoc jitter reimplementation — `with_jitter(base: float, frac: float = 0.1, rand: float = 0.5) -> float` (`backend/app/utils/jitter.py`) scales a base value by a jitter factor in `[1-frac, 1+frac]` and is directly unit-tested in `backend/tests/test_utils.py` (`test_with_jitter_base`/`_bounds`/`_noop`), but confirmed via `grep -rn "with_jitter" backend/app backend/tests` to have zero callers anywhere outside those tests. Notably, `backend/app/services/notifier.py`'s retry loop imports and uses `calculate_backoff_delay()` from `app/utils/backoff.py` instead — which implements its OWN separate, differently-shaped jitter inline (`rng.uniform(0, capped_delay)`, an additive full-jitter scheme, not `with_jitter()`'s multiplicative scaling) rather than reusing this already-tested utility — the same "N implementations, only one (different) one is wired" pattern already established elsewhere on this roadmap (classify_status/http_status, uptime_pct/uptime_ratio), just not yet caught for this pair. Either delete `with_jitter()` and its tests as unused, or refactor `calculate_backoff_delay()` to build its jitter via `with_jitter()` instead of its own inline `rng.uniform()` call (note: this would change `calculate_backoff_delay()`'s returned range/distribution, so treat as a deliberate behavior change requiring updated test expectations in `test_backoff.py`, not a drop-in swap). {cat: backend; size: S; multifile: yes; research: none}

- [ ] [P3] [needs-decompose] `MonitorCreate` validates `type`/`url` consistency but not `type`/`token` consistency — a client can attach a `token` to an `http` monitor where it is silently meaningless — `backend/app/schemas.py`'s `MonitorCreate.check_url_rules()` model-validator enforces that `url` must be empty for heartbeat monitors and required (with an `http(s)://` prefix) for http monitors, but has no equivalent check for the `token: str | None` field. `backend/app/routers/monitors.py`'s `create_monitor()` handler does `elif body.token is not None: ping_token = body.token` unconditionally (regardless of `body.type`), so `POST /monitors {"type": "http", "url": "...", "token": "some-secret"}` silently stores that token as the monitor's `ping_token` even though `backend/app/routers/heartbeat.py::ping()` 400s any request against a non-heartbeat monitor before the token is ever checked — meaning that `ping_token` value can never be used or verified for an http monitor, a harmless-but-confusing bit of accepted-and-silently-ignored client input with no validation error to tell the caller their `token` had no effect. Confirmed via `grep -n "def test.*token" backend/tests/test_schemas.py backend/tests/test_monitors_api.py`: no test covers a `token` supplied alongside `type: "http"`. Fix: extend `check_url_rules()` (or add a sibling validator) to reject `token` being set when `type == "http"`, matching the existing "reject nonsensical field combinations early" pattern already used for `url`, with a regression test in `backend/tests/test_schemas.py` proving `MonitorCreate(type="http", url="https://x", token="abc")` raises a `ValidationError`. {cat: backend; size: S; multifile: yes; research: none}

- [ ] [P4] [needs-decompose] `aegis.py` — a 350-line, domain-unrelated Python code-editing-agent module sitting in the shrike-monitor repo root — has zero importers anywhere in the repo and no connection to the monitoring domain — confirmed via `grep -rn "import aegis\|from aegis" .` (repo root) returning nothing, and via reading its own module docstring ("lightweight, dependency-free Python agent for LLM-driven code editing... designed to be used by an LLM agent that receives instructions to modify code") — this describes a generic code-editing tool, not anything related to uptime/heartbeat monitoring, shrike-monitor's stated purpose. It is not referenced by `backend/app/`, not imported by any test, and not mentioned in `README.md`. This looks like a stray artifact (possibly copied in from tooling infrastructure work rather than intentionally added to this repo). Investigate whether it belongs here at all — if it is confirmed to be an accidental/misplaced file with no repo-specific purpose, delete it; if it is intentionally kept for some fleet-tooling reason, add a README note explaining why a monitoring-service repo carries an unrelated code-editing-agent module. {cat: backend; size: S; multifile: no; research: repo}

## Refuel 2026-09-25 (Claude research pass — roadmap was fully [decomposed]/dry)

**Status check (no new checkbox needed):** direct code read against `origin/develop @ fe7115d` (2026-09-25, current HEAD — the scratchpad's `roadmap.md`/`recent_git_log.txt` snapshot was itself stale by ~480 commits) confirms the 2026-09-22-round-3 items for `StoreFull` 507 handling, `uptime_percent`, `downtime_seconds`'s ongoing-outage undercount, `latency_p95_ms`, the `Health.version` field, `MonitorCreate` token/type validation, `get_current_incident_id()`, `record_probe()`, `calculate_transition_elapsed`/`_calculate_elapsed_seconds`, and the `monitor_incidents()` docstring are all now genuinely LANDED — verified directly in `backend/app/main.py`, `backend/app/schemas.py`, and `backend/app/routers/monitors.py`, not just via the checked boxes in `OVERNIGHT_PROGRESS.md`. `with_jitter()`/`jitter.py` is also confirmed fully deleted. Five fresh gaps below were found by re-reading the current tree end to end, including one item (checked-off in `OVERNIGHT_PROGRESS.md`) whose "done" status does not hold up against the live code.

- [ ] [P1] [ready] **[LIVE BUG — checked-off fix does not work]** The checked-off "heartbeat incident-id" fix (`feat:shrike-monitor-20260922-heartbeat-incident-id-dedup-bug`) does not actually work — dead-man's-switch monitors still go permanently silent after their first down/up cycle. `backend/app/services/scheduler.py`'s `tick()` (incident-states loop) and `seed_baseline()` both still hardcode `cf = 0` for every heartbeat monitor even though a `"threshold": 1 if monitor.type == "heartbeat" else self.config.alert_threshold` override was added alongside them — since `0 >= 1` is always False, `evaluate_incident_transitions()` never assigns a heartbeat monitor a real (non-`None`) incident id. `Notifier._last_notified` (`backend/app/services/notifier.py`) never expires an entry (confirmed via grep: no `del`/`pop`/TTL anywhere), so `alert_key(monitor_id, status, None)` collapses every future down/up cycle onto the same key, silently suppressing every notification after the first for the life of the process — the exact scenario the incident-id feature exists to prevent. The regression test written to prove this fixed, `backend/tests/test_scheduler.py::test_heartbeat_two_cycles`, actually works around the bug instead of exercising it: it manually calls `notifier._last_notified.clear()` between its two cycles with the comment "ensure the second cycle's notifications are not suppressed." A second test, `test_seed_baseline_computes_incident_state`, still asserts the pre-fix behavior (`sched._incident_state[mon_hb.id] is False`, comment "cf is always 0 for heartbeats, so no incident even if down") and is flagged `[AUTO-SKIP ... route to CLAUDE]` in `OVERNIGHT_PROGRESS.md`, never picked up. Fix: compute a real per-heartbeat failure signal in both `tick()` and `seed_baseline()` (e.g. `1 if status == "down" else 0` — the same substitution `_maybe_notify()`'s own `is_debounced_down()` call already uses correctly two blocks away), then delete the `_last_notified.clear()` workaround from `test_heartbeat_two_cycles` and fix `test_seed_baseline_computes_incident_state`'s stale assertion. {cat: backend; size: M; multifile: yes; research: none}

- [ ] [P2] [ready] `backend/app/utils/auth.py` — already deleted once as dead code (2026-09-21), has been recreated by a later fleet cycle and is dead *and* buggy again: `verify_bearer_token` is now defined twice at module scope (line 10: `(provided: str, expected: str) -> bool`; line 45: `(request: Request, expected_token: str) -> bool`, a completely different signature) and the second silently shadows the first. `verify_bearer_token_from_request()` (line 21-37) calls `verify_bearer_token(parts[1], expected_token)` with two plain strings, which — because of the shadowing — resolves to the Request-typed second definition, so calling it would raise `AttributeError: 'str' object has no attribute 'headers'`; this is currently unreachable only because nothing in `backend/app/` imports anything from this module at all (confirmed via grep — the real, correct gate is still `app.auth.require_api_token`, `Depends(require_api_token)` on `backend/app/routers/monitors.py:28`, untouched). This is the file's third go-round with the same "orphaned auth duplicate" shape, and the fleet already tried and failed twice to just delete it — `OVERNIGHT_PROGRESS.md` shows `[AUTO-SKIP staged: 27B could not land this (beyond it) — route to CLAUDE] ... Delete the entire file` (and its companion test-removal item) sitting unpicked, which is presumably how a second, worse copy got piled on top without anyone noticing. Route this directly to a human/Claude session rather than a third fleet decompose pass: delete `backend/app/utils/auth.py` in full, delete any test exercising it, and re-grep to confirm it stays gone this time. {cat: backend; size: S; multifile: yes; research: none}

- [ ] [P2] [ready] `DELETE /monitors/{id}` never tells the running `MonitorScheduler`/`Notifier` to forget that monitor, leaking memory across the life of a long-running process. `backend/app/routers/monitors.py`'s `delete_monitor()` only calls `store.delete(monitor_id)`, and `Store.delete()` (`backend/app/store.py:126-132`) only pops `_results`/`_monitors`. `MonitorScheduler.__init__` (`backend/app/services/scheduler.py`) owns six plain `dict[str, ...]` attributes keyed by monitor id (`_last_checked_at`, `_last_status`, `_incident_state`, `_incident_id`, `_last_status_change_at`, `_consecutive_failures`) with no cleanup method anywhere in the class (confirmed via grep: zero `.pop(`/`del ` calls against any of them), and `Notifier._last_notified` has the same shape. For a `MAX_MONITORS`-bounded, "public-ish write API" (per this repo's own `AGENTS.md`) that creates and deletes monitors over time, every delete leaves behind permanently-orphaned bookkeeping — the exact resource-growth `MAX_MONITORS` is supposed to guard against, just moved one layer down. Fix: add `MonitorScheduler.forget(monitor_id)` that pops the id from all six scheduler dicts, call it from `delete_monitor()` right after a successful `store.delete()`, and add a regression test proving a deleted monitor's id is gone from the scheduler's internals after the next tick. (`Notifier._last_notified` is keyed by a hashed `alert_key(...)`, not directly by monitor id — leave it as a flagged follow-on rather than trying to invalidate it in this pass.) {cat: backend; size: M; multifile: yes; research: none}

- [ ] [P3] [ready] `MonitorScheduler.seed_baseline()` never sets `_incident_id` for a monitor that's already mid-incident across a restart, a narrower instance of the same "incident_id stays None" bug as the heartbeat item above. `seed_baseline()` (`backend/app/services/scheduler.py`) computes `new_incident = (status == "down") and (cf >= effective_threshold)` and stores it in `_incident_state`, but never assigns `_incident_id` — that only ever happens inside `tick()`'s `for t in transitions:` loop, which only fires on a state *flip* (`was_incident != new_incident`). A monitor whose outage started before a restart and is still ongoing after it therefore keeps `_incident_id` unset/`None` for the entire remainder of that incident, under-populating `Notifier.notify_transition()`'s `incident_id` argument for exactly the outages where alerting continuity across a restart matters most. Fix: in `seed_baseline()`, when `new_incident` is True, also assign `self._incident_id[monitor.id] = f"inc-{monitor.id}-{int(now)}"` (matching `tick()`'s own id format), and add a regression test that seeds a `Store` with a monitor already down past threshold, calls `seed_baseline()`, and asserts `scheduler._incident_id[mon.id] is not None`. {cat: backend; size: S; multifile: no; research: none}

- [ ] [P4] [ready] **[repeatedly failed automated decomposition — route to human/Claude, not another 27B pass]** `backend/app/utils/humanize.py`'s `humanize_bytes()`/`humanize_count()` are still fully present with zero callers anywhere outside their own definitions (confirmed via grep against `origin/develop @ fe7115d`), despite being the single most-recovered, least-resolved item in this repo's own fleet history: `OVERNIGHT_PROGRESS.md` shows this exact file escalated as `recovery:escalated` FOUR separate times ("has been recovered/decomposed 3 times (cap 2) without ever landing," immediately followed by "...4 times (cap 2) without ever landing... needs a human/Claude session to understand the actual blast radius") with no human/Claude session ever picking it up. Given three-plus automated attempts have already failed on this two-function, dependency-free file for reasons the fleet's own log admits it can't diagnose, do not queue a fifth pass through the normal pipeline — hand this directly to a human/Claude session to delete `humanize_bytes()`/`humanize_count()` and their `backend/tests/test_utils.py` coverage in one pass, and, while there, work out what about this specific file keeps defeating automated decomposition so the pattern doesn't recur a fifth time. {cat: backend; size: S; multifile: yes; research: repo}

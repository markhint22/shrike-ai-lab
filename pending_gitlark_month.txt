- [ ] [MED] `backend/tests/test_repo_analyzer_package_json.py` — Add a new pytest file unit-testing app.services.repo_analyzer.RepoAnalyzer._parse_package_json(self, content, filename): instantiate RepoAnalyzer() (no token), cover runtime dependencies + devDependencies both mapped with correct type, missing both keys -> [], and invalid JSON string -> [] (no raise). Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_repo_analyzer_requirements.py` — Add a new pytest file unit-testing app.services.repo_analyzer.RepoAnalyzer._parse_requirements_txt(self, content, filename): cover # comment lines and blank lines skipped, pkg==1.2.3 captures name+version specifier, a bare pkg yields version=None, and trailing whitespace stripped. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_repo_analyzer_pipfile.py` — Add a new pytest file unit-testing app.services.repo_analyzer.RepoAnalyzer._parse_pipfile(self, content, filename): cover only lines inside the [packages] section parsed, a following [dev-packages]/[requires] section ending capture, name = "*" splits name/version, and content with no [packages] -> []. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_repo_analyzer_gemfile.py` — Add a new pytest file unit-testing app.services.repo_analyzer.RepoAnalyzer._parse_gemfile(self, content, filename): cover gem 'rails' single-quote and gem "puma" double-quote captured, non-gem lines (source, comments, group) ignored, and empty content -> []. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_repo_analyzer_pom_xml.py` — Add a new pytest file unit-testing app.services.repo_analyzer.RepoAnalyzer._parse_pom_xml(self, content, filename): cover multiple <artifactId> matches each become a Dependency with type="runtime", content with zero <artifactId> -> [], and file field set to filename. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_repo_analyzer_cargo_toml.py` — Add a new pytest file unit-testing app.services.repo_analyzer.RepoAnalyzer._parse_cargo_toml(self, content, filename): cover deps captured only inside the [dependencies] section, other tables ([package], [dev-dependencies]) excluded, and empty content -> []. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_repo_analyzer_find_dep_files.py` — Add a new pytest file unit-testing app.services.repo_analyzer.RepoAnalyzer._find_dependency_files(self, dependencies): build a few Dependency objects sharing/differing file, assert the result is the de-duplicated set of filenames, and an empty list -> []. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_diff_parser_hunk_props.py` — Add a new pytest file unit-testing the DiffHunk dataclass property branches in app.services.diff_parser (added_lines, removed_lines, context_lines, is_new_file, is_deleted_file): construct hunks from mixed +/-/space lines and assert each property filters correctly, plus is_new_file/is_deleted_file true/false cases. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_distributed_tracing.py` — Add a new pytest file unit-testing app.services.distributed_tracing Span.add_tag, Span.log_event, and Tracer.get_trace(trace_id): assert tags/events accumulate on the span and that get_trace returns only in-memory spans whose trace_id matches (and []/empty spans for an unknown id). Pure in-memory, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_code_analysis_empty.py` — Add a new pytest file unit-testing empty/degenerate-input branches of app.services.code_analysis.CodeAnalyzer (async: detect_code_patterns, suggest_refactoring, calculate_complexity): empty string and whitespace-only code return empty/neutral results without raising, and syntactically invalid code is handled gracefully. Use pytest.mark.asyncio. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_code_quality_suggest_refactoring.py` — Add a new pytest file unit-testing app.services.code_quality_metrics.CodeQualityMetrics.suggest_refactoring(self, code) (async): a long/deeply-nested/many-param snippet yields concrete suggestion strings, clean short code yields none, and empty string returns [] without raising. Use pytest.mark.asyncio. Pure, no DB/IO. One file.
- [ ] [MED] `backend/tests/test_validation_service_edges.py` — Add a new pytest file unit-testing edge branches of app.services.validation_service.ValidationService.sanitize_string(text, max_length) and validate_required_fields(data, required): sanitize strips/truncates at boundary and handles empty string, and required-fields returns the (bool, missing_list) tuple for all-present, some-missing, and empty-required cases. Pure, no DB/IO. One file.
- [ ] [MED] `backend/app/services/webhook_events.py` — Create a NEW pure module classify_webhook_event(event: str, action: str | None) -> dict that maps a GitHub webhook (X-GitHub-Event name + payload action) to {category, should_trigger_review} for the GitHub App auto-review loop (e.g. pull_request+opened/synchronize/reopened -> review; closed/labeled -> no; unknown event -> {"category":"ignored","should_trigger_review":False}). No DB/IO. Add tests/test_webhook_events.py covering trigger actions, non-trigger actions, and unknown events. One file.
- [ ] [MED] `backend/app/services/webhook_signature.py` — Create a NEW pure module compute_signature(payload: bytes, secret: str) -> str and verify_signature(payload: bytes, secret: str, header: str) -> bool that implement GitHub's sha256= HMAC webhook signature using stdlib hmac/hashlib with constant-time compare. No DB/IO/network. Add tests/test_webhook_signature.py covering a known-good signature round-trip, wrong secret -> False, malformed/empty header -> False. One file.
- [ ] [MED] `backend/app/services/severity_mapping.py` — Create a NEW pure module severity_rank(level: str) -> int and map_finding_severity(finding_type: str) -> str that normalize AST-analysis finding types (e.g. eval_use, sql_injection, unused_import) to a severity level and give an ordering rank (critical>high>medium>low>info) for sorting review findings. No DB/IO. Add tests/test_severity_mapping.py covering known types, unknown type -> a safe default, case-insensitivity, and rank ordering. One file.
- [ ] [MED] `backend/app/services/tier_evaluator.py` — Create a NEW pure module evaluate_tier_usage(tier: str, tokens_used: int, conversations_used: int, limits: dict) -> dict that returns {within_limits, over_tokens, over_conversations, tokens_remaining, conversations_remaining} for the Stripe usage/quota roadmap, treating an unknown tier via a safe default and negative/None inputs as 0. No DB/IO (limits passed in). Add tests/test_tier_evaluator.py covering under-limit, at-limit boundary, over both limits, and unknown tier. One file.
- [ ] [MED] `backend/app/services/inline_comments.py` — Create a NEW pure module build_inline_comments(findings: list[dict]) -> list[dict] that converts structured review findings ({file,line,severity,message,suggestion}) into GitHub review-comment objects {path, line, body} (severity-prefixed body, optional suggestion block), skipping findings missing file/line. No DB/IO. Add tests/test_inline_comments.py covering a full finding, a finding without a suggestion, and skipped invalid findings. One file.
- [ ] [MED] `backend/app/services/hunk_summary.py` — Create a NEW pure module summarize_diff(patch: str) -> dict (a PR-diff hunk summarizer for the review loop) that returns per-file {additions, deletions, net} plus a totals block by scanning unified-diff @@/+/- lines (ignoring +++/--- headers). No DB/IO. Add tests/test_hunk_summary.py covering a single-file patch, a multi-file patch, and an empty patch -> zeros. One file.
- [ ] [MED] `backend/app/services/language_detect.py` — Create a NEW pure module detect_language(filename: str) -> str | None that maps a filename/extension to a language name (.py->python, .ts/.tsx->typescript, .go->go, Dockerfile->dockerfile, etc.) to route files into the AST code-analysis engine. No DB/IO. Add tests/test_language_detect.py covering several extensions, an extensionless known name, uppercase extension, and an unknown extension -> None. One file.
- [ ] [MED] `backend/app/services/secret_redaction.py` — Create a NEW pure module redact_secrets(text: str) -> str (a security helper) that masks GitHub tokens (ghp_/github_pat_), AWS access keys (AKIA...), and Authorization: Bearer <token> values in log/review text, leaving surrounding text intact. Pure regex, no DB/IO. Add tests/test_secret_redaction.py covering each token type, multiple secrets in one string, and clean text unchanged. One file.
- [ ] [MED] `backend/app/services/chat_transcript.py` — Create a NEW pure module format_transcript(messages: list[dict]) -> str (an agent-chat formatter) that renders [{role, content}] into a readable Markdown transcript with role headers, preserving order and skipping empty-content messages. No DB/IO. Add tests/test_chat_transcript.py covering a user/assistant exchange, empty list -> empty string, and an unknown role handled gracefully. One file.
- [ ] [MED] `backend/app/services/tag_normalizer.py` — Create a NEW pure module normalize_tags(tags: list[str], max_tags: int = 10) -> list[str] for the snippet service that lowercases, strips, drops empties, de-duplicates preserving first-seen order, and caps at max_tags. No DB/IO. Add tests/test_tag_normalizer.py covering dedupe+case, whitespace/empty filtering, cap enforcement, and empty input -> []. One file.
- [ ] [MED] `backend/app/services/commit_message_parse.py` — Create a NEW pure module parse_commit_message(message: str) -> dict that parses a Conventional Commit subject into {type, scope, description, breaking} (handles feat(scope)!: ..., missing scope, BREAKING CHANGE: footer, and non-conforming subjects -> type=None). No DB/IO. Add tests/test_commit_message_parse.py covering typed+scoped, no-scope, breaking !, and free-form message. One file.
- [ ] [MED] `backend/app/services/code_fence.py` — Create a NEW pure module extract_code_blocks(markdown: str) -> list[dict] and wrap_code_block(code: str, lang: str = "") -> str (agent-chat/response formatter helpers) that pull fenced code blocks out of Claude responses (returning {language, code}) and wrap raw code back into a fence. No DB/IO. Add tests/test_code_fence.py covering a language-tagged block, an untagged block, multiple blocks, and no fences -> []. One file.
- [ ] [MED] `backend/app/services/diff_stats_format.py` — Create a NEW pure module format_diff_stats(additions: int, deletions: int) -> str that renders a compact +N -M badge string (net-zero, additions-only, deletions-only, and clamps negatives to 0). No DB/IO. Add tests/test_diff_stats_format.py covering all four branches. One file.
- [ ] [MED] `backend/app/services/review_score.py` — Create a NEW pure module aggregate_severity_score(findings: list[dict]) -> dict that rolls a findings list into {score, rating, counts_by_severity} (weight critical/high/medium/low, map score to a PASS/WARN/FAIL rating) for the code-review service. No DB/IO. Add tests/test_review_score.py covering empty findings -> perfect PASS, a critical -> FAIL, and mixed medium/low -> WARN. One file.
- [ ] [MED] `backend/app/services/pr_label_classifier.py` — Create a NEW pure module classify_pr_size(additions: int, deletions: int) -> str that returns a size label (XS/S/M/L/XL) from total changed lines using standard thresholds, clamping negatives to 0. No DB/IO. Add tests/test_pr_label_classifier.py covering each band boundary and the zero-change case. One file.
- [ ] [MED] `backend/app/services/auto_merge.py` — Create a NEW pure module can_auto_merge(findings: list[dict], checks_passed: bool) -> dict for the GitHub App auto-review loop that returns {allowed, reasons} — blocked when any finding severity is critical/high or checks_passed is False, allowed otherwise. No DB/IO. Add tests/test_auto_merge.py covering clean+checks-passed -> allowed, a high finding -> blocked, and checks-failed -> blocked. One file.
- [ ] [MED] `backend/app/services/usage_period.py` — Create a NEW pure module current_billing_period(now: datetime, reset_day: int = 1) -> tuple[datetime, datetime] for Stripe usage tracking that computes the [start, end) of the billing window containing now, clamping reset_day to a valid day for short months. Uses stdlib datetime only, no DB/IO. Add tests/test_usage_period.py covering mid-month, exact reset day, and a Feb-clamped reset_day=31. One file.
- [ ] [MED] `backend/app/services/ast_node_kind.py` — Create a NEW pure module classify_node_kind(node_type: str) -> str for the AST code-analysis engine that maps a Python AST node type name (FunctionDef, If, For, Call, Import, ...) to a category (definition/control_flow/call/import/other) as a string lookup (no ast import at runtime). No DB/IO. Add tests/test_ast_node_kind.py covering each category and an unknown node type -> other. One file.

# Supervisor digest — 2026-08-31 03:00

## Actionable (5)
- branch-hygiene flagged: gitlark — feature->main gate not green.
- branch-hygiene flagged: iptv_apps — feature->main gate not green.
- branch-hygiene flagged: shrike-labs-website — feature->main gate not green.
- DRIFT: gitlark overnight/feature is 58 commits ahead of main — consider a salvage-merge.
- Local-27B review flagged commit(s) — see supervisor report

## Local-27B review
billwatch@bcf96d3356b605f4e08d9f067a8cb46f0e0f5972: The commit message claims to auto-credit items touched by a "green change," but the diff only updates the progress tracker (`OVERNIGHT_PROGRESS.md`) without including the actual code change (the type hint addition in `bill_alert_service.py`) in the same commit. While the code change exists in a separate commit (`3339052`), the "auto-credit" commit is logically dependent on it but presented as a standalone chore, which is a minor process inconsistency but not a code bug. However, looking closer at the other commits:

iptv_apps@11e1384530b2720f30224b84a53e5bdf79cb6441: The commit message says "filter blocked streams by id instead of stream_id". The diff changes `b.stream_id !== streamId` to `b.id !== streamId`. In the same file, the `isStreamBlocked` function (visible in context of other commits or typical patterns) uses `b.stream_id`. If the API returns objects with `stream_id` (as implied by the previous line `b.stream_id !== streamId` being the "bug" or the "fix" depending on the data model), changing it to `b.id` might be incorrect if the object doesn't have an `id` field or if `id` is different from `stream_id`. However, without seeing the data model, this is ambiguous. Let's look for a clearer issue.

shrike-labs-website@28b28b16eb88f3e0ee0c148fa8558fe2fbccbf64: The commit message says "split vendor chunks... splitting `vue`, `vue-router`, and `marked` into a vendor chunk". The diff creates a `vendor-router` chunk for `vue-router` and a `vendor` chunk for everything else in `node_modules`. This effectively puts `vue` and `marked` into `vendor` and `vue-router` into `vendor-router`. This matches the description reasonably well (splitting them).

Let's re-evaluate `iptv_apps@11

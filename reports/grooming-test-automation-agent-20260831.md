# Grooming proposal — test-automation-agent — 2026-08-31 04:01

## Current Next Steps
```
- [x] [HIGH] `backend/app/services/llm_service.py` — `analyze_screenshot` lost its `def` line, so its body is orphaned inside `_first_text` (dead code after that method's `return ""`) and `LLMService.analyze_screenshot` doesn't exist (callers hit AttributeError). Insert exactly one line `    async def analyze_screenshot(self, base64_image: str, prompt: str) -> str:` immediately after the `        return ""` that ends `_first_text` and immediately before the orphaned `        """` docstring, so the indented block becomes the method body. One file.
- [x] [HIGH] `backend/app/agents/orchestrator.py` — line 11 `VALID_ACTIONS = {...}` omits `"hover"` though `executor._execute_step` supports hover, so `parse_test_plan` rejects valid hover steps. Add `"hover"` to that set literal. One file.
- [x] [MED] `backend/app/agents/executor.py` — in `_validate_assertion`, add a new branch just before the final `else`: `elif assertion.startswith("element_hidden:"):` extract selector, `is_hidden = await page.locator(selector).is_hidden()`, raise `AssertionError` when `not is_hidden`. Purely additive (mirrors `element_visible:`). One file.
- [x] [MED] `backend/app/routers/webhooks.py` — `get_webhook` has a duplicated dead block: the `if not webhook:` 404 and ownership 403 checks appear twice. Delete the redundant second pair before the `return`. Behavior unchanged. One file.
- [x] [MED] `backend/app/main.py` — `health`, `status`, `metrics`, `root` each return a dict but have no return annotation. Add `-> dict` to each of these four `async def` signatures. One file.
- [x] [MED] `backend/app/services/notification_service.py` — `EmailNotificationConfig.smtp_port: int = 587` has no bounds. Add `Field` to the pydantic import and change to `smtp_port: int = Field(587, ge=1, le=65535)`. One file.
- [x] [MED] `backend/app/routers/webhooks.py` — `WebhookCreate.url: str` has no length bound. Add `Field` to the import and change to `url: str = Field(..., max_length=2048)`. One file.
- [x] [LOW] `backend/app/agents/executor.py` — `_execute_step` has its docstring written twice on consecutive lines. Delete the second identical docstring line. One file.
- [x] [LOW] `backend/app/agents/optimizer.py` — `SelectorOptimizer.__init__` has no return annotation. Add `-> None` to its signature. One file.
- [x] [LOW] `backend/app/agents/analyzer.py` — `FailureAnalyzer.__init__(self, context, llm_service)` has no return annotation. Add `-> None`. One file.
- [x] [HIGH] `backend/app/agents/executor.py`: in `_validate_assertion`, add a new branch `element_disabled:` immediately before the final `else: raise ValueError`. Mirror `element_hidden:`: parse selector, `is_disabled = await page.locator(selector).is_disabled()`, raise `AssertionError(f"Element not disabled: {selector}")` when not disabled. Additive; don't modify existing branches. One file.
- [x] [HIGH] `backend/app/routers/test_runs.py`: change `CreateTestRunRequest.name: str` to `name: str = Field(..., min_length=1, max_length=200)`. `Field` already imported. Only that field. One file.
- [x] [MED] `backend/app/routers/auth.py`: add bounds to `LoginRequest`. Change the import to `from pydantic import BaseModel, Field`, then `customer_id: str` -> `customer_id: str = Field(..., max_length=128)` and `api_key: str` -> `api_key: str = Field(..., max_length=256)`. Don't modify `TokenResponse`/`UserResponse`. One file.
- [x] [MED] `backend/app/routers/webhooks.py`: change `WebhookCreate.events: List[str]` to `events: List[str] = Field(..., min_length=1)`. `Field` already imported. Only that line; leave `url`/`secret`. One file.
- [x] (already-satisfied in code, implement-verified) [MED] `backend/app/services/notification_service.py`: in `WebhookSubscription`, change `url: str` to `url: str = Field(max_length=2048)`. `Field` already imported. Only that field; don't touch `NotificationPayload`/`EmailNotificationConfig`. One file.
- [x] [MED] `backend/app/services/llm_service.py`: harden `generate_selector_strategies` — inside the existing `try`, replace `return json.loads(text)` with `parsed = json.loads(text)` then `return parsed if isinstance(parsed, list) else []`. Keep the `except Exception:` returning `[]`. Don't modify `analyze_error`/`validate_assertion`. One file.
- [x] [MED] {py·typing·T2·test-covered} `backend/app/core/database.py`: fix `get_db` return annotation. Add `from typing import AsyncGenerator` and change `async def get_db() -> AsyncSession:` to `async def get_db() -> AsyncGenerator[AsyncSession, None]:`. Don't change `init_db`/engine setup. One file.
- [x] (already-satisfied in code, implement-verified) [MED] {py·typing·T3·test-covered} `backend/app/agents/orchestrator.py`: fix implicit-Optional on `parse_test_plan`. Change `raw_yaml: str = None` to `raw_yaml: Optional[str] = None` (`Optional` already imported). Only that signature. One file.
- [x] [MED] {py·other·T2·test-covered} `backend/app/routers/uploads.py`: in `upload_test_plan`, immediately after `contents = await file.read()` add `if len(contents) > 5_000_000: raise HTTPException(status_code=413, detail="File too large (max 5MB)")`. The existing `except HTTPException: raise` re-raises it. Don't change YAML parsing. One file.
- [x] [MED] {py·other·T2·test-covered} `backend/app/agents/analyzer.py`: in `categorize_error`, add `elif "net::err" in error_message.lower(): return "network"` before the final `else: return "other"`. Additive. One file.
- [x] [MED] {py·other·T2·test-covered} `backend/app/agents/optimizer.py`: in `find_element`, inside the `for strategy in strategies:` loop, right after `selector = strategy.get("selector")` add `if not selector: continue`. Don't change `find_best_selector`/`learn_pattern`. One file.
- [x] [LOW] {py·typing·T1·test-covered} `backend/app/core/config.py`: change `Settings.__init__(self, **data):` to `def __init__(self, **data) -> None:`. Only that signature. One file.
- [x] [LOW] {py·other·T1·test-covered} `backend/app/main.py`: in the `metrics` endpoint, `os` is already imported at module top, so change the in-function `import psutil, os` to `import psutil`. Only that line. One file.
- [x] [LOW] {py·typing·T1·test-covered} `backend/app/routers/websocket.py`: add a return hint to `websocket_test_run` — change `async def websocket_test_run(websocket: WebSocket, run_id: str):` to end with `-> None:`. Only that signature. One file.
- [x] [LOW] {py·typing·T1·test-covered} `backend/app/agents/__init__.py`: change `AgentContext.__post_init__(self):` to `def __post_init__(self) -> None:`. Only that signature. One file.
- [x] [HIGH] {py·other·T2·test-covered} `backend/app/agents/__init__.py` — give the `AgentContext` dataclass defaults for `autonomy_level='phase-based'`, `autonomy_minutes=5`, `llm_tier='starter'` so construction without them succeeds (fixes ~6 failing executor tests). One file.
- [x] [HIGH] {py·test·T2·test-covered} `backend/tests/test_config.py` — change `from backend.app.core.config import Settings` to `from app.core.config import Settings` to fix the collection ModuleNotFoundError that halts the whole suite. One file.
- [x] [HIGH] {py·typing·T2·test-covered} `backend/tests/test_main_type_hints.py` — replace the four `inspect.get_full_annotations(...)` calls with `inspect.get_annotations(...)` (the real stdlib function). One file.
- [x] [MED] {py·other·T2·test-covered} `backend/app/core/config.py` — add settings fields with defaults: `artifact_dir='artifacts'`, `max_step_retries=2`, `capture_video=False`, `capture_trace=False` (config hooks for artifact/heal work). One file.
- [x] [MED] {py·test·T2·test-covered} `backend/app/services/notification_service.py` — add `RUN_STARTED`, `RUN_PAUSED`, `TEST_RETRIED`, `HEALING_APPLIED` members to the `NotificationEvent` enum. One file.
```

## Proposed (LLM re-evaluation — review before applying)
1. [HIGH] `backend/tests/test_config.py` — Verify the import fix (`from app.core.config import Settings`) is present and the file collects without `ModuleNotFoundError`; if the fix is missing, apply it to unblock the test suite.
2. [HIGH] `backend/app/agents/__init__.py` — Verify `AgentContext` dataclass has defaults for `autonomy_level`, `autonomy_minutes`, and `llm_tier`; if missing, add them to fix executor test failures.
3. [MED] `backend/app/agents/executor.py` — Verify the `element_disabled:` branch exists in `_validate_assertion` before the final `else`; if missing, add the branch mirroring `element_hidden:` logic.
4. [MED] `backend/app/agents/orchestrator.py` — Verify `"hover"` is included in the `VALID_ACTIONS` set; if missing, add it to allow valid hover steps in test plans.
5. [MED] `backend/app/services/llm_service.py` — Verify `analyze_screenshot` has a proper `async def` signature and `generate_selector_strategies` validates JSON output as a list; if missing, apply the fixes to prevent `AttributeError` and type errors.
6. [MED] `backend/app/routers/webhooks.py` — Verify `WebhookCreate` has `url` (max_length=2048) and `events` (min_length=1) constraints and that the duplicated 404/403 block in `get_webhook` is removed; if missing, apply the cleanup and validation.
7. [LOW] `backend/app/core/database.py` — Verify `get_db` return annotation is `AsyncGenerator[AsyncSession, None]` and `AsyncGenerator` is imported; if missing, update the signature and import.
8. [LOW] `backend/app/routers/auth.py` — Verify `LoginRequest` fields `customer_id` and `api_key` have `Field` length constraints; if missing, add `max_length` bounds to prevent oversized inputs.
9. [LOW] `backend/app/main.py` — Verify `health`, `status`, `metrics`, and `root` endpoints have `-> dict` return annotations and that `metrics` does not redundantly import `os`; if missing, add annotations and clean up the import.

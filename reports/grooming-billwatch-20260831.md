# Grooming proposal — billwatch — 2026-08-31 04:00

## Current Next Steps
```
- [x] [HIGH] `billwatch-web/src/views/ProfileView.vue` — the external link at line 146 (`<a href="https://www.congress.gov" target="_blank" ...>`) has no `rel`. Add `rel="noopener"` to that single `<a>` tag. One file.
- [x] [HIGH] `billwatch-web/src/views/BillDetailView.vue` — the Congress.gov link at line 192 (`:href="bill.url"` with `target="_blank"`) has no `rel`. Add `rel="noopener"` to that single `<a>` tag. One file.
- [x] [HIGH] `billwatch-web/src/views/LegislatorDetailView.vue` — two external links with `target="_blank"` and no `rel`: line 144 (`legislator.website`) and line 156 (twitter). Add `rel="noopener"` to each of those two `<a>` tags. One file.
- [x] [HIGH] `billwatch-backend/app/services/legislative_priority_service.py` — three serialized OUTPUT timestamps use naive `datetime.utcnow().isoformat()`: line 53 (`updated_at`), 137 (`recorded_at`), 187 (`created_at`). Change each to `datetime.now(timezone.utc).isoformat()` and update the import on line 5 to `from datetime import datetime, timedelta, timezone`. One file.
- [ ] [HUMAN-ONLY BLOCKED ITEM — auto-skipped after 3 failed cycles; needs a human] [HIGH] `billwatch-backend/app/services/trending.py` — four serialized OUTPUT timestamps use naive `datetime.utcnow().isoformat()`: lines 113, 334, 356, 386. Change each to `datetime.now(timezone.utc).isoformat()` and update the import on line 5 to `from datetime import datetime, timedelta, timezone`. DO NOT touch lines 140/235 (`cutoff = datetime.utcnow() - timedelta(...)` window comparisons — keep naive). One file.
- [x] [MED] `billwatch-backend/app/services/legislator_stats_service.py` — line 136 `"calculated_at": datetime.utcnow().isoformat()` is serialized output. Change to `datetime.now(timezone.utc).isoformat()` and update the import on line 6 to `from datetime import datetime, timedelta, timezone`. One file.
- [x] [MED] `billwatch-backend/app/routers/legislator_search.py` — line 60 `limit: int = Query(50)` has no bounds. Change to `limit: int = Query(50, ge=1, le=100)`. One file.
- [x] [MED] `billwatch-backend/app/routers/advanced_analytics.py` — line 16 `days: int = Query(30)` unbounded. Change to `days: int = Query(30, ge=1, le=365)`. One file.
- [x] [MED] `billwatch-backend/app/routers/activity_logs.py` — line 16 `limit: int = Query(100, le=500)` and line 27 `days: int = Query(30)`. Change line 16 to `Query(100, ge=1, le=500)` and line 27 to `Query(30, ge=1, le=365)`. One file.
- [x] [MED] `billwatch-backend/app/routers/recommendations.py` — four `limit` params have upper but no lower bound (lines 18, 29, 40 `Query(10, le=50)`, line 51 `Query(5, le=20)`). Add `ge=1` to each. One file.
- [x] [MED] `billwatch-backend/app/routers/search.py` — four `limit` params `Query(50, le=100)` (no lower bound) on lines 21, 34, 45, 59. Add `ge=1` so each is `Query(50, ge=1, le=100)`. Leave `q` untouched. One file.
- [x] [LOW] `billwatch-web/src/pages/BillRankingPage.vue` — no `<form>`; the single `<button>` (line 105, `@click="savePreferences"`) has no `type`. Add `type="button"`. One file.
- [x] [LOW] `billwatch-web/src/views/TrendingBillsView.vue` — no `<form>`; the `<button>` at line 42 (`@click="toggleView"`) has no `type`. Add `type="button"` to that one button. One file.
- [x] [HIGH] `billwatch-web/src/stores/bills.ts`: in `fetchTrending` (~line 43) `trendingBills.value = response.data` is unguarded. Change to `trendingBills.value = Array.isArray(response.data) ? response.data : []`. Only that line. One file.
- [x] [HIGH] `billwatch-web/src/stores/civic.ts`: line 28 `representatives.value = response.data.representatives` unguarded. Change to `representatives.value = Array.isArray(response.data.representatives) ? response.data.representatives : []`. Only that line. One file.
- [ ] [HUMAN-ONLY BLOCKED ITEM — auto-skipped after 3 failed cycles; needs a human] [HIGH] `billwatch-web/src/stores/legislators.ts`: in `fetchVotes` (line 58) `votes.value = response.data` unguarded. Change to `votes.value = Array.isArray(response.data) ? response.data : []`. Only that line. One file.
- [x] [HIGH] {ts·validation·T1·test-covered} `billwatch-web/src/stores/ranking.ts`: line 84 `rankedBills.value = response.data` unguarded (consumed by `.filter`). Change to `rankedBills.value = Array.isArray(response.data) ? response.data : []`. Only that line. One file.
- [x] [HIGH] {ts·validation·T2·test-covered} `billwatch-web/src/stores/priorities.ts`: line 63 `billsRequiringAction.value = response.data` unguarded. Change to `billsRequiringAction.value = Array.isArray(response.data) ? response.data : []`. Don't modify `recommendations`/`stats`. One file.
- [x] [HIGH] {py·security·T1·test-covered} `billwatch-backend/app/routers/advanced_auth.py`: `get_login_history` has `limit: int = Query(10, le=100)` (line 59) with no lower bound. Change to `limit: int = Query(10, ge=1, le=100)`. `Query` already imported. Only that line. One file.
- [x] [HIGH] {py·validation·T2·test-covered} `billwatch-backend/app/routers/ranking.py`: `get_recently_enacted` has bare `days: int = 30` (line 176). Change to `days: int = Query(30, ge=1, le=365)` and add `Query` to the `from fastapi import APIRouter, Depends, HTTPException` line (line 4). One file.
- [x] [HIGH] {py·validation·T2·test-covered} `billwatch-backend/app/routers/legislator_stats.py`: `get_legislator_voting_record` has bare `limit: int = 50` (line 47). Change to `limit: int = Query(50, ge=1, le=200)` and add `Query` to the fastapi import (line 5). One file.
- [x] [HIGH] {py·validation·T2·test-covered} `billwatch-backend/app/routers/admin.py`: `list_users` has bare `skip: int = 0` and `limit: int = 100` (lines 12-13). Change to `skip: int = Query(0, ge=0)` and `limit: int = Query(100, ge=1, le=500)`, and add `Query` to the fastapi import (line 2). One file.
- [ ] [HUMAN-ONLY BLOCKED ITEM — auto-skipped after 3 failed cycles; needs a human] [HIGH] {py·other·T2·test-covered} `billwatch-backend/app/services/trending.py`: convert ONLY the serialized-output `.isoformat()` calls — line 113 `timestamp`, 334 `added_at`, 356 `reached_at`, 386 `generated_at` — to `datetime.now(timezone.utc).isoformat()`, and add `timezone` to the import (line 5). DO NOT touch the cutoff-window lines 140/235. One file.
- [x] [MED] {vue·a11y·T1·test-covered} `billwatch-web/src/components/RepCard.vue`: the rep photo `<img :src="rep.photoUrl" :alt="rep.name" ...>` (line 6) is in a below-the-fold list. Add `loading="lazy"` to that `<img>`. Only that tag. One file.
- [x] [MED] {vue·a11y·T1·test-covered} `billwatch-web/src/layouts/MainLayout.vue`: the feedback button already has `aria-label`, so its inner icon `<svg class="w-6 h-6" ...>` (line 100) is decorative. Add `aria-hidden="true"` to that `<svg>`. Only that tag. One file.
- [x] [MED] {vue·a11y·T1·test-covered} `billwatch-web/src/components/FeedbackModal.vue`: the close button already has `aria-label`, so its inner icon `<svg class="w-5 h-5" ...>` (line 23) is decorative. Add `aria-hidden="true"` to that `<svg>`. Leave other svgs. One file.
- [x] [MED] {vue·a11y·T1·test-covered} `billwatch-web/src/pages/LegislatorCareerHistoryPage.vue`: the empty-state document icon `<svg class="w-12 h-12 mb-4" ...>` (line 222) is decorative. Add `aria-hidden="true"` to that `<svg>`. Only that tag. One file.
- [x] [LOW] {py·typing·T1·test-covered} `billwatch-backend/app/services/user_activity_service.py`: `def __init__(self):` (line 12) has no return hint. Change to `def __init__(self) -> None:`. Only that line. One file.
- [x] [LOW] {py·typing·T1·test-covered} `billwatch-backend/app/services/bill_search.py`: `def __init__(self, db: Session):` (line 8) -> `def __init__(self, db: Session) -> None:`. Only that line. One file.
- [x] [LOW] {py·typing·T1·test-covered} `billwatch-backend/app/services/performance_optimization_service.py`: `def __init__(self):` (line 12) -> `def __init__(self) -> None:`. Only that line. One file.
```

## Proposed (LLM re-evaluation — review before applying)
1. [HIGH] `billwatch-backend/app/services/trending.py` — Convert the four serialized output timestamps (lines 113, 334, 356, 386) from `datetime.utcnow().isoformat()` to `datetime.now(timezone.utc).isoformat()` and update the import on line 5 to include `timezone`; explicitly preserve the naive `datetime.utcnow()` usage on lines 140 and 235 for cutoff window comparisons.
2. [HIGH] `billwatch-web/src/stores/legislators.ts` — In the `fetchVotes` function (line 58), guard the assignment by changing `votes.value = response.data` to `votes.value = Array.isArray(response.data) ? response.data : []` to prevent runtime errors from malformed API responses.
3. [MED] `billwatch-android/app/src/main/java/com/policylogs/ui/screens/BillDetailScreen.kt` — Audit external links (e.g., Congress.gov URLs) for missing `rel="noopener"` or equivalent security attributes and add them to prevent reverse tabnabbing vulnerabilities.
4. [MED] `billwatch-android/app/src/main/java/com/policylogs/ui/screens/LegislatorsScreen.kt` — Add `loading="lazy"` or equivalent lazy-loading logic to legislator profile images to improve initial screen load performance and reduce memory usage.
5. [LOW] `billwatch-android/app/src/main/java/com/policylogs/data/repository/BillsRepository.kt` — Add explicit return type hints to `suspend` functions and ensure all public methods have KDoc comments to improve code maintainability and IDE support.

# Grooming proposal — shrike-labs-website — 2026-08-31 04:01

## Current Next Steps
```
- [x] [HIGH] `src/App.vue`: the 8 navigation `<button>` elements (logo line 67 and Services/About/Contact/Careers/Privacy/Terms/Get Started lines 72-79) have no `type` and aren't in a `<form>` (default submit). Add `type="button"` to each. Markup only. One file.
- [x] [HIGH] `src/components/BlogSection.vue`: the three `<button>`s — "All" filter (line 119), the `v-for` category filter (line 130), "Clear filters" (line 197) — have no `type` and aren't in a `<form>`. Add `type="button"` to each. One file.
- [x] (already-satisfied in code, implement-verified) [HIGH] `src/components/FooterSection.vue`: the "Privacy Policy" and "Terms of Service" `<button>`s in the Company `<ul>` (lines 67, 68) have no `type` and aren't in a `<form>`. Add `type="button"` to both. Don't touch the newsletter submit button. One file.
- [x] [HIGH] `src/components/BackToTop.vue`: the scroll-to-top `<button v-if="visible" @click="scrollToTop" ...>` (line 15) has no `type` and isn't in a `<form>`. Add `type="button"`. One file.
- [x] [MED] `src/components/BlogSection.vue`: the decorative search magnifier emoji at line 107 (`<span ...>🔍</span>`) is exposed to screen readers. Add `aria-hidden="true"` to that `<span>` (the input already has its own aria-label). One file.
- [x] [MED] `src/composables/useSEO.ts`: `export function useSEO(config: SEOConfig)` (line 19) returns nothing but has no return type. Add `: void` so it reads `export function useSEO(config: SEOConfig): void`. Don't change the body. One file.
- [x] [MED] `src/composables/useStructuredData.ts`: `export function useStructuredData(config: StructuredDataConfig)` (line 20) has no return type. Add `: void`. Don't change the body. One file.
- [x] [LOW] `src/components/ServicesSection.vue`: the service-card icon at line 51 (`<div class="text-4xl mb-4">{{ service.icon }}</div>`) renders a decorative emoji announced by screen readers. Add `aria-hidden="true"` to that `<div>`. One file.
- [x] [HIGH] `src/components/BackToTop.vue`: the `<button>` already has `aria-label="Back to top"`, but its child up-arrow `<svg>` is decorative and announced redundantly. Add `aria-hidden="true"` to that `<svg>`. One file.
- [x] [HIGH] `src/components/AboutSection.vue`: the FAQ toggle `<button>` exposes its label via `<span>{{ faq.question }}</span>`, so the decorative chevron `<svg class="w-5 h-5 text-slate-400 ...">` (~line 121) should be hidden. Add `aria-hidden="true"` to that chevron `<svg>`. One file.
- [x] [HIGH] `src/components/FooterSection.vue`: each social link `<a aria-label="GitHub|LinkedIn|Twitter">` already has an accessible name, so its child brand `<svg class="w-5 h-5" fill="currentColor" ...>` (lines 113, 118, 123) is decorative. Add `aria-hidden="true"` to each of those three social-icon `<svg>` tags. One file.
- [x] [MED] `src/components/ContactSection.vue`: the envelope `<svg class="w-6 h-6" ...>` (line 143) and location-pin `<svg class="w-6 h-6" ...>` (lines 155-158) sit beside visible `<h3>` labels, so they are decorative. Add `aria-hidden="true"` to both `<svg>` tags. One file.
- [x] [MED] `src/pages/AboutPage.vue`: the per-service icon `<div class="text-4xl mb-4">{{ service.icon }}</div>` (line 110) and founder avatar `<div class="text-6xl mb-6">emoji</div>` (line 139) are decorative. Add `aria-hidden="true"` to both `<div>` tags. One file.
- [x] [LOW] `src/components/CaseStudiesSection.vue`: the below-the-fold card image `<img :src="study.image" :alt="study.title" class="w-full h-48 object-cover">` (line 50) should be lazy-loaded. Add `loading="lazy"` to that `<img>`. One file.
- [x] [LOW] `src/components/BlogSection.vue`: the below-the-fold post image `<img :src="post.image" :alt="post.title" ... />` (lines 156-160) should be lazy-loaded. Add `loading="lazy"` to that `<img>`. One file.
- [ ] [HUMAN-ONLY BLOCKED ITEM — auto-skipped after 3 failed cycles; needs a human] [HIGH] `src/style.css` — no `prefers-reduced-motion` guard exists; fade-in keyframes and `html { scroll-behavior: smooth }` run for all users (WCAG 2.3.3). Add an `@media (prefers-reduced-motion: reduce)` block disabling the `animate-fade-in*` animations and smooth scrolling. One file.
- [ ] [HUMAN-ONLY BLOCKED ITEM — auto-skipped after 3 failed cycles; needs a human] [HIGH] {ts·config·T3·test-covered} `src/composables/useBlogPosts.ts` — the posts mapper sets `content: body.trim()` (raw markdown) though `marked` is a dependency, so `v-html` shows literal `#`/`**`. Import `marked` and set `content: marked.parse(body.trim()) as string`. One file.
- [ ] [HUMAN-ONLY BLOCKED ITEM — auto-skipped after 3 failed cycles; needs a human] [HIGH] {xml·docs·T2·unverifiable} `public/sitemap.xml` — omits the blog. Add `<url>` entries for `/blog`, `/blog/building-ai-applications-with-claude`, and `/blog/multi-platform-development-at-scale` (the two real markdown posts). One file.
- [x] [MED] {ts·other·T2·test-covered} `src/router.ts` — `scrollBehavior()` always returns `{ top: 0 }` (ignores `to.hash`) and there's no catch-all route. Make `scrollBehavior` return `{ el: to.hash }` when a hash is present, and add a catch-all `{ path: '/:pathMatch(.*)*', redirect: '/' }`. One file.
- [x] (already-satisfied in code, implement-verified) [MED] {vue·other·T2·test-covered} `src/App.vue` — the `useStructuredData` call has an empty `sameAs: []`. Populate it with `https://github.com/markhint22`, `https://linkedin.com/company/shrike-labs`, `https://twitter.com/shrikelabs`. One file.
- [x] [MED] {vue·a11y·T2·test-covered} `src/components/CareersSection.vue` — the 'Apply Now' `<button type="button">` has no click handler (does nothing). Replace it with an `<a>` using `mailto:mark@shrikelabsllc.com` and a `?subject=Application: {{ job.title }}` query. One file.
- [x] [MED] {vue·a11y·T1·test-covered} `src/components/TestimonialsSection.vue` — the avatar `<img>` in the v-for lacks `loading="lazy"` and intrinsic dims (below-fold CLS). Add `loading="lazy" width="48" height="48"` to that `<img>`. One file.
- [x] [LOW] {ts·perf·T2·test-covered} `vite.config.ts` — `build.rollupOptions.output.manualChunks` is undefined (one bundle). Add a `manualChunks` config splitting `vue`, `vue-router`, and `marked` into a vendor chunk. One file.
- [x] [LOW] {vue·a11y·T1·test-covered} `src/components/PartnersSection.vue` — the partner logo `<img>` in the v-for lacks `loading="lazy"` and dims. Add `loading="lazy" width="150" height="50"`. One file.
- [x] (already-satisfied in code, implement-verified) [LOW] {html·config·T2·unverifiable} `index.html` — the `<head>` has no theme-color and carries legacy no-op tags. Add `<meta name="theme-color" content="#dc2626">` and remove the two legacy meta tags (revisit-after, language). One file.
```

## Proposed (LLM re-evaluation — review before applying)
1. [HIGH] `src/composables/useBlogPosts.ts`: Import `marked` and update the posts mapper to set `content: marked.parse(body.trim()) as string` to render markdown correctly instead of raw text.
2. [HIGH] `src/style.css`: Add an `@media (prefers-reduced-motion: reduce)` block to disable `animate-fade-in*` animations and set `scroll-behavior: auto` for `html` to satisfy WCAG 2.3.3.
3. [HIGH] `public/sitemap.xml`: Add `<url>` entries for `/blog`, `/blog/building-ai-applications-with-claude`, and `/blog/multi-platform-development-at-scale` to include blog posts in the sitemap.
4. [MED] `src/components/PrivacyPolicy.vue`: Add `aria-hidden="true"` to any decorative icon `<svg>` or emoji `<div>` elements that are not essential for screen readers.
5. [MED] `src/components/TermsOfService.vue`: Add `aria-hidden="true"` to any decorative icon `<svg>` or emoji `<div>` elements that are not essential for screen readers.
6. [MED] `src/components/HeroSection.vue`: Add `loading="lazy"` and intrinsic `width`/`height` attributes to any below-the-fold `<img>` tags to prevent CLS.
7. [LOW] `src/pages/Privacy.vue`: Verify and add `type="button"` to any non-submit `<button>` elements to prevent default form submission behavior.
8. [LOW] `src/pages/Terms.vue`: Verify and add `type="button"` to any non-submit `<button>` elements to prevent default form submission behavior.

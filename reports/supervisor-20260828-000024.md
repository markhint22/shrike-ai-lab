# Supervisor digest — 2026-08-28 00:00

## Actionable (3)
- 37 unacked alert(s) — queue.sh alerts; newest: ongoing-test-automation-agent | build-gate reverted a commit that broke the build (syntax/import/parse error) — the model produced non-loading code
- DIVERGED clone: ongoing-iptv-apps — manual reconcile needed.
- Local-27B review flagged commit(s) — see supervisor report

## Local-27B review
billwatch@c921ff3: `set()` ignores the `ttl` parameter, always using the instance-level `ttl_seconds` for expiry, making the per-call TTL argument dead code.

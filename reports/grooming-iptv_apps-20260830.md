# Grooming proposal — iptv_apps — 2026-08-30 04:00

## Current Next Steps
```
- [x] Add `iptv-web/src/views/__tests__/AnalyticsView.accessibility.test.ts` mirroring the existing `GuideView.accessibility.test.ts` (assert a main landmark, a descriptive heading, all inputs have labels, and interactive elements have accessible names). New test file only.
- [x] Add `iptv-web/src/views/__tests__/DiscoverView.accessibility.test.ts` the same way. New test file only.
- [x] Add `iptv-web/src/views/__tests__/HomeView.accessibility.test.ts` the same way. New test file only.
- [x] Add `iptv-web/src/views/__tests__/SettingsView.accessibility.test.ts` the same way. New test file only.
```

## Proposed (LLM re-evaluation — review before applying)
1. Add `iptv-web/src/views/__tests__/EPGView.accessibility.test.ts` mirroring the existing `GuideView.accessibility.test.ts` (assert a main landmark, a descriptive heading, all inputs have labels, and interactive elements have accessible names).
2. Add `iptv-web/src/views/__tests__/PlayerView.accessibility.test.ts` mirroring the existing `GuideView.accessibility.test.ts` (assert a main landmark, a descriptive heading, all inputs have labels, and interactive elements have accessible names).
3. Add `iptv-web/src/views/__tests__/SearchView.accessibility.test.ts` mirroring the existing `GuideView.accessibility.test.ts` (assert a main landmark, a descriptive heading, all inputs have labels, and interactive elements have accessible names).
4. Add `iptv-web/src/views/__tests__/ProfileView.accessibility.test.ts` mirroring the existing `GuideView.accessibility.test.ts` (assert a main landmark, a descriptive heading, all inputs have labels, and interactive elements have accessible names).

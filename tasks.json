[
  {
    "id": "chickadee-railway-404",
    "type": "aider_fix",
    "enabled": false,
    "repo": "/home/mhintermeister/overnight-queue/repos/iptv_apps",
    "prompt": "The Railway-deployed backend for this project (Chickadee streaming app) is returning 404, per the deployment status tracked in the shared repo's CLAUDE.md. Note: this repo already has the asyncpg/aiosqlite async-driver fix applied (that's ruled out as the cause here). Investigate other common Railway causes instead: the app not binding to 0.0.0.0:$PORT, a wrong/missing start command in railway config (railway.json/nixpacks/Procfile-equivalent), a health-check path that doesn't match an actual route, or the service crashing on boot (check for a startup exception before assuming routing is the issue). Keep the change focused and minimal \u2014 don't refactor unrelated code.",
    "_comment": "Already fixed and pushed 2026-08-01 (PR open, awaiting review). Disabled to avoid redundant re-runs while on vacation."
  },
  {
    "id": "ripple-backend-html",
    "type": "aider_fix",
    "enabled": false,
    "repo": "/home/mhintermeister/overnight-queue/repos/social-media-manager",
    "prompt": "The deployed backend API is returning HTML instead of JSON on its endpoints (wrong content-type), per the deployment status tracked in the shared repo's CLAUDE.md. Note: this repo already has the asyncpg async-driver fix applied (that's ruled out as the cause here). Investigate other common causes instead: a catch-all/static-file/frontend route registered ahead of the API routes and swallowing them, a reverse-proxy/rewrite rule sending API paths to the frontend, or the API service not actually running (requests landing on a different static host/error page instead). Keep the change focused and minimal \u2014 don't refactor unrelated code.",
    "_comment": "Already fixed and pushed 2026-08-01 (PR open, awaiting review). Disabled to avoid redundant re-runs while on vacation."
  },
  {
    "id": "specpilot-railway-404",
    "type": "aider_fix",
    "enabled": false,
    "repo": "/home/mhintermeister/overnight-queue/repos/test-automation-agent",
    "prompt": "The Railway-deployed backend for this project (SpecPilot) is returning 404, per the deployment status tracked in the shared repo's CLAUDE.md (note: the frontend and the separate Fly-hosted backend are already reported working \u2014 this is specifically about the Railway-hosted piece). This repo already has both asyncpg and aiosqlite fixes applied (that's ruled out as the cause here). Investigate other common Railway causes instead: the app not binding to 0.0.0.0:$PORT, a wrong/missing start command in railway config, a health-check path that doesn't match an actual route, or the service crashing on boot. Keep the change focused and minimal \u2014 don't refactor unrelated code.",
    "_comment": "Already fixed and pushed 2026-08-01 (PR open, awaiting review). Disabled to avoid redundant re-runs while on vacation."
  },
  {
    "id": "ongoing-billwatch",
    "type": "aider_fix",
    "enabled": true,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/billwatch",
    "prompt": "This is billwatch, a federal legislation tracker. Reading input is fast and cheap here, but each response now generates at ~85 tokens/second and is hard-killed at 600 seconds (~40000 tokens), so you have plenty of room - still ask only for the 1-2 files that actually matter, but write a COMPLETE, correct change; do NOT rush out a half-finished or syntactically broken diff to save budget. OVERNIGHT_PROGRESS.md always exists (auto-created as a stub if this is the first run) - read it first. If it still just has the placeholder '(not yet reviewed)' text: read README.md, implement one small real improvement, and propose 5-10 future items using NEW: trailers in your commit message (the runner populates the Next Steps list). If it already has a real Next Steps list: pick the single top not-yet-done item, ask to add ONLY the 1-2 specific files that item actually needs (not more), and implement ONLY that one item with a minimal, focused change. Add/update a test for it if the project has a test setup - actually run the test suite if you find one and report the real pass/fail result, don't claim untested work was tested. Commit the code with a clear message. Report progress ONLY via commit-message trailers (never edit OVERNIGHT_PROGRESS.md yourself - it is read-only, maintained by the runner): add 'DONE: <the exact item text you finished>', 'NEW: <a new item>', and/or 'DECISION: <what and why>' lines at the end of your commit message. Stay on the current branch only - never touch main or develop."
  },
  {
    "id": "ongoing-gitlark",
    "type": "aider_fix",
    "enabled": true,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/gitlark",
    "prompt": "This is gitlark, an AI-powered code workspace (GitHub OAuth + Claude conversations + workspaces + code review): a FastAPI backend (pytest, in backend/) and a Vue 3 + Vite web app (vitest, in web/). Backend deps live in backend/.venv.\n\nWork the single top not-yet-done item in OVERNIGHT_PROGRESS.md Next Steps; ask for only the 1-2 files it names.\n\nREPO RULES:\n- Each item is ONE small single-file change. Keep it minimal and keep existing tests green.\n- Verification is FAST and TARGETED: the runner uses backend/.ovn-verify.sh (a smoke set + the changed module test), NOT the slow full suite - so make a clean focused change and let it verify.\n- Add or update a focused test for any NEW behavior you introduce; do NOT author test suites for code you are not changing (skip pure test-coverage items).\n- Backend tests run from backend/ (the repo-root pytest.ini has a coverage gate - ignore it).",
    "max_files": 3,
    "timeout_secs": 1200
  },
  {
    "id": "ongoing-task-manager-platform",
    "type": "aider_fix",
    "enabled": false,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/task-manager-platform",
    "prompt": "This is task-manager-platform (FlockWorks), an AI-powered task management app. Reading input is fast and cheap here, but each response now generates at ~85 tokens/second and is hard-killed at 600 seconds (~40000 tokens), so you have plenty of room - still ask only for the 1-2 files that actually matter, but write a COMPLETE, correct change; do NOT rush out a half-finished or syntactically broken diff to save budget. OVERNIGHT_PROGRESS.md always exists (auto-created as a stub if this is the first run) - read it first. If it still just has the placeholder '(not yet reviewed)' text: also read README.md, then replace the stub content with a short 'Current Status' summary (note: some older docs may describe a React Native mobile stack that was reportedly replaced with native iOS/Android - don't assume either way from the README alone, just note it as something to verify later) and a 'Next Steps' list of 5-10 small, single-PR-sized improvement ideas - reasonable guesses are fine. If it already has a real Next Steps list: pick the single top not-yet-done item, ask to add ONLY the 1-2 specific files that item actually needs (not more), and implement ONLY that one item with a minimal, focused change. Add/update a test for it if the project has a test setup - actually run the test suite if you find one and report the real pass/fail result, don't claim untested work was tested. Update OVERNIGHT_PROGRESS.md marking the item done with today's date, and add a couple more ideas if fewer than 3 remain. Commit the code + doc update together with a clear message. Stay on the current branch only - never touch main or develop.",
    "_paused_comment": "deferred by user (2026-08-29)"
  },
  {
    "id": "ongoing-shrike-labs-website",
    "type": "aider_fix",
    "enabled": false,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/shrike-labs-website",
    "prompt": "This is shrike-labs-website, the company marketing site. Reading input is fast and cheap here, but each response now generates at ~85 tokens/second and is hard-killed at 600 seconds (~40000 tokens), so you have plenty of room - still ask only for the 1-2 files that actually matter, but write a COMPLETE, correct change; do NOT rush out a half-finished or syntactically broken diff to save budget. OVERNIGHT_PROGRESS.md always exists (auto-created as a stub if this is the first run) - read it first. If it still just has the placeholder '(not yet reviewed)' text: read README.md, implement one small real improvement, and propose 5-10 future items using NEW: trailers in your commit message (the runner populates the Next Steps list). If it already has a real Next Steps list: pick the single top not-yet-done item, ask to add ONLY the 1-2 specific files that item actually needs (not more), and implement ONLY that one item with a minimal, focused change. Add/update a test if the project has a test setup - actually run the test suite if you find one and report the real pass/fail result, don't claim untested work was tested. Commit the code with a clear message. Report progress ONLY via commit-message trailers (never edit OVERNIGHT_PROGRESS.md yourself - it is read-only, maintained by the runner): add 'DONE: <the exact item text you finished>', 'NEW: <a new item>', and/or 'DECISION: <what and why>' lines at the end of your commit message. Stay on the current branch only - never touch main or develop.",
    "_paused_comment": "removed from active by user 2026-08-31 (small polished site, queue-complete)"
  },
  {
    "id": "ongoing-iptv-apps",
    "type": "aider_fix",
    "enabled": true,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/iptv_apps",
    "prompt": "This is iptv_apps (Chickadee Streams), a production IPTV streaming app: a FastAPI backend (pytest, in iptv-backend/) and a Vue 3 + Vite web app (vitest, in iptv-web/). Mobile (iOS/Android) is OUT of scope for this queue.\n\nWork the single top not-yet-done item in OVERNIGHT_PROGRESS.md's Next Steps; ask for only the 1-2 files that item names.\n\nREPO RULES:\n- Test policy: ALWAYS add or update a focused test for any source code you ADD or CHANGE - new code needs a test, that is expected and good. What you must NOT do is take on a standalone test-COVERAGE item that adds tests to code you are not otherwise changing (e.g. the a11y or auth-coverage items) - those need request/mock setup the local model gets wrong, so skip those items. Rule of thumb: test the code you touch; do not author test suites for code you do not touch.\n- Backend changes: keep pytest green. Web changes: keep `npm run build` and vitest green. Prefer small single-file source fixes (type fixes, error handling, empty states) that existing tests cover.\n- v1 polish is essentially complete, so a clean NO-OP is a perfectly good outcome when nothing source-level remains - do not invent risky work to look busy.",
    "protected_files": [
      "iptv-android/app/build.gradle.kts",
      "iptv-ios/ChickadeeStreams/Services/RevenueCatService.swift"
    ],
    "map_tokens": 4096,
    "timeout_secs": 900,
    "max_files": 4
  },
  {
    "id": "ongoing-social-media-manager",
    "type": "aider_fix",
    "enabled": false,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/social-media-manager",
    "prompt": "This is social-media-manager (Ripple), an AI content creation platform. Reading input is fast and cheap here, but each response now generates at ~85 tokens/second and is hard-killed at 600 seconds (~40000 tokens), so you have plenty of room - still ask only for the 1-2 files that actually matter, but write a COMPLETE, correct change; do NOT rush out a half-finished or syntactically broken diff to save budget. OVERNIGHT_PROGRESS.md always exists (auto-created as a stub if this is the first run) - read it first. If it still just has the placeholder '(not yet reviewed)' text: also read README.md, then replace the stub content with a short 'Current Status' summary and a 'Next Steps' list of 5-10 small, single-PR-sized improvement ideas - reasonable guesses are fine. Note: several social platform OAuth credentials (Instagram/Meta, Facebook, TikTok, Pinterest, YouTube) are known to still be missing - don't pick items that require secrets you don't have. Do not duplicate the existing overnight/2026-08-01/ripple-backend-html branch's work (a content-type routing fix, already pushed separately). If it already has a real Next Steps list: pick the single top not-yet-done item, ask to add ONLY the 1-2 specific files that item actually needs (not more), and implement ONLY that one item with a minimal, focused change. Add/update a test for it if the project has a test setup - actually run the test suite if you find one and report the real pass/fail result, don't claim untested work was tested. Update OVERNIGHT_PROGRESS.md marking the item done with today's date, and add a couple more ideas if fewer than 3 remain. Commit the code + doc update together with a clear message. Stay on the current branch only - never touch main or develop.",
    "_paused_comment": "deferred by user (2026-08-29)"
  },
  {
    "id": "ongoing-test-automation-agent",
    "type": "aider_fix",
    "enabled": true,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/test-automation-agent",
    "prompt": "This is test-automation-agent (SpecPilot), a test automation product. Reading input is fast and cheap here, but each response now generates at ~85 tokens/second and is hard-killed at 600 seconds (~40000 tokens), so you have plenty of room - still ask only for the 1-2 files that actually matter, but write a COMPLETE, correct change; do NOT rush out a half-finished or syntactically broken diff to save budget. OVERNIGHT_PROGRESS.md always exists (auto-created as a stub if this is the first run) - read it first. If it still just has the placeholder '(not yet reviewed)' text: read README.md, implement one small real improvement, and propose 5-10 future items using NEW: trailers in your commit message (the runner populates the Next Steps list). Do not duplicate the existing overnight/2026-08-01/specpilot-railway-404 branch's work (a Railway 404 fix, already pushed separately). If it already has a real Next Steps list: pick the single top not-yet-done item, ask to add ONLY the 1-2 specific files that item actually needs (not more), and implement ONLY that one item with a minimal, focused change. Add/update a test for it - this is a testing product, its own test coverage matters - actually run the test suite if you find one and report the real pass/fail result, don't claim untested work was tested. Commit the code with a clear message. Report progress ONLY via commit-message trailers (never edit OVERNIGHT_PROGRESS.md yourself - it is read-only, maintained by the runner): add 'DONE: <the exact item text you finished>', 'NEW: <a new item>', and/or 'DECISION: <what and why>' lines at the end of your commit message. Stay on the current branch only - never touch main or develop."
  },
  {
    "id": "ongoing-xlite",
    "type": "aider_fix",
    "enabled": true,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/xlite",
    "prompt": "This is XLite, an XCOM-style isometric pixel-art turn-based tactics game in Godot 4 (GDScript), mobile-first for iOS/Android. It is verified by GUT: tests under tests/ run automatically after your commit via JUnit XML - you never run Godot yourself.\n\nWork the single top not-yet-done item in OVERNIGHT_PROGRESS.md's Next Steps; ask for only the 1-2 .gd/.tres files that item names.\n\nREPO RULES:\n- GDScript gotchas that cause auto-reverts: gameplay LOGIC is in cartesian cell space, RENDERING is in isometric pixel space - never mix them; every gameplay class uses `class_name` (not imports); use explicit `var x: T =` and NEVER `:=` type-inference through an untyped value (it fails on a cold import and the whole file gets reverted).\n- SAFEST, highest-success work (do this by preference): author new `.tres` mission/data files, and add focused GUT tests for pure-logic classes - both land green reliably. When you add new pure-logic code, add a GUT test for it in the same cycle. Editing existing gameplay .gd source is higher-risk; keep such diffs tiny and make sure the file still parses.\n- Some files are HARD-BANNED (listed in .queue-hard-banned-files) - never open or touch them.",
    "map_tokens": 4096,
    "_paused_comment": "user-managed (playtest + Claude features); excluded from the all-repos MVP push"
  },
  {
    "id": "ongoing-shrike-notify",
    "type": "aider_fix",
    "enabled": true,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/shrike-notify",
    "prompt": "This is shrike-notify, a topic-based publish/subscribe notification service (FastAPI, ntfy-style) in the Shrike Dev Kit. Each response streams at ~85 tok/s, hard-killed at 600s, so write COMPLETE correct changes, not rushed broken diffs. OVERNIGHT_PROGRESS.md always exists - read it first. It already has a real Next Steps list: pick the single top not-yet-done item, ask for ONLY the 1-2 files it needs, and implement ONLY that item with a minimal focused change. The backend is Python/FastAPI at backend/app with tests in backend/tests (pytest, asyncio_mode=auto) - add/update a test and actually run backend/.venv/bin/pytest, reporting the real pass/fail. Commit with a clear message. Report progress ONLY via commit trailers (never edit OVERNIGHT_PROGRESS.md): DONE: <exact item text>, NEW: <new item>, DECISION: <what/why>. Stay on the current branch only - never touch main or develop."
  },
  {
    "id": "ongoing-shrike-monitor",
    "type": "aider_fix",
    "enabled": true,
    "persistent_branch": true,
    "repo": "/home/mhintermeister/overnight-queue/repos/shrike-monitor",
    "prompt": "This is shrike-monitor, an uptime + heartbeat (dead-man's-switch) monitoring service (FastAPI) in the Shrike Dev Kit. Each response streams ~85 tok/s, hard-killed at 600s - write COMPLETE correct changes, not rushed broken diffs. OVERNIGHT_PROGRESS.md always exists - read it first; it has a real Next Steps list: pick the single top not-yet-done item, ask for ONLY the 1-2 files it needs, implement ONLY that item minimally. Backend is Python/FastAPI at backend/app, tests in backend/tests (pytest) - add/update a test and actually run backend/.venv/bin/pytest, report real pass/fail. Commit clearly. Report ONLY via commit trailers (never edit OVERNIGHT_PROGRESS.md): DONE:/NEW:/DECISION:. Stay on the current branch - never touch main or develop."
  }
]